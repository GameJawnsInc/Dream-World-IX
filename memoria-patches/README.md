# `memoria-patches/` — engine patches for Dream World IX

Dream World IX runs **novel** fields on stock [Memoria](https://github.com/Albeoris/Memoria) and needs a
small **custom engine** only for **forked** fields. The full rationale is in
[`../ff9mapkit/docs/ENGINE.md`](../ff9mapkit/docs/ENGINE.md). These files are unified diffs against a Memoria
source clone — apply the *live* set and compile `Assembly-CSharp`, or just grab the pre-built
`dwix-custom-memoria-<version>.zip` from the GitHub **Releases** page.

This README is the per-file status map (which patches are live, which ship in the bundle, which are dead
history) so a reader isn't left guessing what each file is for.

## Live fork-fidelity set — SHIPPED in the engine bundle

These wrap the hardcoded `fldMapNo == N` engine gates with an *effective field id* so a custom fork inherits
the original field's tuned behavior. This is the set that makes a forked field play faithfully.

| File | What it restores |
|---|---|
| `s23-narrow-map-fork-width.patch` | A forked narrow field gets the donor's exact tuned screen width (no letterbox loss). `s23-narrow-map-fork-width.PR.md` is its upstream write-up. |
| `s24-fork-donor-remap.patch` | The fork→donor remap suite (`EffectiveFieldId` / `ForkSiblingField` / `IsForkField`) — off-mesh exemptions, the fake-battle return field, overworld→fork entry, the no-encounter wrap, and the rest. Folds in the intermediate s25–s28 milestone steps; there is **no separate s25–s28 file**. |
| `s29-fork-donor-softlocks.patch` | The same `EffectiveFieldId` wrap on the remaining late-game (disc 2–4) softlock gates (Iifa / Burmecia / Gulug / Oeilvert / Esto Gaza / Ipsen / Epilogue). Disc-1 gates are in-game proven; these late-disc gates are still being playtested as those zones are forked. |
| `s30-doeventcode-fork-walk.patch` | `EventEngine.DoEventCode.cs` — the file the s24 gate census MISSED. Its per-field event gates use a local alias `Int16 mapNo = fldMapNo` (not the literal `fldMapNo`), so a `grep fldMapNo` census skipped all ~150 `mapNo == N` gates. Adds `Int32 effMapNo = EffectiveFieldId(mapNo)` and routes every gate comparison through it (the 2 non-gate uses — the VoicePlayer arg + `sOriginalFieldNo = mapNo` — stay raw). Restores the per-field scripted-walk destination corrections (the `MOVE`/Walk fixups), e.g. field 53's "Blank jumps in hole" reposition (`destX 250→330`) that puts the falling characters over the hole. ★ IN-GAME PROVEN — fixed the character *positioning* for the 6003 fall (the *occlusion* needed s31). |
| `s31-fork-name-keyed-overlay-offset.patch` | The fork-gate census also missed a whole **NAME-string-keyed** class: `FieldMapExtraOffset` (`mapExtraOffsetList.txt`) tunes per-field overlay z-offsets keyed on the FBG **name**, not `fldMapNo` — so `EffectiveFieldId` (id-based) never reached it. A fork's deployed name (`FBG_N11_PRIM_TSHP_TH_MET`) misses the donor's entry (`FBG_N00_TSHP_MAP006C_TH_MET_4,-1,29,0,-100`), so the floor overlay keeps its raw depth during the fall-move and stops occluding the falling characters. Adds `DataPatchers.EffectiveFieldName(name)` — the name-space analogue of `EffectiveFieldId` (resolves a fork's FBG name → its donor's via the global id↔FBG-name table) — and routes `SetOffset` / `UpdateOverlayOffset` through it on a dict miss. Fixes the WHOLE name-keyed-offset class for every fork, not just 6003. ★ **IN-GAME PROVEN 2026-06-23** — the 6003 jump-into-hole now occludes the falling characters, matching real field 53. |
| `s32-fork-name-gates.patch` | The **rest** of the name-keyed gate class, from a follow-up engine census (the sibling sweep s31 flagged): 7 more sites where engine behavior is keyed on the FBG name a fork doesn't share, wrapped with the same `EffectiveFieldName` lever (generalized in this patch to resolve by the *passed name* so it is correct even at cross-field call sites like the SPS `ChangeFieldOrigin`). **Tier-A (gameplay-breaking on a fork):** the Iifa telescope/harp minigame player- + menu-control unlock (`UIManager.cs`, 2 sites — without it the fork freezes the player), the Conde-Petie space-scene menu unlock (`UIManager.cs`), the Iifa rain-particle world offset (`FieldRainRenderer.cs` — else rain spawns at origin), the Oeilvert star-display opaque-shader override (`BGSCENE_DEF.cs`). **Tier-B:** the per-field SPS sprite z-offsets (`FieldMapSPSExtraOffset.cs`, name-keyed dict like s31) and the Treasure-Hall +8px overlay nudge (`FieldMap.cs`). All identity-safe for real (non-forked) fields. |
| `s33-fork-fldmapno-arg-lookups.patch` | A **THIRD** fork-gate class the censuses missed: `fldMapNo` used as a per-field **LOOKUP ARGUMENT** (not a `== N` comparison like s24/s30, nor a name key like s31/s32). Found via the blank menu **LOCATION** field on a fork. The place-name is looked up by raw field id in `loc_name.mes` (field-id-keyed → a fork's custom id returns empty → blank) at **two** sites: `EventEngine.cs:536` (`mapNameStr`, the debug title) and `MainMenuUI.cs:497` (the in-field menu label — a *separate direct call* the title-bar fix alone missed). Adds `FF9TextTool.FieldLocationName(fldMapNo)` — try the field's **own** id first, else the fork's **donor** — and routes both through it. **Authorable title:** a new `LocationName <id> <title>` DictionaryPatch directive (`DataPatchers.CustomLocationNames`, consulted first by `FieldLocationName`, survives language reloads) lets the kit's `[field] location = "…"` set a from-scratch field's place-name **or override** a fork's inherited donor title. A sweep found 4 sibling argument-lookups, also wrapped: the field→battle **BGM** fallback (`BattleSwirl.cs` + `HonoluluFieldMain.cs` `GetMusicForBattle` — the kit's per-scene BattlePatch override still fires first, so this only changes the *fallback* to the donor), the **mesh-combine** render path (`BGSCENE_DEF.cs` — Crystal World / Iifa Roots / Oeilvert / Pand. Maze), and the **smooth-cam** exclusion (`FieldMap.cs`, ×2). **Deferred** (low value / fiddly): the moogle auto-save eligibility (`IsMoogleField` — gates only the optional `AutoSaveOnlyAtMoogle`, *not* manual save), the cosmetic field-audio preset, the custom-model swap. ★ The menu LOCATION fix is the reported bug; the sweep siblings are IN-GAME UNVERIFIED, verify opportunistically. |

**Area-title gates — investigated, NO fix (a false alarm, resolved 2026-06-23):** the two deferred gates were *not* an asset risk. `FieldMap.SetFieldMapAtlasName` (the `atlas_<lang>` rename) has **zero runtime callers** — dead code — so the missing-texture premise never existed; the runtime atlas load is a flat `Load("atlas")` (`BGSCENE_DEF.cs:839`), and the title lettering is baked into the base `atlas.png` every fork ships (a native Mognet-Central fork renders the card, in-game proven — `FORK_FIDELITY.md`). So English area titles already work on forks (synth forks correctly *hide* them; `--verbatim` shows them via the carried donor `.eb`). `FieldMapLocalizeAreaTitle.GetInfo` only gates the **non-English** `_<lang>.bgs` title-*geometry* swap (`LoadLocalizationInfo` returns early for US), which a native/editable fork can't use without shipping 7 localized `.bgs` sidecars per fork — a cosmetic, non-English-only follow-up, intentionally left (BG-borrow + same-name forks already get it via the donor bundle).

## Custom-overworld (Path C) — NEW capability

| File | What it adds |
|---|---|
| `s39-world-selfheal-control.patch` | **The overworld black-screen SELF-HEAL** (`ff9.w_worldSelfHealControl`, captured from the live tree 2026-07-15). Recovers the "no controlled actor" state (`project-ff9-overworld-actor-brick`): editing overworld geometry under a saved spawn leaves the leader with no `wmActor` / no control designator → `ff9.GetControlChar()` null → `w_frameUpdate` keeps `w_moveActorPtr` on the DUMMY → the camera has nothing to follow → **BLACK, baked into the save**. Called from `w_frameUpdate` case 0 ONLY when `GetControlChar()` is null; rebuilds the control body, ensures every active world actor has a `wmActor` (`w_movementChrInitSlice` dereferences it with no null check), force-grounds via a sky-cast, and binds the camera actor. **SELF-TERMINATES** (once it binds a real actor the else-branch never fires again — no one-shot flag) and is try/catch-wrapped so recovery can never crash the frame loop. Never touches the Bee scene. ✅ The author's TODO on the degenerate-spawn fallback is RESOLVED (verified with the kit's engine-faithful ground query, `world/placement.py`, against stock disc-1 blocks): `kWorldSafeFallback` (768, −640) — the map centre — IS open sea; it sits on the corner of blocks [11][9]/[12][9]/[11][10]/[12][10], three of which do not exist (open sea) and the fourth grounds on Sea4 (topograph 57). Replacement chosen and verified walkable: **(1092, −788)** — Block[17][12], the Dali plain (the first overworld ground a new game walks), a single walkable sheet, ground_y 11.4, dominant plains topograph 49, gentle slope, ~30u clear of the Dali entrance trigger. The constant fix lands with the b18 bundle rebuild. |
| `s34-worldmap-mesh-override.patch` | Load a world-map block TERRAIN mesh from a loose **`.ff9mesh`** file in a mod folder instead of the baked Resources mesh — so overworld geometry can be edited **without** an AssetBundle repack (FF9 is Unity 5.2.3; `AssetManager.LoadFromDisc<Mesh>` is unsupported, so there is no other loose-mesh hook). New class `Memoria.World.WorldMeshOverride` (reads the kit's [`ff9mapkit.world.mesh`](../ff9mapkit/ff9mapkit/world/mesh.py) format) + a hook in **`WMWorld.RegisterBlockComponent`** — the RUNTIME block-stream path. ⚠ The first cut hooked `WMWorldPrefabMaker.LoadMesh`, which is reachable only from `LoadModelAsset` — an **editor-only prefab-builder with ZERO runtime callers** (the overworld loads a *pre-built* WorldDisc prefab and streams blocks via `WMWorld.LoadBlock`), so it never fired. The live hook reads `block.InitialX/InitialY` (grid coords) + `transform.name` to build the override path, and swaps the `MeshFilter.sharedMesh` (render) **and** the `mesh` fed to `WMBlock.AddWalkMesh` (collision + the `tangent.x` entry id) — one object. **The override is GENERIC over `transform.name`, not terrain-only**: any part (`Terrain` / `Object` / `Sea1..6` / `Beach1`) that has a component to override takes this path, and for `ObjectForm1` the loose mesh IS fed to the Form1 walkmesh (`AddWalkMeshForm1`), registered BEFORE `TerrainForm1` — so a baked Object override SHADOWS the terrain in the ground query unless its triangles carry a `WMPhysics`-skipped IDALL (4078/4088/2040). **Also carries `RegisterBareObjectOverride`**: a block with a `TerrainForm1` but NO stock `ObjectForm1` gets an Object child created from the terrain template so a loose `Object` `.ff9mesh` renders at all — that one is render-only on purpose (`AddForm1Transform`, no `AddWalkMeshForm1`), since a whole 3D building mesh as a collider turns its back-face-culled walls + buried base into invisible collision. Identity-safe: no override file → stock mesh. **Also add `<Compile Include="Memoria\World\WorldMeshOverride.cs" />` to the csproj** — ⚠ that Compile line currently lives in **`s22`**, misattributed but load-bearing (it is the only patch adding it); moving it here is deferred while the csproj is in flight (see STACK HEALTH). **Also carries the custom-ocean loose-TEXTURE override** (recaptured from the live tree 2026-07-15, previously in no patch): `WorldMeshOverride.TryLoadTexture` + a render hook in `RegisterBlockComponent` that clones the material and swaps `mainTexture` when a loose custom texture exists for a cell+part, **keeping the sea shader** — so a custom ocean renders as a continuous gradient instead of discrete tile shades. Cell-scoped; identity-safe (no override file → stock material). ★ Built + deployed 2026-06-30 (re-hooked + rebuilt clean); ★ IN-GAME PROVEN by continuous use — the dedicated `world-deploy --spike` playtest was never run as such, but every custom-overworld playtest since rides this override: the Southern Ring's 56 reclaimed disc-1 cells and the Path D synthetic-disc rounds all load through it (`studies/path-d-new-world/RUNGS-0-3-PLAYTEST.md`: those cells are "the already-in-game-proven path"; `DRAFT.md`: the reclaim/donor route is "in-game-proven as of s34"). The foundation for Path C (geometry-edit the real world) and Path D (a minted continent's geometry). → `project-ff9-worldmap-feasibility`. |

## A genuinely THIRD overworld (Path D) — NEW capability, ★ PROVEN IN-GAME (the 9013 field↔world round trip owner-confirmed 2026-08-05 → `studies/path-d-new-world/rung6/PLAYTEST.md`)

Rungs 0–2 of [`../studies/path-d-new-world/PLAN.md`](../studies/path-d-new-world/PLAN.md). All three built +
deployed together 2026-07-29 (0 errors, both arches, `Output\Assembly-CSharp.dll` == both deployed copies,
sha `B60003A67130A90A…`; pre-build full DLL backup **`20260729-153010`** → `py tools/restore_memoria_dll.py
20260729-153010`). **Every one of them is INERT until deliberately armed**, so a normal disc-1/4 load — and
every other concurrent worktree sharing this install — is unaffected. Gate: the three patches apply forward
in order (`patch -p1 --binary`) onto pristine snapshots and reproduce the live tree **byte-for-byte** on all
five touched files. ⚠ Bundling note: three patches, but each in-game test still changes exactly ONE variable
(`s72` does nothing without a `WorldScene` DictionaryPatch line; `s71` does nothing until its toggle is
armed) — the bundling buys one relaunch instead of three, it does not merge the success signals.

| File | What it adds |
|---|---|
| `s70-debug-menu-reach-widen.patch` | **THE REACHABILITY FIX (Rung 0) — the prerequisite nothing else could be observed without.** `Ff9mkDebugMenu.ForceWorldState` hard-rejected any id outside `9000-9012`, and it is the ONLY route to a chosen `wldMapNo`: the other candidate, `WMBeeMenu`'s "Jump To", is a same-world POSITION teleport whose host `Bee` object `WMScriptDirector.HonoAwake` destroys on every world load — doubly dead. Widened to `1-32767` (`wldMapNo` is `Int16` and `ArmWorldReload`'s cast is unguarded, so a larger number would silently wrap onto a different, possibly REAL dispatcher). The engine's own dispatch path never had this gate: `ff9InitStateWorldMap` just does `FF9DBAll.EventDB[MapNo]`. Also adds a `Memoria.Prime.Log.Message` line — **the only channel that reaches `Memoria.log`; plain `Debug.Log` does not** (no Unity→Memoria bridge exists; this file's three existing `[ff9mk]` `Debug.Log` calls are dead-lettered, and the plan's stated verify channel was therefore unreadable). ⚠ `ArmWorldReload` has two preconditions the plan did not price: `UIManager.State == WorldHUD` **and** `sys.mode == 3` — it can only be fired while already standing on an overworld. Worth keeping permanently; every later rung needs it. |
| `s71-worlddisc-runtime-spike.patch` | **THE WORLDDISC SPIKE (Rung 1) — THROWAWAY, the pivotal unknown of the whole plan** (`s63`/`s67` house style: remove or re-gate once it has answered). New `Memoria/World/WorldDiscSpike.cs` (+ csproj include) mints a complete 24×20 `WMBlock` grid in C# at runtime and assigns it to `WMWorld.WorldDisc`, hooked at the top of `Initialize()` **before** the `:105` `BuildBlockArray` — `InitialBlocks` and `Blocks` must come from the SAME hierarchy or `Block31/115/219`, `ResetBlockForms` and the `ff9.cs` grid lookups silently address the wrong one. There is no smaller version: `OnInitialize`'s position loop walks all 480 cells with no null guard, so day one needs a complete gap-free grid. Armed from a **session-only, default-OFF** debug-menu toggle (the `_vehicleForceAll` idiom) rather than an `.ini` flag — an ini flag persists and costs a relaunch between Rungs 0 and 1; a toggle is off on every launch, which matters on an install shared by ~26 concurrent worktrees. **Three landmines the plan's own sketch would have died on, all found by re-reading source and all fixed here:** (1) `Form2Transforms` is declared with no initializer and `LoadBlock` re-creates only THREE of the four collections while `ApplyForm()` foreaches it unconditionally — all four are now assigned (free: `LoadBlock` overwrites three moments later); (2) `CurrentX`/`CurrentY` are read by `GetAbsoluteBlock`/`SwapBlock`/`DetectUnseenBlocks` and stock seeds them alongside `Initial*` — left 0, every lookup returns cell (0,0); (3) with no dispatcher there is no player, so `w_moveActorPtr` stays the origin-parked dummy and `DetectUnseenBlocks` indexes `Blocks[0,-1]` → `IndexOutOfRangeException` **that has nothing to do with the substitution** and would be misread as the spike failing — the sentinel is now parked mid-grid at (12,10) so its ±8 window stays in bounds. ⚠ The plan's "all cells sea, no override lookups" premise is **refuted by the live install**: 56 disc-1 `Terrain.ff9mesh` overrides + `Donor.txt` sidecars already ship in `FF9CustomMap-world`, so 56 of 480 cells take the s34 reclaim path. Deliberately NOT suppressed — those cells are the already-proven path, and an ocean containing only the Southern Ring's reclaimed cells is an unmistakable success picture. |
| `s73-third-worlddisc-wire.patch` | **THE PERMANENT WIRE (Rung 3→5).** Promotes `s71` from a manual debug toggle to an automatic per-id wire: `WorldDiscSpike.Latch(wldMapNo)` engages a synthetic disc for any id in the **reserved Path D band 9013-9099** (verified clear across both live `DictionaryPatch.txt` files apart from 9013 itself), or via the session-only toggle for experiments on other ids. `Latch()` ALWAYS assigns, so an engaged load can never leak into the next real disc-1/4 load, and it latches ONCE per load into `Engaged` so every downstream consumer (the `s74` tag, the special-object guard) agrees even if the toggle is flipped mid-load. Reads `FF9StateSystem.Common.FF9.wldMapNo`, which is already populated when `WMWorld.Initialize()` runs (`WMScriptDirector.HonoAwake` sets it upstream), wrapped defensively. `s71` is retired-not-deleted per the `s63`/`s67` house style — its patch file stays as history and its debug toggle stays as the manual override. |
| `s74-sentinel-disc-override-namespace.patch` | **THE OVERRIDE NAMESPACE (Rung 5 prerequisite).** New `private Int32 overrideDiscTag` on `WMWorld` (⚠ `private`, no `[SerializeField]` — `WMWorld` is baked into a prefab and ANY new serialized field shifts its layout into a corrupt deserialize/black screen; same reason `currentDisc` is private), assigned in `Initialize()` once `currentDisc` is final and gated on `WorldDiscSpike.Engaged`, so real play is byte-identical. Threaded through the **six** `WorldMeshOverride` lookups across five methods — `LoadBlock`'s `HasLandOverride`, `ResolveReclaimDonor`'s `TryReadDonorPath`, `RegisterBlockComponent`'s mesh + texture lookups, `RegisterBareObjectOverride`, and `UpdateLoadBlocks`' streaming mirror. **Two corrections to the plan's own design, both verified by direct read.** (1) It said `RegisterBlockComponent`/`RegisterBareObjectOverride` need new parameters — they do NOT: both are instance methods already reading `this.currentDisc`, and the parameter-threading alternative would have needed THREE signature changes, not two. (2) It said `WorldMeshOverride.cs` needs **zero** change — **false, and a silent-failure trap**: `TryReadDonorPath` spends its single `disc` argument on two different jobs, the loose `Donor.txt` override key (`:94`) AND a REAL `WorldMap/Prefabs/WorldDisc{disc}/…` Resources path (`:112`). A sentinel threaded through both resolves every donor to a nonexistent `WorldDisc9` prefab → `AssetManager.Load` returns null → `ResolveReclaimDonor` falls back to the plain inland donor, dropping every beach/sea/foam carry **with nothing logged**. Split into a 4-arg `TryReadDonorPath(overrideDisc, assetDisc, x, y)` with a 3-arg overload so every existing caller is untouched. Sentinel disc = **9** (safe: `w_frameDisc` is only ever 0/1/4 and the only `WorldMap/Disc<N>` folders on disk are 1 and 4). **Also carries the special-object guard** — the four ACTIVE `block.Number` cases (219 Water Shrine, 389 the Daguerreo bridge prefab, 91 + 115 quicksand; the other six cases are empty stubs) are suppressed on a synthetic disc, so a new continent does not inherit stock landmarks just for reusing their grid coordinates. Found by an owner sailing the all-sea 9013 world → `studies/path-d-new-world/INHERITED-STOCK-BEHAVIOR.md`. Built + deployed both arches, `Output` == both deployed copies; pre-build backup **`20260729-165601`**. ★ IN-GAME PROVEN — s75's Rung 5a close rode this namespace (CLONE mode resolves per-cell overrides through it, owner-confirmed 2026-07-30), and rung 6 sealed the arc: the 9013 field↔world round trip owner-confirmed 2026-08-05 ("! showed and warped me back, the loop works") → `studies/path-d-new-world/rung6/PLAYTEST.md`. |
| `s75-worlddisc-clone-mode-and-mist.patch` | **CLONE-vs-BLANK MODE + MIST SUPPRESSION (Rung 5, owner decisions 2026-07-29).** Three session-only statics on `WorldDiscSpike`: **`CloneStockWorld`** (default false = BLANK, a true 480-all-sea empty canvas; true = copy the STOCK world's per-cell flags off the real baked hierarchy so the synthetic grid renders the stock world map while still resolving per-cell overrides through the s74 Path-D namespace — the owner's "reshape a stock world without touching it" non-destructive sandbox); **`SuppressMist`** (default true — "the new continent is CLEAN OF MIST"; read by `WorldConfiguration.UseMist()`; NOT purely cosmetic, `w_frameFog` is part of the encounter lookup key); **`SuppressStockLandmarks`** (getter `Engaged && !CloneStockWorld` — a BLANK world suppresses the four cell-keyed stock landmark cases (219 Water Shrine / 389 Daguerreo bridge / 91+115 quicksand) it would otherwise inherit for reusing their grid coordinates, a CLONE deliberately keeps them, and off a synthetic disc it is always false so real play is untouched). ★ In-game confirmed 2026-07-30 with the Rung 5a close: CLONE mode carves the island into the same cells (F2), mist suppression toggles live (F3). |
| `s72-worldscene-directive.patch` | **THE `WorldScene` DIRECTIVE (Rung 2)** — the `FieldScene`/`BattleScene` sibling for the world lane, in `DataPatchers.cs`. `WorldScene <id> <NAME> [mesID]` writes `FF9DBAll.EventDB[id] = "EVT_WORLD_" + NAME`. **Registers `EventEngineUtils.eventIDToMESID[id]` too — a second unguarded indexer the plan missed entirely**: `ff9ShutdownStateFieldMap` reads it when a field EXITS to the world map, firing while still in the old field, before the world scene even loads. Defaults to mesID **68**, the shared world text block every stock world id maps to; reusing it deliberately avoids minting a new message-file id, which would shadow some real location's dialogue. Inert until a `DictionaryPatch.txt` actually carries a `WorldScene` line. ⚠ EventDB is ONE flat namespace shared with `FieldScene`/`BattleScene`, last-write-wins with no collision check — a world dispatcher id must be unique across every stacked mod folder, not just among world ids. |

## Multiplayer co-op (experimental) — NEW capability

| File | What it adds |
|---|---|
| `s36-netsync-ghost.patch` | **Exploration co-op "ghost sync"** — two players see each other walk a shared field in real time. New namespace `Memoria.Netsync`: `NetSyncClient` (a `DontDestroyOnLoad` MonoBehaviour bootstrapped from `HonoluluFieldMain.HonoAwake`) + **two transports behind `INetTransport`**: `NetSyncSocket` (direct LAN TCP host/client, background read/write threads, lock-guarded latest-state slots, staleness timeout) and `NetSyncRelay` (a hand-rolled RFC-6455 WebSocket **client** for the public rendezvous relay — handshake, client-masked binary frames, ping/pong, reconnect, a 75s pre-pairing receive window matched to the relay's 60s pair timeout); plus one `HonoAwake` line and three csproj `<Compile Include>`s. Reads a `[Netsync]` section from `Memoria.ini` (`Enabled` / `Role`=selftest\|host\|client / `PeerAddress` / `Port` / `TargetField` / `RelayUrl` / `SessionCode` / `InsecureTls`); `RelayUrl` set → relay transport, empty → direct TCP; **OFF by default** (`Enabled=0` → nothing is created, zero footprint on a normal game). **`TargetField = 0` (the default) = co-op EVERYWHERE** — the transport dials once and lives for the whole session, each side broadcasts its true location every frame (a field-0 sentinel during menus/battle/transitions — which also fixed a latent bug where the 30 Hz keepalive re-sent a stale field id and FROZE the departing player's ghost on the peer's screen), and ghosts appear on any screen both players share (`gMode==1` — battle and the overworld are out of scope); a nonzero `TargetField` restricts to that one field (the original hangout mode). The overlay names the peer's current field ("your friend is on another screen — field N"). ★ Everywhere-mode solo tier IN-GAME PROVEN 2026-07-12 (selftest ghost follows into stock fields; nothing on the world map or in battle; clean reconnect on field re-entry); the cross-machine bits (peer-elsewhere overlay, leave-a-field despawn) await the next two-machine session. A relay **host with no `SessionCode` auto-generates a random namespaced `ff9-XXXXXXXX` code and persists it into `Memoria.ini`** (the code is the session's access gate; a guest with no code refuses to dial). **`[Netsync]` HOT-RELOADS**: the client watches `Memoria.ini` (mtime-gated file stat every ~2s + a parsed-value signature, so its own code-persist isn't an "edit") and applies changes to a RUNNING game — transport redialed, ghost/overlay reset; enabling co-op from OFF applies at the next field load (nothing is created while disabled). The relaunch-per-edit rule is dead for this section (★ in-game proven 2026-07-12: enabling from off appeared at the next field transition; a live disable vanished the overlay in ~2s, no relaunch). ⚠ The game's old Mono has no TLS 1.2, so `RelayUrl` should point at the local [`tools/netsync-bridge/`](../tools/netsync-bridge/README.md) (`ws://127.0.0.1:49201`), which carries the connection to the relay over TLS. On a shared field it spawns the peer as a "ghost" **wearing the peer's OWN model** (wire v2, 38 bytes: `{field,anim,model,meshVis,pos,rot}` — the model id resolves through `FF9BattleDB.GEO` exactly like the event engine spawns actors, re-dresses live when the peer changes character, and falls back to Zidane ONLY when the id can't resolve/load, e.g. a modded character this install lacks; the peer's own 16-bit per-submesh visibility mask drives sheathed weapons etc., replacing the old local-player index-mirror; v1 peers are length- **and** version-rejected → mixed engine versions silently don't sync). The model recipe is `FieldMap.AddPlayer` minus the controller — renderers enabled + masked, `GeoTexAnim` texture anims, huge skinned-mesh bounds so the PSX-shader-projected model isn't frustum-culled — and the transform + animation interpolate with exponential smoothing (`LerpRate=12`) to hide network jitter (anim ids are global, so the peer's clips bind natively on their rig — the upside-down-Zidane bug is dead). ★ Non-Zidane ghosts solo tier IN-GAME PROVEN 2026-07-12 (a correct right-side-up Garnet mirror, re-dress on a mid-session character swap, Zidane regression clean); cross-machine awaits the next two-machine session. NO field/battle/story-state sync (each save is independent). ★ Direct-TCP **IN-GAME PROVEN 2026-07-08** — two machines over WiFi, each rendering the other's live Zidane ghost moving + animating. ★ Relay path **IN-GAME PROVEN 2026-07-11** — two machines each through a local bridge to the public relay over TLS, paired by an auto-generated session code; the ghost appeared ~1 s after field load (`test_relay_roundtrip.py` re-proves the bridge+relay path offline, without the game). An **on-screen status overlay** shows the session code + pairing state in game — "waiting for your friend… code ff9-XXXX" while unpaired, a bridge-not-running hint if the transport can't connect (`INetTransport.IsConnected`), an F6-warp nudge when co-op is on but you're in another field — and hides the moment the peer's ghost is up (★ in-game proven 2026-07-12 — including cross-version pairing: an overlay-less peer DLL paired fine, the wire protocol is unchanged). **Ghost lighting** (the last cosmetic gap, closed): registered characters get a flat per-char `_Color` tint each frame — `charClr(model) + lightClr(floor)` from the field's `mapconfig` (`fldmcf.ff9fieldMCFService` computes, `fldchar` applies) — which the ghost, living outside the char table, never received; the ghost now replicates the same math at spawn (its OWN model's char color + the LOCAL player's floor light, default light as fallback) — ★ lighting IN-GAME PROVEN 2026-07-12 (the selftest mirror matches the scene's shading). No known cosmetic gaps remain. → `project-ff9-multiplayer-injector`. |
| `s37-netsync-battle.patch` | **Battle co-op, rungs B0+B1 (EXPERIMENTAL — applies on top of s36)** — the "networked controller 2" design from `studies/battle-coop/`: the battle always runs on ONE machine; the peer spectates it live and (optionally) commands assigned party slots. **Wire v3**: every message becomes a typed frame `[magic][ver=3][type][len][payload]` on both transports — type 0 position (the old 36-byte payload, latest-slot + keepalive), type 1 battle state (latest-slot), type 2 battle commands (**FIFO, never collapsed** — the TCP/WS streams are already reliable+ordered), type 3 control; v2 peers are version-rejected (the established mixed-versions-don't-sync fail-safe; update both machines). New `NetSyncBattle` (+ csproj include): **B0** — in battle, a ~150 ms sampler serializes the roster (name markup stripped, logical-HP accessors) into type-1 frames; the peer renders a left-side OnGUI panel (enemies numbered, party HP/MP/ATB%, ready markers) and the idle overlay quiets while it shows. **B1** — `[Netsync] GuestSlots = <bitmask of party slots 0-3>` (default 0 = spectate-only) offers those slots to the peer: the local HUD skips them (guards in the ready loop, `SwitchPlayer`, the two switch-rotation paths), the peer's panel turns into a one-keystroke command menu on their turn (`[1-8]` = Attack the Nth listed enemy, `[0]` = Defend), and the arriving command frame is validated (guest slot, alive, ATB-ready, `cmd[0]` free; dead target → first live enemy) then queued through a new `BattleHUD.SendNetCommand` — modeled verbatim on the engine's own `SendAutoAttackCommand`/`SendCommand` (duplicate guard, `sel_mode=1`, `InputFinishList`, blink clear; never touches `CurrentPlayerIndex`). **ATB feel**: a `RemoteMenuOpen` control flag (staleness-guarded, ~2.5 s decay — a hung/disconnected guest can never freeze the host) is OR'd into `IsNativeEnableAtb` (vanilla WAIT) and the Turn-Based `isMenuing` — so Wait/Turn-Based hosts get their native gauge-freeze during remote turns while Active/Dynamic stay untouched; on guest stale/disconnect the slots revert to local control (the skip guards read the same liveness). **Selftest = the solo proof**: with `Role=selftest` + `GuestSlots` set, your own battle loops back through the FULL codec — the panel appears over the real fight and the guest slots are commanded by the assist keys instead of their menu (PS1 2-player battle, played solo with both hands). Assist digits are SWALLOWED from the game's key handlers while the panel awaits a command (`SwallowAssistKey`, an F6-pattern early intercept in `UIKeyTrigger.Update` — found in playtest: Alpha1 is Memoria's default help/Select binding and double-fired). ★ SOLO TIER IN-GAME PROVEN 2026-07-12 (panel live, keypress commands, WAIT-mode gauge freeze on remote turns, kill-renumber retarget, clean digits after the swallow fix). **ABILITIES + ITEMS (wire v4, same day, ★ solo-proven):** a type-4 ROSTER frame streams each guest slot's menu content (learned-ability ids + MP + live enabled state + target type, battle-usable items + counts — ids only, names resolve from the guest's own install) built by `BattleHUD.CollectNetMenus` (mirrors DisplayAbility/GetAbilityState/DisplayItem per slot); the guest panel is a digit-menu state machine (Root → paginated ability/item lists → target pick; All/Self/Random auto-resolve; ForDead abilities list the DEAD — Phoenix Down proven); the host re-resolves the command per slot (trance-aware) and revalidates LIVE at injection (`NetAbilityUsable`; closed a real unlearned-cast cheat path found in playtest — an empty-list single-shot now requires the first USABLE ability). **`[Netsync] GuestWaitMs`** (default 30s, 0=unlimited) caps an idle guest's Wait/Turn-Based gauge freeze per turn — a fallback, not a rush timer; the host adjusts. Excluded v1: Throw, double-cast/mix, monster-transform. **VISITOR MODE (wire v5, same day; ⚠ dressing+suppression solo-provable, follow-warp two-machine-only):** the stock-play vision — the guest is a presence in the HOST's game. New `NetSyncVisitor` (+ csproj include): (a) **ghost dressing** — `[Netsync] GhostAs = auto|<playable>` dresses the PEER's ghost as the party member of my first GuestSlots bit (the character they command in battle is the character they appear as); anim ids are rig-partitioned, so the pos frame gains a sender-classified **rig-neutral loco byte** (idle/walk/run/turnL/turnR/other, classified against the sender's own rig via the kit's proven home-field clip table, 8 mains; the receiver resolves it on the dressed rig — gestures fall back to idle; the wire submesh mask is skipped when dressed, it's only index-valid on the peer's own model); (b) **`FollowHost = 1`** (guest side) — auto-warp my game to the peer's broadcast field id, debounced 1.2s against door transitions, registration-guarded (`eventIDToFBGID` — an unregistered field shows "your friend is on field N, which this install doesn't have" instead of a black screen), using the exact F6 deferred-fade warp recipe; (c) **encounter suppression** — while following a connected host (or selftest), `SettingsState.IsNoEncounter` ORs in `NetSyncVisitor.SuppressEncounters` (one line; the booster property all three encounter sites already consult) so only the host's battles exist; link down → normal encounters instantly. ★ VISITOR SOLO TIER IN-GAME PROVEN 2026-07-12 (dressing, party-order re-dress, GhostAs hot-reload cycling incl. auto↔explicit, encounter pause + hot-reload, dressed-ghost lighting incl. per-frame light-zone tracking, mask parity "no dagger", and the camera clean through reorders). The proof round fixed three real bugs: dressed-ghost lighting FALLBACK (a dressed model has no entry in the host field's mapconfig → now worn-model → the LOCAL PLAYER's entry → default) and CONTINUITY (the engine re-tints registered chars per frame; the ghost now re-applies at ~7 Hz, riding the local player's floor light), and — the big one — **the HonoBehavior teardown law**: the ghost's `GeoTexAnim` is a `HonoBehavior` (self-registers at Awake), and `HonoBehaviorSystem.Update` SWEEPS its list (`IsEnabled()`) BEFORE processing queued removals — so a raw `Object.Destroy` of a HonoBehavior-carrying object MID-SCENE leaves one tick that dereferences the destroyed component → a per-frame NRE that aborts the sweep forever and freezes every later HonoBehavior (the field camera pipeline; found via a frozen camera after a party reorder re-dressed the ghost mid-field). Sanctioned teardown: `UnregisterHonoBehavior(dispose: true)` — the system removes every child behavior synchronously and destroys the GameObject itself. Field-transition despawns never hit this (the system rebuilds per scene), which is why it lurked. FollowHost's warp remains the one two-machine-only untested piece. **THE COOP CELLS (V2 `[[coop]]` authoring; ★ SOLO-PROVEN 2026-07-12 — the two-plate gate fired ONCE on step-in at field 4003, proving the broadcast, the expression compare, AND the once-latch in one report):** while Enabled, every frame writes the PEER's presence + walkmesh position into kit-reserved `gEventGlobal` bytes 2032-2039 (`WriteCoopCells`: presence byte @2032, X/Z Int16 LE @2034/2036 — byte-addressed exactly as `EBin.GetVariableValueInternal` reads, beside the kit's mask scratch at 2040) — kit-compiled `[[coop]]` two-plate gates read them as ordinary GLOB vars in region Range bodies (engine = mechanism, `.eb` = policy; the engine knows nothing about plates). Never written unless Enabled (a vanilla save is untouched); presence zeroes on unpair/out-of-scope/config-change. Selftest writes the mirror's position (+250 x), so a gate with plates 250 apart is solo-provable. **STATE-MIRROR RUNG 1 (wire v5→v6, regenerated into this patch 2026-07-15):** the authoritative-host substrate — a type-5 STATE frame carries the HOST's `gEventGlobal` (section-tagged payload `[id u8][len u16 LE][bytes]`; section 0 = the 2048-byte story heap; the coop cells 2032-2041 are masked BOTH ways so the guest keeps its own `[[coop]]` view), published ~7 Hz by the host; a FOLLOWING guest applies it at the field-load boundary (`HonoluluFieldMain.ff9InitStateFieldMap` → `NetSyncClient.ApplyStoryBeforeEvents()` immediately before `StartEvents`) so its fields stage the HOST's story instead of scenario-zero. New `NetSyncState.cs` (+ csproj include): snapshot/apply (`ApplyStoryTo` is scratch-targetable) + capture/restore + a selftest that round-trips the REAL wire codec (`BuildFrame`/`TryParseHeader` → the real section-walk apply into a scratch buffer, never the live array). **Session save-safety (two guards + an exit ramp):** while mirroring, the guest can neither manual-save (`SaveLoadUI.OnKeyConfirm` refuses on `NetSyncClient.IsMirroringStory`) nor autosave (the `EventEngine` guard below), so the guest's DISK stays pristine all session; and a session that ENDS exits through **the autoload ramp** (`NetSyncState.ExitMirrorToOwnSave`): the guest's OWN autosave is reloaded — the verbatim moogle-menu load transition; no autosave yet → title screen — NEVER an in-place flag restore, which would leave the guest's own story standing at the HOST's location (a sequence-break/softlock frankenstate the base game can't produce). Link-drop arms the ramp debounced (3s, relay-blip-proof); a config change arms it immediately; the mirroring flag stays true until the ramp fires so the save-block stays armed through the gap; the ramp fires only free-standing on a field/world map; app teardown skips it (the disk is already coherent). (Kit-side, `coop host` forces `FollowHost=0`.) Two battle-menu fixes ride the same round: `CollectNetMenus` SKIPS an Ability command with zero learned abilities (no more empty "Swd Art" → dead target prompt on the guest), and `BuildRoster` waits quietly via the new `BattleHUD.NetMenusReady` probe until `InitialBattle()`→`ManageAbility()` seeds the ability dict — the battle-intro `KeyNotFoundException`/`roster build failed` log spam (pre-existing since B1) is gone, same wire behavior. v5-and-older peers are version-rejected; NOTE the transport asymmetry: `NetSyncSocket` logs+disconnects on a version mismatch, the relay just never pairs — check both DLLs before debugging "no sync". ★ SOLO TIER PROVEN 2026-07-15 (selftest codec OK ×3 launches, Sword Art absent in-game, zero roster errors + a guest Attack through the quiet path). **★ TWO-MACHINE PROVEN same day (Phase 6): fail-safe (v5+v6 never pair), RENDER-MATCH (field 354 rotating cast re-stages from the host's ScenarioCounter — the authoritative-host headline), Twin Altar incl. the held door, battle B0+B1, follow-warp.** The session also found the last save-safety hole: **AUTOSAVE bypasses the SaveLoadUI block** — `EventEngine`'s field-load `serializer.Autosave` writes the Continue slot on every transition, so a following guest's Continue collected the host's mirrored story + the followed-to field. Fixed in this patch (`EventEngine.cs` joins the set, 16 files): `noSave \|= NetSyncVisitor.SuppressEncounters \|\| NetSyncClient.IsMirroringStory` — a FOLLOWING guest never autosaves (encounter-suppression's exact scoping: following + connected, or selftest; vanilla on link-drop; hosts and non-following guests keep vanilla autosave). Save-safety re-test = the remaining Phase 6 box (`studies/battle-coop/PLAN.md`). |
| `s38-netsync-spectator-field.patch` | **ROAD A — SPECTATOR-STRICT (applies on top of s37)** — the correctness half of the SPECTATOR-FIELD paradigm: *a following co-op guest has no interaction authority, no local state authority, and no local escape.* Closes **a live, present-tense exploit**: the guest's flags are mirror-owned and TRANSIENT (all 2048 gEventGlobal bytes overwritten from the host on every field load) while its bag is guest-owned and PERSISTENT (the mirror never touches `FF9.item`) — so a guest opening a chest the host hasn't looted banks the item in its REAL bag, the next field load reverts the "opened" flag from the host's snapshot, and the chest is closed again → **an unbounded item farm, one item per field re-entry**. Three gates, all keyed on `NetSyncClient.IsMirroringStory` (the same predicate as the save gates; its "arming gap" — false until the first mirrored field load — is CORRECT, since until then the guest's own story is still authoritative): (1) **`AchievementManager.ProcessAchievementReport`** — the guest's stray writes are all discarded by the autoload exit ramp *except* a Steam achievement report, which leaves the machine and no ramp can retract; this is the single funnel every channel (item/battle/ability/minigame) crosses before the native call. Do NOT gate `FF9Item_Achievement` instead — `ITEMADD` fires five `EMinigame` achievements that bypass it. (2) **ENCOUNT `0x2A` + ENCOUNT2 `0x8C`** (`DoEventCode`) — a mirrored script was booting a LOCAL battle the host isn't in, whose result screen writes the guest's real gil + item drops + Steal; `SuppressEncounters` never covered these (it reaches only `ff9.IsNoEncounter` = the random-step and worldmap rolls). Returns **0** (inline continue), and **the guard MUST sit below every `getv`** — `commandDefault2` branches on `gArgUsed` and REWINDS the IP one byte if no arg was consumed, re-executing the opcode forever. (3) **the menu** — all four `UIKeyTrigger` `IsMenuControlEnable` reads, incl. the Alt+F2 party / Alt+F5-F9 save-load shortcuts that bypass `OnKeyMenu` entirely. Gated at the READ, **not** via an `IsMenuControlEnable` getter: `FieldMapActorController:167` reads that flag to decide an INACTIVE actor should idle, so forcing it false drops those actors into movement processing. **Deliberately NOT blocked:** tread regions (tag 2) — the `[[coop]]` plate gates and the Twin Altar's held door live there, and a gateway only moves the guest (follow-warp overrides it, the ramp discards location); and dialogue/`B_KEYON`, which the guest needs to advance its own mirrored cutscenes. **No wire change (still v7)** — so a stale peer still pairs: **the gates are guest-side, so BOTH machines need this DLL for symmetric protection.** ★ SOLO REGRESSION + TWO-MACHINE PROVEN 2026-07-15 (menus refuse incl. both Alt lanes; Evil Forest Plant Brain + plant-spider scripted fights skipped on the guest while the host fought; no regressions in coop gates, battle B0/B1, cutscenes, or the exit ramp). **Known residue (rung 2, not shipped):** a real chest on a *verbatim fork* is proximity-dispatched with its confirm read INSIDE the script (`B_KEYON`), so it still opens — but post-A the item lands in a bag nothing reads and the ramp discards, i.e. theater, not corruption. Closing it needs a pad mask (risks wedging any script that polls a button) → `studies/battle-coop/inventory-authority.md`. |
| `s40-netsync-diorama.patch` | **THE BATTLE DIORAMA (B3.0-B3.5, wire v9) — applies on top of s38** — the authoritative-host battle headline: a FOLLOWING guest's game boots the HOST's battle scene and PLAYS it — the host's party seated (scratch-PLAYER dict swap), enemies from the same (scene, pattern), live HP/death/revive/trance/visibility reconciled from the type-1 lane every ~150 ms, and the host's ACTIONS (exact damage/heal/miss/crit figures + flinches + caster swings) from the new **type-6 FIFO action lane** fed by a CalcResult emit (whole-body try + finally backstop; the seq-HORIZON consumes the sampler's `_seq` so stale type-1 samples skip gated units by construction). NEW `Memoria/Netsync/NetSyncDiorama.cs` (the containment predicate `Active` = armed+booted+`loadedLevelName`; the gil/battle_no/kill-counter/battle-field bracket; the reference party bracket; boot/leave/teardown; the reconciler; the action handler; the state/figure/wire benches) + the engine containment gates all keyed on `NetSyncDiorama.Active`: `btl_sys.SavePlayerData`+`CheckBattlePhase`+`CheckForecastMenuOff` (definitions), the `btl_stat` OnOpr dispatch + `UpdateAbnormalStatus` achievement write, `BattleActionCode`'s gEventGlobal write, `btl_scrp` op-37 SetNextMap, `battle.cs` no-enemy-left auto-end, `SettingsState.SetTranceFull`, the `BattleHUD.Unity` L+R escape combo, `btl_para`'s Overload checkpoint dispatch, `BattleUI.OnGUI` — plus the `EventEngine` noSave + `SaveLoadUI` blocks on `Booted` (a selftest guest has no other save guard). Wire v7→v9 along the way: v8 = the type-1 DIORAMA BOOT BLOCK (patNum/startType/flags/nonce; mapNo==battleMapIndex is one id space) + the guest watcher (`[Netsync] Diorama`, default 1); v9 = the action lane + the type-1 trance-gauge byte + the section-1 party v9 block (basis/status/trance/SA — closes the default-SA and 9999-clamp seat gaps; equip ContainsKey→NoItem guard; per-slot containment; NEVER Validate* in a seat). Also carries THE TICK-BASELINE LAW sweep (TickCount wraps negative at 24.86 d uptime — 8 timer baselines re-founded; the entire first two-machine failure) and the mirror/watcher telemetry (every gate that can decline says so). ★ B3.0-B3.5 ALL SOLO PROVEN 2026-07-15/16; B3.3 two-machine proven ("host battles pull the guest in"); B3.4/B3.5 two-machine boxes ride the 20260716 laptop package README. The deployed DLL `6F999C4575266592` == base+s22..s40 (minus the FieldMapActorController exclusion below; built pre-EOL-normalization, content-identical). → `studies/battle-coop/diorama-lane.md`, `b34-recon.md`, `b35-recon.md`. |
| `s41-netsync-diorama-polish.patch` | **B3.6 — THE POLISH ROUND (wire v10) — applies on top of s40** (6 files, no new sources). The diorama boots THROUGH the real battle swirl playing the HOST's music: the type-1 boot block gains `songId` u16 (host samples the COMPUTED id at both `BattleSwirl` branch heads so BattlePatch `Music:` + s33 remaps ride along; sentinels 0xFFFF unknown → guest-local fallback, 0xFFFE host-silent → guest silent; `[Netsync] DioramaSwirl` default 1, hot-reloadable, 0 = the plain-fade lever) + the pre-swirl field-audio replicate + a SwirlScene watchdog and stranded-flags fences. Poison/Regen **TICK figures** ride the type-6 lane (marker bit7; guest pops via `Btl2dStatReq`, host figure verbatim — closes B3.5's most visible accepted gap; nonce = payload byte 0). The B0 spectate panel hides while the diorama is live (assist menu always; the panel survives every no-diorama state). The F6 **opt-out replay bug** fixed both halves — the staleness flicker no longer clears the skip, and **THE LEAVE-INTENT LAW**: who initiated a teardown is recorded AT THE INITIATOR (`NetSyncDiorama._leaveTick`, read as `LeaveRequestedRecently` <5 s — a tick with decay, never a wedgeable Boolean; user/watchdog leaves SETTLE = stamp the skip, watcher/dropped-boot RETRY). Guest-side `ValidateWireBoot` PatNum guard (sync scene pre-read BEHIND the 1/s rate limit; durable refuse = skip-nonce + one line). `[Netsync] SelfTestOffset = dx,dz` (default 250,0; quoted form legal — `GetSetting` returns quotes verbatim and FolderNames trains quoting; the first real edit arrived quoted and silently kept the default). Build shape: recon 6/6 CORRECTED → delegated build → 7-skeptic verify → repair → all NINE leave paths re-walked clean. ★ SOLO PROVEN 2026-07-16 (swirl+leave, bench `song 156`, random-encounter `song 0` = the generic-Battle-Theme law, opt-out functional, vanilla battle, SelfTestOffset mirror-move; poison ticks piggyback later); two-machine boxes = the 20260716b laptop package. Deployed DLL `0537BC580F0D68C7` == base+s22..s41 (same exclusion). → `studies/battle-coop/b36-round.md`, `diorama-lane.md`. |
| `s42-netsync-config-split.patch` | **The session-hardening round (built 2026-07-19, ★★ two-machine PROVEN 2026-07-20; wire UNCHANGED at v10; one file: `NetSyncClient.cs`).** Fixes the two verified root causes of the 07-19 two-machine round (11-agent source review, `wf_3c741130`): **(1) THE CONFIG-SIGNATURE SPLIT** — the old single config signature made ANY `[Netsync]` edit fire the full teardown (transport drop + `SuppressEncounters` forced off for the reconnect window + a mirroring guest exit-ramped to their own autosave). Now `_cfgTransportSig` (enabled/role/peer/port/relay/code/tls → full `ApplyConfigChange`) vs `_cfgBehaviorSig` (targetField/guestSlots/guestWait/ghostAs/followHost/selfTestOffset/dioramaSwirl → new `ApplyBehaviorChange`: session KEPT; arms the exit ramp only on FollowHost-off-while-mirroring); `LoadConfigIfChanged` returns 0/1/2. **(2) THE SAME-FIELD KICK** — a guest co-located with the host at pair time never crossed a field-load boundary so the mirror never armed (next host battle = a diorama seated with the guest's OWN party); `SameFieldKickTick` fires ONE forced fade+reload of the current field through the proven deferred-fade warp, gated on role=client + !mirroring + !rampPending + FieldHUD + a host snapshot on the wire, re-armed via `RearmSameFieldKick` when a load skipped its mirror-arm — CAPPED at 3/session with one give-up log. **(3) the `[Netsync] Diorama` knob DELETED** (3/3 judge verdict — its only realized effect was this defect class; stray ini lines ignored). Verify-round fixes: an in-flight follow-warp ALWAYS completes (`ServiceFollowWarp` moved ABOVE the `!_enabled` gate; `ApplyConfigChange` no longer clears `_followWarpPending` — the old clear stranded a black screen if an edit landed inside the ~0.9s fade) + `DioramaTick` declines to boot while a warp is pending. Also carries the s43-round overlay string ("meet your friend: ~ menu > Warp"), captured together as planned. ★★ PROVEN: same-field kick / live play-style edit (session kept) / transport edit reconnects — all on the real link. Gate 2026-07-20: single-file lineage replay (s36+s37+s40+s41 `NetSyncClient.cs` hunks) + s42 == live bytes; GNU `patch -R -p1 --dry-run -F0` clean on live; forward dry-run clean on the pre-tree. Kit side (--diorama flag delete, Workspace checkbox) landed at `521661a`. |
| `s44-netsync-field-coop.patch` | **F1 FIELD CO-OP — the spectator-field round (built + ★ SOLO BENCH 6/6 PROVEN 2026-07-20; wire UNCHANGED v10; 9 files, one new: `NetSyncField.cs`).** The spectator-field paradigm's field half (`studies/field-coop/DESIGN.md` + `F1-BUILD.md` = the full decision record): a FOLLOWING guest never starts interactions, never self-navigates, and can always recover to the host. **The predicate**: `NetSyncField.SpectatorField` = `IsLiveFollowedSession \|\| IsMirroringStory \|\| (IsSelfTestRole && BenchGates)` (closes the pre-mirror AND post-link-death-pre-ramp windows; `BenchGates` = the ~-menu Field-gate bench lever, the selftest arm — no `[Netsync]` knob). **The gates**: `EventCollision.CheckQuadInput`/`CheckNPCInput` at the would-start points (chests, NPC talk, card starts) · the `MENU` opcode (both `getv1()` consumed first) · **THE GATEWAY REDIRECT** — MAPJUMP keeps the vanilla instruction flow (`return 0` was refuted by an 817-field census: 4,400+ live-code post-`Field()` followers incl. two `while(1)`) and rewrites dest to a same-field reload via `RedirectGuestFieldJump` (follow allowance: dest == `LastHostField` passes; a 3-in-20s BOUNCE BUDGET lets auto-transition fields through; the opcode's return-4 gets `nextMode=1`/`attr\|=8` from HonoluluFieldMain case 4 — transition-mode-identical to the s42 kick) · **THE KEYON MASK** — `FilterScriptButtons` strips Confirm\|Special from `B_KEYON`/`B_KEY` script reads ONLY while `SpectatorField && GetUserControl()` (stock loot/chest tread-bodies poll control-ON and mask; the census's 271 blocking Confirm-waits are cutscene beats that run control-OFF and pass — built when the first playtest surfaced a lootable field-206 Phoenix Down). **Teleport-to-host**: F11 tap / held-L3 (session-gated intercept — vanilla never reads the keys) + AUTO recovery (floor 640/hardCap 1200/dwell 4.5s/cooldown 10s seeds; the ratified "gap keeps GROWING" steering override with a persistent arm baseline; snap via `fieldmap.playerController` SetPosition nearest-Y, honest off-mesh verdict). **The snapshot-taint fix** (~ menu): `_snapshotMirrored` = mirroring \|\| `OwnSaveReloadInFlight` (the ramp's async window, cleared in the autoload CALLBACK) \|\| selftest-bench; tainted restores post-session are discarded with a visible reason. Build shape: 12-agent recon-verify → implement → 7-skeptic adversarial verify → repairs → focused re-verify (`wf_86eb27dc`/`wf_e66632e8`/`wf_7c682de1`). Gates: forward `git apply` onto the pre-round snapshots (`backups/preF1-snapshots.20260720`) == live bytes ×9; reverse `-F0` clean on live; forward GNU dry-run clean on the pre-tree. Deployed DLL `7F3E17BAF6EC0401`. Two-machine boxes pending (real-guest reachability, redirect on the live link, auto-TP with a real host, snapshot leak end-to-end). |
| `s54-netsync-diorama-silent.patch` | **THE HOST-SILENT DIORAMA (captured 2026-07-23; wire UNCHANGED at v10; 4 files; ★ two-machine proven 2026-07-23).** Stops a following guest’s own field/world BGM bleeding through a diorama battle when the HOST is audibly silent. Pre-s54 the `0xFFFE` host-silent sentinel folded to `songid = -1`, which hit `BattleSwirl`’s `songid == -1` early return with `IsPlayFieldBGMInCurrentBattle` left TRUE (the guest’s own music kept playing), and `NetSyncDiorama`’s pre-swirl seam SUSPENDED the field BGM (a suspend-slot the guest’s own field reload re-stacked, doubling the music across boot→leave→re-boot). Now: `BattleSwirl`’s two Request handlers (field + world) render TRUE silence on `0xFFFE` (`IsPlayFieldBGMInCurrentBattle=false` + `FF9SOUND_SONG_STOPCURRENT`, the `HonoluluFieldMain.cs:151` primitive); the `NetSyncDiorama` boot pre-swirl STOPS the field BGM instead of suspending (no slot to re-stack); the real-song lane keeps SUSPEND (two-machine proven — its field-reload restore is the intended resume). `NetSyncBattle.WireBenchBoot` gains `Boolean silent = false` (the silent bench ships `0xFFFE` instead of the 156 ending-theme bench via a derived `BenchSong`; the default keeps the 156 bench byte-for-byte) + `Ff9mkDebugMenu`’s "Bench (silent)" button to exercise the host-silent BGM lane SOLO. |
| `s55-netsync-unpair-guards.patch` | **THE UNPAIR / RECONNECT GUARDS (captured 2026-07-23; wire UNCHANGED at v10; 1 file: `NetSyncClient.cs`; ★ partially two-machine proven 2026-07-23).** Four independent robustness fixes for a following guest across a transport blip or battle-return: **(1)** the `[Netsync] AutoTeleport` knob (default OFF, pending a leash rework) gates the live-coop AUTO teleport-to-host arm — the selftest bench arm stays INDEPENDENT of the knob, and the manual F11/L3 teleport is a separate path, unaffected. **(2)** ENCOUNTER-SUPPRESSION HYSTERESIS — a ~1-3s relay blip no longer instantly re-enables the guest’s random encounters (which booted a LOCAL battle on the guest’s own party and crashed the battle-return seam): hold the pause while connected OR within a 5s `EncounterGraceMs` grace of the last connected frame, FOLLOWING-only, fail-safe after the grace (tick-baseline law, pre-expired). **(3)** `SnapToHostAuto` — the AUTO recovery captures the on-mesh position and RESTORES it when the host’s spot is off this walkmesh (the engine’s default-spawn dump can sit on a gateway quad and reload-cycle, and the player never asked to move); the human-initiated manual refloor still lands at the default spawn. **(4)** the exit-ramp SETTLE DEFERRAL — don’t fire `ExitMirrorToOwnSave`’s Autoload on the FIRST raw frame of a battle-return field load (it raced the return’s own load, the guest-crash seam): wait for `FieldHUD` control-return, bounded at 10s (`ExitRampSettleMaxMs`), the world map (gMode 3) exempt. |
| `s56-netsync-blip-grace.patch` | **THE BLIP-GRACE ROUND (captured 2026-07-23; wire UNCHANGED at v10; 4 files; ★ holding on the real link 2026-07-23).** Smooths a following guest through a relay transport blip (`IsConnected` false for the ~2.5-3s reconnect cycle) so a momentary drop doesn’t visibly tear down the session. `NetSyncClient.cs`: **(a)** GHOST BLIP-GRACE (`_ghostLastPairedTick` / `GhostGraceMs = 5000`, link-DOWN-gated) holds the peer’s ghost planted at its last pose through a transport drop instead of vanishing-and-popping-back — a deliberate leave (field-0 sentinel with the link UP) still despawns instantly; **(b)** the DIORAMA BLIP-HOLD (`_dioramaStaleSince` / `DioramaBlipHoldMs = 8000` + the `_dioramaWatcherLeftFinal` skip-nonce stamp) freezes a booted diorama while its lane is stale AND the transport is down, rather than the watcher’s shorter 2s horizon leaving and the same still-live nonce re-booting on reconnect ("battles start twice"), and stamps the skip on a REAL leave so the same fight can’t re-boot. Both transports gain `_breakReason` instrumentation (`NetSyncRelay.cs` + `NetSyncSocket.cs`, `ReadFull` static→instance) so the read loop stashes WHY a link ended — caught exception / orderly EOF / WS close code+reason / wire-version mismatch — logged ONCE at the "disconnected; retrying" line, never per read-iteration. And `Memoria.Prime/Log.cs:125` — the log-timestamp `hh`→`HH` (24-hour) fix: `hh` logged PM as its 12-hour hour with no AM/PM, so every afternoon line read 12h off. |
| `s57-netsync-transition-intent.patch` | **THE TRANSITION-INTENT LANE + DIALOGUE L1 (captured 2026-07-23; wire v10→**v11**; 5 files; ★ two-machine proven 2026-07-23).** Two new HOST→GUEST sections on the existing type-5 story lane make a following guest track the host's field changes and dialogue scenes without chasing the position lane. **(1) THE TRANSITION INTENT** (section 4, `[destField u16 LE][nonce u8]`): the host announces its field change at the MAPJUMP commit — the `return 4` funnel is the only decidably field→field point (WMAPJUMP shares `SetNextMap` but is world-map exit, excluded) — via `EmitFieldIntentIfHost`, so a FOLLOWING guest warps IN PARALLEL instead of debounce-chasing the position lane; the serial `FollowHostTick` stays the guaranteed floor if the intent is lost. **(2) DIALOGUE L1 CO-LOCATION** (section 5, `[flag u8][nonce u8]`): flag 1 = the host is in a blocking dialogue event → a co-located guest snaps to the host once (`SnapToHost`) and HOLDS control so both watch the scene from the same start point; releases on flag-clear / leaving the field / a follow-warp takeover / link drop. Wire **v10→v11** (`NetSyncSocket.cs` + the wire-format doc): the sections ride the type-5 lane and the section-walk skips unknown ids, so a new section is not a wire break by itself — the Version bump only keeps mixed engine builds from half-syncing (v10-and-older peers version-rejected). `NetSyncState.cs`: the `SectionIntent=4`/`SectionEvent=5` consts + pure `AppendSection`/`TryReadSection` helpers (allocation-only, bounds-checked, never throw — the SAME encoder/decoder the state builder and the offline `SelfTest` share; the SelfTest now round-trips both sections, proves an unknown id declines, and re-parses the story past them). `NetSyncClient.cs` (the bulk): the F2 statics; `StartFollowWarp` extracted verbatim from `FollowHostTick` so the serial follow AND the parallel intent fast-path share ONE fade+warp body (stamps `_followWarpedTo` → the serial same-dest guard suppresses a redundant warp); `EmitFieldIntentIfHost` (host+socket only, ENDING excluded, an out-of-band frame push so the intent leaves at the transition instant, not the next 7 Hz tick); `BuildStateFrame` (the ONE host frame builder for both the ~7 Hz publish and the intent emit — a null story ships nothing, as the bare `SnapshotAll` publish did); `UpdateHostEventFlag` (`!GetUserControl()` corroborated with a live `UIManager.Dialogs.Visible` so a gateway MOVQ can't false-trip, edge-logged only); `ServiceIntentFollow` (guest fast-path ahead of the serial debounce, nonce-dedup, DEFER-not-mark mid-menu/diorama, DECLINE-and-mark if the field isn't on this install); `ServiceEventCoLocate` + `ReleaseL1` (incl. the post-review pin-flag coupling fix — `_l1Pinned` re-coupled in the hold block + the `|| _l1WeForcedControl` release gate, so a mid-pin blip can't strand control OFF); `DebugInjectIntent`/`DebugInjectIntentImpl` + `DebugHostEventFlag`; the `Update` wiring (publish → `BuildStateFrame`, the two `Service*` calls, the host intent-clear + event-flag block, the selftest L1 bench); `ResetIntentAndL1State` on `ApplyConfigChange`/`OnDestroy`. `EventEngine.DoEventCode.cs`: the one `EmitFieldIntentIfHost` line at the MAPJUMP commit, after the F1 `RedirectGuestFieldJump`. `Ff9mkDebugMenu.cs`: the debug FIELD-warp (`ServicePendingWarp`) intent emit (the overworld `WorldWarp` mode-3 deliberately NOT hooked — world-map is frontier) + the Transition-intent + Dialogue-L1 selftest bench sections in the field-gate bench. Vanilla + a non-following guest untouched; identity-safe. Gate: 18 hunks; forward reproduces all 5 live files sha256-exact under BOTH `git apply --binary` AND `patch -p1 -F0 -s -f --binary` onto the per-file lineage baselines (Socket `s36/s37/s40/s41/s56` · State `s37/s44` · Client `s36/s37/s40/s41/s42/s44/s55/s56` · DoEventCode `s30/s38/s44` · DebugMenu `s22/s40/s43/s44/s48/s54`; base 6b8bb2d5's LF blob CRLF-matched to the autocrlf=true tree, and the LF-stored `s48` CRLF-normalized on replay); reverse `patch -p1 -R --dry-run -F3` clean on live (stack-top for all 5 files). |
| `s84-netsync-dialogue-lockstep.patch` | **F3 — DIALOGUE L2: full confirm/choice LOCKSTEP of a co-located guest's copy of the host's blocking scene (built + deployed 2026-07-23 night, wire v11→**v12**; ★ SOLO BENCHES PROVEN 2026-09-04, 40/40 unattended via the harness's `netsync` verbs — the first time they ever ran; CAPTURED 2026-09-04: 28 hunks, 6 files). ⚠ STACK POSITION: right after `s57`, before `s58` — numbering is capture order, and `s83`'s DebugMenu/UIKeyTrigger hunks were captured from a tree that already carried F3 (`tools/memoria_stack_replay.py` pins it).** HOST: `Dialog.OnKeyConfirm`'s two confirm-driven Hide sites tap `EmitDialogAdvanceIfHost` — one 8-byte TypeDialog frame `[fldMapNo u16][winnum u8][textId u16][kind u8][choiceIndex u8][seq u8]` per confirm-that-advances (`IsClosedByScript`-filtered; a `[NFOC]`/`[TIME]` window never reaches the Hide branch, so no frame can target one). WIRE: type 7 on its OWN FIFO queue (the type-6 precedent; drop-oldest at 32, once-per-link log), `SendDialog`/`NextRemoteDialog` on both transports, Version 11→12. GUEST: `ServiceDialogLockstep` engages ONLY under L1 (host event flag + co-located + lane fresh + no follow-warp mid-fade); `ServiceDialogApply` is a peek-until-match FIFO over a one-slot holder keyed `(field, winnum, textId)` — unique-while-live but NOT an instance id (14.2% duplicate reopens), so frames apply strictly in host order against the CURRENTLY-open window (`ResolveOpenDialog`, winnum-scoped: ~19% of fields hold two live windows); match@printing = fast-forward + HOLD, re-apply next tick (one clean advance, never two page-skips); match@Complete = `TrySetCurrentChoice` (NEW, bounds-checked — the unguarded `SetCurrentChoice` is unreachable with a bad index; out of range → logged once, local default) then the window's own `OnKeyConfirm`; per-key seq dedup. THE SOFTLOCK-ESCAPE INVARIANT: suppress is set only on first-match (S3 — a HELD-unmatched frame never suppresses) and released on every non-engaged tick, window close, the 8000 ms `DialogWaitMs` hold-timeout (discards the frame, logs once) and session reset. `UIKeyTrigger`: ONE static read (`SuppressLocalDialogInput`) on the dialogue-input path swallows the guest's own Confirm/Cancel while engaged. `Ff9mkDebugMenu`: the L2 selftest bench (inject advance / inject choice / the B5 unmatched frame) + `FrontmostOpenDialog`. No MAP mirror (the census: MAP state is DERIVED); same-language sessions documented, not special-cased. Vanilla provably unchanged: the taps role-gate before touching the dialog and never throw. Gate: the full live stack (dead s12/s18/s21/s59 skipped) replays base→s57→s84→s58…s83 at ZERO fuzz under `git apply --binary` and reproduces all 18 netsync/harness/dialog files byte-exactly; `s84` lands on the s57 snapshot under both `git apply --check` and `patch -p1 -F0 --dry-run`; reverse cascade live −s83 −s84 clean. Solo proof + the two bench laws → `studies/field-coop/F1-BUILD.md` (F3 SOLO BENCH); the 8 two-machine boxes still OPEN. |

## The Folklore codex (player-facing feature) — NEW capability

| File | What it adds |
|---|---|
| `s46-folklore-render-rig.patch` | **THE RENDER RIG, rung 1 — ★★ IN-GAME PROVEN + CLOSED 2026-07-21 (3 same-day playtest rounds: mechanism 4/4 incl. the field+battle leak snaps + the world-map open → transparency/facing → the Equip-sentence split, "looks good"; 1 file: `FolkloreUI.cs` only — deliberately NO csproj change, so no adjacency risk). **RUNGS 2+3 ★ IN-GAME PROVEN + CLOSED same day over 3 playtest rounds** (round 1's "red circle then blank" = `GEO_MON_B3_187` IS a ring-shaped battle PROP — the model-preview/offline-eye law; round 2 "looks good" proved wire+swap+idle; round 3 = **the LIVE-SILHOUETTE FIT**, "good" — bind-pose-only framing clipped tall idles, replaced by a live renderer-bounds envelope: `updateWhenOffscreen=true` (the ghost-hack's deliberate opposite), first live sample REPLACES the static seed (the 2-skeptic round's headline: an expand-only envelope seeded from the bind pose can never zoom IN), candidates NaN/Inf/MaxExtent-validated pre-commit (poison-proof), the settle window in `Time.deltaTime` seconds (VSync makes frame-counts refresh-rate-dependent), grow epsilon `max(2, 1% of envelope)`; deployed DLL **`3000418F860C843B`** both arches). **+ THE BODY-BLEND ZOOM (★ IN-GAME PROVEN — "the gale sprite is way bigger now"):** the flavor-pass Dragon Fly rendered as a speck — the live fit frames the MOTION VOLUME and a flyer's hover-bob makes that ~3.4× its body (settle-log-measured); now `folkloreFitBodyMax` (per-frame instantaneous half-extents, componentwise max) blends `body + (envelope − body) × 0.4` aimed at the flight center — ~1.7× zoom on flyers, grounded creatures unchanged (envelope ≈ body); the skeptic's catch: body growth must OR into the refit gate (envelope growth stabilizes after one cycle — a late wing-spread would crop while the log lied); static path bit-identical through `ApplyFolkloreFitCore`. **+ THE TURNTABLE YAW (rung-5 garnish, DEPLOYED same day, DLL `BA5DE4A838DE4AB9` both arches; ★ IN-GAME PROVEN — "good"):** 25°/s vertical-axis rotation (`AngleAxis(yaw,up)·AngleAxis(20,right)` — explicit product, same matrix as the Euler form) + **THE ROTATION-INVARIANT SWEEP FIT** (the settle window observes only a partial arc before freezing, so the fit computes the swept cylinder analytically — aim recentered on the axis, horizontal half-extent = swept radius; the skeptic's HIGH: sweep the RAW envelope and blend RADII, never the pre-blended box — only the envelope union carries the every-yaw safety proof); levers `FolkloreRigTurntable`/`FolkloreTurntableDegPerSec`; honest diagonal trade (rotating creatures render slightly smaller). **+ THE idleClip TOKEN (DEPLOYED same day, DLL `E6556ACEB3D2463C` both arches; ★ IN-GAME PROVEN — the toad's suffix-form `idle:020` attack-lunge loop, then reverted to defaults):** the registry's 4th token `idle:<clipName-or-suffix>` overrides which clip plays as the idle (the 187-class lesson) — engine resolves against the model's DISCOVERED list only (exact-first, unique-suffix, ambiguous warns naming candidates; the FAIL-SOFT law: bad idle = default clip, never text-only), kit `idle = "..."` on `[[folklore]]` (lint ERROR without display, build warn-and-drop-idle-only, canonical full-name emission so engine exact-match always wins; 86 folklore tests, suite 3467/0); both workflow lanes skeptic-clean first pass.** → `studies/folklore-codex/RENDER-RIG.md` §RUNGS 2+3.** **Round 2 (playtest-driven, recaptured into this patch):** yaw 180→**0** (the guess showed the creature's back; 0 = the viewer's own default), **transparent clear** `(0,0,0,0)` replacing the opaque black box (the rung-5 probe pulled forward — field-look Cutout writes clean 0/1 alpha; retreat lever = `Color.black`), and portrait depth `textDepth−1` (=5): the census log MEASURED the pane (Shadow 3 / Body sheet 4 / body label 6 / Border 8 / Caption 9) and round 1's `textDepth−2` "safety margin" was an EXACT TIE with the Body sheet — the skeptic's feared instability, created by the margin itself. Ledger: awake 4ms · mint 7ms · frames 21/3/15. **Round 3 (same day, user-directed — "split the right panel like Equip"):** THE EQUIP-SENTENCE SPLIT — the single 780×880 detail pane becomes TWO stacked bordered windows via the file's own `BuildFramedPane` (both stock GenericInfoPanel skin, column footprint byte-preserved): TOP 780×500 portrait window (caption = entry name) over a 20-unit gap over BOTTOM 780×360 lore window (caption blank); portrait re-seated 680×400 == the resized RT (1:1); framing knobs `FolkloreRigCamDist −700` / `FolkloreRigAimY −80` (the round-2 snap: low-pivot model high in frame, legs clipping the narrow aspect); depth now scoped to the TOP pane's own Body sheet (+1) per the round-3 skeptic (the min-of-both-labels form coupled the panes). Recorded, not fixed: the column's right edge 800 vs the ±771 pillarbox comment (inherited from 16 approved rounds — a someday 4:3 check). Look-check pending. The s46 display window's core mechanism (`studies/folklore-codex/RENDER-RIG.md` = the build plan): when the detail pane shows an OWNED entry, a lazily-minted offstage rig renders ONE hardcoded creature (`GEO_MON_B3_118`, the Model Viewer's own test class) into the pane — stage GameObject at local (0,−10000,−10000) under the scene's own transform, layer 10 stamped recursively, an ALWAYS-DISABLED camera (`cullingMask 1<<10`, `depth −4096` parking, opaque-black SolidColor) driven by ONE manual `Render()` per entry-show into a persistent `RenderTexture(640,720,24)`, displayed through a runtime `NGUITools.AddWidget<UITexture>` seated with the pane's own SetAnchor(null)+pivot+SetRawRect idiom at depth `min(labelDepths)−2` (behind the lore text, above the sheet). No animation, no per-frame drive, no registry parse (`Entry.Display` stays unread until rung 3); locked entries render nothing (the spoiler convention). Fail-safe ladder: try/catch mint+render → `Log.Error` + text-only pane; a failed rig never retries within the visit. Built via a 9-agent ground→design→3-skeptic-verify→repair workflow; the verify round caught 2 BLOCKERs pre-compile (teardown deactivated-but-never-destroyed the portrait GameObject = one leaked widget per codex visit; mid-mint exceptions left every `this.folklore*` field null so the catch's teardown was a no-op = a permanent half-built-rig leak under the DontDestroyOnLoad tree) + the lazy-mint promise being defeated by `Show()`'s own synchronous `RefreshDetail()` (fixed: a one-frame coroutine defer for exactly the first mint) + `UIManager.OnLevelWasLoaded`'s direct `SetActive(false)` sweep bypassing `Hide()` (fixed: an `OnDisable()` teardown backstop) + the HonoBehavior-dispose branch destroying the wrong GameObject (fixed: dispose is an extra step, the ancestor `Destroy(stage)` is unconditional). Teardown destroys portrait GameObject + RT + stage whole, idempotent + exception-safe, called from Hide AND OnDisable. Grounding drift worth keeping: the viewer's pose is `Euler(20,0,0)` on the model + yaw on a SEPARATE drag-driven wrapper — the rig's single composed `Euler(20,180,0)` is a declared simplification, yaw not load-bearing; `SetActive(false)`-before-Destroy has NO in-tree precedent (a new discipline, labeled as such in-code). Gates: reverse `patch -R -p1 -F0` clean on live (TEXT mode); forward `-F0 --binary` onto the pre-round snapshot (`backups/preS46-snapshots.20260721`) == live bytes, zero fuzz; zero `\ No newline` markers so the PRE-MARKER CR RULE is trivially green. Rung-1 playtest checklist = RENDER-RIG.md §2 (creature framed right-side-up · clean open/close · the field+battle leak snaps · world-map open). **Rungs 2+3 (same day, one round, 12-agent Sonnet workflow `wf_746aa256`, 3-skeptic verified):** THE LIVING IDLE — viewer-recipe clip discovery re-implemented against the bare GEO name (the viewer's own is private), `AnimationCullingType.AlwaysAnimate` + `updateWhenOffscreen` (the tree has ZERO cullingType precedent; renderer culling would freeze an offstage never-seen model), the render pump in **LateUpdate** (legacy Animation samples between Update and LateUpdate — Update would render last frame's pose), the re-Play loop GATED on a non-empty clip list (the viewer's own un-gated block would spam `Play(\"\")` warnings); THE REGISTRY WIRE — `ResolveFolkloreDisplay` (`model:` scheme or colon-less shorthand; digits → `FF9BattleDB.GEO.TryGetValue` only, the indexer throws; `GetGEOID != -1`; `GEO_SUB_W0` refused), the mint split per-VISIT rig vs per-ENTRY `EnsureFolkloreModel`/`DestroyFolkloreModel` (dispose loop + unconditional Destroy, finally-cleared fields), per-entry failure isolation (a bad token degrades ONE entry text-only, the rig survives; `folkloreRigFailed` stays infrastructure-only), and AUTO-FRAMING from 8-corner bind-pose `sharedMesh.bounds` in stage space skipping DISABLED renderers (isBattle:false disables battle_model renderer components, not GameObjects), margin 1.2, degenerate → the round-3 proven pose re-roled as fallback; retreat lever `FolkloreRigLiveIdle=false` = rung-1 static behavior. Skeptics folded 1 BLOCKER (`ResolveFolkloreDisplay` un-guarded while `GetGEOID`→`Path.GetFileNameWithoutExtension` throws on `"`/`<`/`>`/`\|` in a hand-edited token) + 2 HIGH (the fade-window race — `UIScene.Hide` deactivates only after the fade, a pending defer coroutine could resurrect the rig post-teardown → the `folkloreClosing` flag; the lever not gating auto-framing). Gates re-run: reverse `-F0` TEXT clean on live; forward `-F0 --binary` onto preS46 == live bytes; 9 hunks. Kit rung 4 (display grammar) shipped the same session — 63 folklore tests, suite 3444/0. Playtest wiring: live FolklorePatch.txt = 80 small/83 proven-medium/84 large + 81 token-less + 82 garbage. |
| `s45-folklore-submenu.patch` | **THE FOLKLORE CODEX SUBMENU, Phase A (built + ★ IN-GAME GATE PASSED 2026-07-20; 3 files + 1 new: `Global/FolkloreUI.cs`).** The first-class main-menu entry for the Folklore codex (`studies/folklore-codex/SUBMENU.md` = the build-ready design; the data spine is the kit's DLL-free `[[folklore]]` P0). **Gated on `Memoria.ini [Folklore] Enabled` (default 0 = fail-safe vanilla — no row, no scene, no construction; the parity review verified bit-identical behavior when off, incl. IEEE-float equality of the generalized tween expressions).** What it does: `UIState.Folklore` APPENDED at ordinal 28 (`state`/`prevState` are serialized — a mid-enum insert would shift baked ordinals) + `IsUIStateMenu` widened; a `[NonSerialized] FolkloreScene` field (the WMWorld baked-layout law) constructed at RUNTIME in `UIManager.Start` (inactive GameObject on the UI layer + UIPanel depth 5 + AddComponent — a C# patch cannot add a serialized scene ref) + the `GetSceneFromState` case + the hand-maintained `OnLevelWasLoaded` sweep line; the **Folklore ROW** in `MainMenuUI.Awake` via the shipping Party-row runtime-clone precedent (Card → [Party] → Folklore → Config; nav splice symmetric in both Party configs; the generalized `SetRawRect` layout loop + `shiftFactor = 7/(7+extras)` + Config's `588+98·extras` shift); `FolkloreUI : UIScene` = the Phase-A SHELL (cloned `ScreenFadeGameObject` HonoFading; cloned `GenericInfoPanel` frame — child 2 IS the frame, the root is a bare container; the ItemUI return idiom incl. the **`StartSubmenuTweenIn()` in the Hide override** — omitting it leaves the row column collapsed for the session, the review's HIGH catch). Labels resolve via `Localization.GetWithDefault("Folklore"/"FolkloreHelp")` — ship rows in the mod folder's `StreamingAssets/Data/Text/LocalizationPatch.txt`. Also adds the csproj `AfterBuild` **`DWIXNoDeploy` condition** (compile-check without auto-deploying — pass `/p:DWIXNoDeploy=true`). The 4-lens adversarial review caught 4 real defects pre-deploy (the tween-in omission, the layer-0 culled scene root, the frame-stripping child loop, a help-toggle clobber) — all fixed; vanilla-parity lens fully clean. **Phase B (built over 16 playtest rounds, functional ★ 2026-07-21; +`ItemUI.cs`):** the real codex screen — LEFT = the KEY ITEMS LIST donor (`ItemScene.KeyItemListPanel` clone, name-only window-bar rows, forced 1-column via the engine's own `GOSubPanel.ChangeDims`; the donor's authentic `CaptionBackground` dressing (sheet + capped rails) resurrected with the CATEGORY name on the header rail; owned = white name + the stock `New!` bang (cleared by Confirm via `FF9Item_UseImportant`, save-persistent), locked = grey `???`) + RIGHT = a bordered detail window (frame caption = entry name, lore body ShrinkContent) + the stock `item_bg` backdrop + L1/R1 category paging + `FolkloreRegistry` (mod-root `FolklorePatch.txt`, `<keyItemId> <category> [displayRef]`, folders low→high override per id) + the ItemUI Key-Items filter keyed on the REGISTERED set. The 16 rounds minted the NGUI-menu law set (→ `~/.claude` memory `project-ff9-ngui-menu-construction` + `studies/folklore-codex/SUBMENU.md` §Phase B): clones must live in the DontDestroyOnLoad tree (an orphan itemPrefab dies on field load and RefreshPool wipes-then-throws forever); anchors re-assert EVERY frame (SetAnchor(null) or re-target every repositioned clone; the GenericInfoPanel FRAME child anchors to MAIN-MENU transforms — re-anchor it onto your pane root, the ControlPanel.InstantiatePanel recipe); window color = an imperative per-row `DisplayWindowBackground` sweep; separate UIPanels sort by PANEL depth; the finger renders in a pooled panel at per-group depth DEFAULT 5 (`SetPointerDepthToGroup`) and its limit rect reads `localPosition±size/2` in SCENE space; NGUI scroll state = panel transform + clipOffset counter-sliding (a clone carries the donor's arbitrary scroll state — force the SetDragAmount(0,0) FIXPOINT: clip at panel origin, content top-left on it); the cell TEMPLATE must sit in the table at the row-0 seat (spawned clones inherit its position and SetDragAmount caches bounds from them). **The Option B REDESIGN (★ IN-GAME PROVEN 2026-07-21 — BOTH open defects CLOSED):** the menu-shape-language study (`studies/menu-shape-language/VOCABULARY.md`) ratified the composition as the Chocograph sentence, and the rebuild minted **THE PRESERVE-THE-BAKE LAW** — capture the donor's solved geometry BEFORE mutation and COPY it: the scroll frame keeps the BAKED clip center (Phase B zeroed it — the rows-over-rail mechanism, with the formula-refit dressing canceling the error except at the rail); the dressing seats by `bakedDressingPos − bakedPanelPos − bakedClipOffset` — **THE SCROLL-SUM INVARIANT**: the live donor's three state values are individually wild (clipOffset −1930 on the pristine bake!) and only their SUM is the scroll-invariant window anchor (the 3-lens adversarial review's blocker, fixed pre-playtest and vindicated by the bake log); dressing children keep baked y/height/pivot/active with a width-only refit; `rowCount` DERIVED = round(bakedClipH/rowH) (the stock invariant inverted — absorbs the pre/post-widescreen clone-time variance). The first-open hang measured GONE (awake 4ms, first frames 8/14/18ms; instrumentation now also brackets the Invoke-deferred/LateUpdate window). L1/R1 = SFX **1047** with `ButtonGroupState.MuteActiveSound` held across the 2-frame settle (the rebuild re-seats the cursor and fires 103 on top otherwise; strand-insurance unmute in Hide since deactivation kills coroutines). NOTE: the `FolkloreUI.cs` csproj include is seated after `UIWidget.cs` (NOT beside s22's `Ff9mkDebugMenu.cs`) so the capture has no cross-patch adjacency. **The 9-row overflow fix (2026-07-21):** with the Party hack (`[Hacks] AllCharactersAvailable`) AND Folklore both on, 9 rows overflowed the command window's baked clip — Config drawn outside the window, collider mouse-unreachable. The s45 layout loop now derives its pitch from the row count against the proven 684-unit span (8 rows = Memoria's exact 79/78@−39 walk byte-faithfully; 9 rows = 69/68@−34; colliders track the widget h). **Same round, the row-children seat:** the shared `LayoutSubMenuRow` helper also runs `child.SetAnchor(row.transform)` on each row child (Label / Spliter / `dialog_hilight`) — the Transform overload keeps the baked rel/abs anchor values and executes OnAnchor immediately against the squeezed rect (both the stock Party block and the Folklore block). **And THE TITLE-PLATE COLLAPSE fix (the playtest's "misaligned card" — a stock Memoria bug on Form Party):** every sub-screen's top-right titled window IS the live collapsed command window (`HideAfterClip=0`; bake `TweenClipTo=(0,74)` / `TweenSpriteTo=(0,104)` sized to crop a 90-tall row, aimed by `ShiftContentClip=9+98·rowIndex`). A relaid list breaks both inputs (rows land ~5-11px high; a 69/79-tall row underfills the clip → neighbor slivers). All 8 collapse sites now call `CollapseSubmenuTo(row)` — shift `= 9 + (bakedItemRowY − rowY)` from the row's CURRENT seat, landing the row's center on the collapsed clip's center per the bake's own anchor law; stock `9+98k` bit-for-bit on the untouched vanilla bake; the `shiftFactor` approximation is deleted. The plate keeps its BAKED 74/104 minima (scaling them moves the collapsed clip center off the landing slot — a measured ~8px misalign, reverted). And the Party/Folklore descends set the collapsed state via `ResetToMinimum()` directly (`collapseInstantly` consumed in Hide) — their Show paths kill the collapse coroutine mid-flight, which is why stock Memoria's Form Party title plate freezes half-collapsed; stock screens keep the stock tween. Root-caused offline by dumping the baked UI scene (level2 via UnityPy + TypeTreeGeneratorAPI). Recapture gates (re-run same day): GNU `patch -R -p1 -F0` clean on live in **TEXT mode** + forward `-F0 --binary` clean on a fresh `base+s22..s44` replay, all 5 files byte-identical to live, zero fuzz/offsets — **the forward gate is now TRULY binary-exact: the old "phantom fuzz-1" was a real byte mismatch** (a no-EOL final line has no CR, so the CRLF patch's line before a `\ No newline` marker must be CR-less; the OLD capture failed forward `--binary` identically when re-run — its "clean" record never reproduced). The display window (render rig) = s46. |

## Performance / quality — shipped in the bundle, not a fidelity patch

| File | What it fixes |
|---|---|
| `s35-overlay-texture-cache.patch` | **RETIRED 2026-07-29 (owner call) — reverted from the live tree; the file stays for provenance.** Was: an overlay-PNG **decode cache** for pure-`.bgx` scenes (`static Dictionary<String, Texture2D> MemoriaOverlayTextureCache` in `BGSCENE_DEF.ProcessMemoriaOverlay`'s `Image` op, keyed by resolved path). Why retired: (1) the cache key had NO file identity, so a re-deployed overlay PNG at the same path served the STALE decode for the whole session — the Trace-tab cut-out loop's "art needs a full relaunch" playtest bug (2026-07-29); (2) the 2026-07-07 ★ "fade fixed" claim was CONFOUNDED — the field-entry/entry-settle work churned concurrently and the benefit was never isolated (owner: "never confirmed to be useful"). Removal restores hot-reloadable field art (~ → Reload picks up re-deployed PNGs). If the see-through fade provably returns, reinstate WITH path+mtime+size invalidation (s22's `Reload + anims` precedent for the same disease class). Reverse-applied `-F0` clean; no sibling patch carries its lines as context (s12/s32/s33/s65 checked); DWIXNoDeploy compile-check clean with the game up, then the deploying build closed-game 2026-07-29: Output == both arches sha `44090B29…`, cache symbol verified ABSENT + the fork-gate symbols verified present, pre-build full-set DLL backup `preS35removal.20260729-114832`. ★ **Owner-confirmed same day: re-deployed field art hot-reloads via ~ → Reload ("image movement is hot-reloadable now"); no fade regression reported.** |

## Dev tooling — shipped in the bundle, but NOT a fidelity patch

| File | What it is |
|---|---|
| `s51-world-constructor-name-guards.patch` | **The world-constructor clone-name guards (2026-07-22, found via the custom-vehicle rung-2 mint; renumbered s50→s51 — a concurrent session owns `s50-sfx-camera-matrix`).** `ff9.w_movementMapConstructor` hardcodes each vehicle actor's CLONE NAME — the index-8 boat block `Find("GEO_SUB_W0_008(Clone)")` falling back to `"321(Clone)"` (the loose-override-at-the-REAL-id name), the index-3-7 chocobo block likewise on `GEO_SUB_W0_003`/`308` — and dereferenced the miss UNGUARDED: a **minted** vehicle instantiates as `"<mintId>(Clone)"`, both Finds miss, and the per-frame NRE black-screened the overworld (the Garnet name-keyed-branch law, world flavor). Fix: a third fallback keyed on **the actor's own `posObj.model` id** (the exact rule the `321(Clone)` special case encodes — so a minted hull now GETS the stock draw nudge / chocobo texture wiring instead of merely not crashing) + null guards on wmActor and every transform. Diagnostic red herring for the record: the `Can't add component 'Animation'` Unity error (ModelImporter double-add, `ModelImporter.cs:132` vs `:334`) fires for EVERY loose skinned world FBX incl. the proven Zidane override — non-fatal, not the cause. One file: `ff9.cs`. |
| `s49-world-smoother-anim-guard.patch` | **The RegisterState anim-null guard (2026-07-22, found via the custom-vehicle rung-1 bench).** `SmoothFrameUpdater_World.RegisterState` resolved every world actor's current anim unguarded: an actor whose `originalActor.anim` id is missing from AnimationDB — or whose Animation component holds no clip under that name — made `anim[curAnim]` return null and the `.time` reads NRE **every frame**, inside `HonoBehaviorSystem.Update`'s sweep — which aborts the sweep and with it the WORLD SCRIPT TICK (the HonoBehavior teardown law's exception shape): the player still walks (engine-side) while every `.eb` per-frame loop is silently dead. Symptom that found it: an authored vehicle's Confirm-board trigger dead at point-blank; output_log.txt carried 1,868 `RegisterState` NREs. The guard `TryGetValue` + null-`AnimationState` → `continue` (that actor skips frame-smoothing only) + a one-shot-per-(uid,anim) `Log.Warning` naming uid/model/anim so the log diagnoses the data side. One file: `SmoothFrameUpdater_World.cs`. |
| `s78-debug-menu-flag-band-labels.patch` | **THE ~ MENU'S FLAG-BAND LABELS, CORRECTED (★ IN-GAME CONFIRMED 2026-08-02 — owner playtest after relaunch, `~` → Flags tab; one file, `Ff9mkDebugMenu.cs`; found in the completion-journal T0 scoping pass).** Three wrong numbers in the Flags tab, all one class — the readout mirrored `ff9mapkit/ff9mapkit/flags.py` as it stood in mid-2026 and was never re-mirrored after the band corrections landed there. **(1) `StorySummary`'s "chests opened" was a meaningless total**: it popcounted `gEventGlobal` bytes 1047–1063 = bits **8376–8511**, which is the MOGNET give/read variant lock table (`flags.MOGNET_LOCK_LO/HI`), not chests — real FF9 keeps **no per-chest registry anywhere**. Replaced with the metric the engine actually scores: `EventState.GetTreasureHunterPoints()` (already on that line) **plus its rank H..S**, thresholds verbatim from that method's own comment block (`EventState.cs:53-71` — bytes 896–960 + 966–975 at 1 pt/bit, 182–186 at 2 pts/bit). `CountBits` was the popcount's only caller and is deleted with it. **(2) `RegionLabel` still called 8376–8511 `chest_opened`** — same mislabel, now split into the three real sub-bands (give locks 8376–8439 / read locks 8440–8503 / margin 8504–8511) plus the stock mognet mailbox 8192–8367 it never covered at all. **(3) ⚠ THE LIVE SAVE-CORRUPTER: `RegionLabel` told an author `[safe custom band]` from bit 8512**, but `flags.FIRST_SAFE_FLAG` is **8712** — bits 8512–8711 are stock read-mail payload, whole-byte-written by ordinary Mognet play. It also claimed safety all the way to 16320, over the kit's nameplate-explored words / QTE scratch / netsync coop cells. Corrected to **[8712, 16048)**, with those four bands above it labelled; the comment now says outright that flags.py is the authority and a green label is a hint, not a clearance. **NOT changed — a reported finding that did not hold:** `bit == 184 \|\| bit == 191` is **correct**, not a bit-index-vs-byte-offset confusion. Those are byte 23's engine handshake bits (`flags.py` `field_menu_guard` / `boot_scratch`; `research/STORY_FLAGS.md` row **23**), and the chocograph/dig **BYTES** 184–191 are a *different location* (bits 1472–1535) — now labelled separately, with the aliasing spelled out in-comment so the next reader does not re-file it. Behavior outside the Flags tab is untouched. Gate: the patch applied to the pre-edit snapshot reproduces the live file **byte-for-byte** (`d151d27a…`). Verify in-game: `~` → Flags tab — read the summary line, then type a bit index (8600 → read-mail payload; 8712 → safe; 184 → field menu guard; 1480 → chocograph). |
| `s48-debug-vehicle-rows.patch` | **~ menu: the unused vehicle physics rows 10/11 (2026-07-22, the custom-vehicle rung-0 enabler).** The Vehicle section gains two force-only buttons for the UNUSED `w_moveCHRControl` rows — **10 = a populated stock all-terrain row** (type 3, speed 140, `limit = {MaxValue,MaxValue}` = every topo passable, no encounters) and 11 = the all-zero immobile row — behind the existing "Force every mode" toggle via a new `ExtendedVehicleModes = 0xFFF` mask (the real allow-lists still cap at `AllVehicleModes = 0x3FF`; a force-off click still refuses ≥10 through `VehicleAllowed`). `SetVehicle` gains a physics-row bounds guard (`mode >= w_moveCHRControl.Length` refuses with a row count instead of an IndexOutOfRange mid-frame — 12 rows stock, a `WorldCHRControl` CSV can ship more). ⚠ THE LOAD LAW (in-game confirmed 2026-07-22): rows 10/11 are **LIVE-APPLY ONLY** — no dispatcher Init binds control for a mode >9, so any world LOAD (reload button, field exit, save+load) with `[190]>9` is the 'no controlled actor' black screen; the force-mode hint says so. One file: `Ff9mkDebugMenu.cs`. |
| `s43-debug-menu-ux-tilde.patch` | **The hotkey remap + UX round (2026-07-20).** The debug menu moves **F6 → `~` (tilde/BackQuote)** — stock Memoria already binds F6 as the **LvMax cheat** hotkey (`HandleBoosterButton`), which the old early intercept swallowed; BackQuote is bound to nothing engine-wide. Unlike F6 the key TYPES a character, so the toggle is suppressed while the menu is open with a text box focused (`GUIUtility.keyboardControl != 0` — a closed menu skips the guard, and `SetOpen(false)` clears stale focus). UX pass in the same round: one prefs row (Disable control + Font), tab switches reset the scroll, the diorama bench moved below the everyday Go actions, boxed accent status bar, bold section headers, full-color readouts, whole-header drag, a "~ toggles" header hint, ASCII-safe preset star + batch-hint glyphs (the FF9 fonts lack `·`), and the batch "bad index" error now prints the op grammar (re-landing the fix a 2026-07-16 regen silently dropped). Two files: `UIKeyTrigger.cs` (line-count-neutral, so s37/s38 hunks stay put) + `Ff9mkDebugMenu.cs`. Gate: `base + s22..s41 + s43` applies at zero fuzz/artifacts and reproduces live byte-exactly. **s42 CAPTURED 2026-07-20** (`s42-netsync-config-split.patch`, see its row) — this round's one-string overlay edit in `NetSyncClient.cs` ("meet your friend: ~ menu > Warp") rode with it as planned. |
| `s22-debug-menu-f6.patch` | The in-game debug menu (Warp / Move / Cheats / Flags / Time, in field + battle; **hotkey since remapped F6 → `~` by s43**) **plus a `World` tab on the overworld**: live pos/cell/block/topograph/disc/vehicle readout, vehicle-mode swap (`gEventGlobal[190]` + `w_movementChange`), disc 1↔4 switch, a Disable-control toggle, and a working **teleport** (the fix defeats Memoria's `SmoothFrameUpdater_World` reverter via `Skip`, see `../ff9mapkit/docs/OVERWORLD_ENGINE.md`). The Flags tab has a **Batch** box: many bit/byte/word writes in one click (`1520 !1423 b191=5 w0=7200`), all-or-nothing validated, with the live text + named one-click presets persisted via PlayerPrefs — multi-flag warp-in recipes (e.g. the Chocobo Hot & Cold one) stop being seven manual round-trips. The Go tab also has a **Reload + anims** button beside Reload field: it clears the static `AnimationClipReader.LoadedClips` cache before reloading so a re-deployed loose `.anim` (the model animation-edit loop) shows without a full relaunch — Reload field alone keeps the cached clip. A tester convenience; the beta bundle includes it, but it is not part of the fork-fidelity set or the upstream-candidate set. |
| `s47-sfx-mesh-probe.patch` | **THE SFX MESH PROBE (built 2026-07-22) — default-OFF, permanent debug-class instrumentation, gated on `Memoria.ini [SfxProbe] Enabled=1` (default 0 — zero cost: one cached-bool check per call site, nothing allocated, no file opened, when off).** New `Memoria/Battle/SFX/SfxMeshProbe.cs` (+ csproj include) with two call sites: `SFXDataMesh.cs` `Runtime.Render()` (the `SFXRender.commandBuffer` draw walk, right before the existing per-mesh render loop — that loop itself is untouched) calls `LogFrame()`, which logs EVERY `SFXMesh` in the buffer that frame (hidden/prevented ones included) as `MESH,effectId,frame,index,keyHex,vertCount,triCount,cx,cy,cz,ex,ey,ez` (`effectId`=`SFX.currentEffectID`, `frame`=`SFX.frameIndex` — the same counter the CAM lines use, so the two streams correlate; `keyHex`=`SFXMeshBase._key`, the same hex key `BattleActionCode.TryGetArgMeshList`'s `HideMeshes`/`-Colors` take; bounds are WORLD space here because `SFXMesh.Render()` draws via `Graphics.DrawMeshNow(_mesh, Matrix4x4.identity)`, no transform in play). `SFXDataCamera.cs` `UpdateCamera()` calls `LogCamera()` once per frame inside the existing `CameraEngine.SFX_PLUGIN` branch (right after `SFX.UpdateCamera()`), logging `CAM,frame,px,py,pz,rx,ry,rz` from `Camera.main`. Output: a fresh `./sfxmeshprobe.log` per launch (`FileMode.Create`), flushed every write, wrapped in try/catch so a probe failure can never take the real battle render down. **Why:** the Bahamut→Thomas custom-summon swap needs (1) the mesh-key split between a donor summon's creature meshes and its swirl/flare/effect meshes for `HideMeshes`, and (2) the donor's baked per-frame camera path to author a replacement flight curve against — one instrumented cast (`build_thomas.py --calibrate`, no `HideMeshes` filter, so every native mesh renders and gets logged) gives both, no `ef###.bytes` decoding. Arming + read-out procedure: `studies/custom-summons/thomas-swap/PROBE.md`. Gate: reverse `patch -R -p1 -F0` clean on live in TEXT mode; forward `-F0` (TEXT mode) onto a pre-round reconstruction of the 3 touched files reproduces live byte-for-byte once EOL-normalized (the new file is LF in the stored patch / CRLF in the live tree — the same accepted EOL-only class as the other patch-created files in this stack). Built + deployed clean (0 errors, 186 pre-existing warnings, unrelated) while the game was closed; `Output\Assembly-CSharp.dll` byte-identical to both deployed `x64`/`x86` copies. |
| `s48-sfx-output-capture.patch` | **THE SFX OUTPUT CAPTURE (authored + ★ BUILT + DEPLOYED 2026-07-22; 0 errors, both arches, output==deployed sha; the CAM hook now logs + `CapturePrims` works).** Two additions on top of s47, both still gated on `[SfxProbe]`: (1) **the dead CAM hook, fixed by relocation.** s47's `LogCamera()` lived in `SFXDataCamera.cs` `UpdateCamera()` and read `Camera.main` directly — root cause of the observed 19,436 MESH rows / 0 CAM rows in one cast: `Camera.main` is routinely null during native SFX battle playback (15+ other call sites in this exact subsystem already fall back to the `"Battle Camera"` GameObject for that reason), *and* that call site is driven by `battle.cs BattleMain()` every Unity `Update()` tick, uncorrelated with the native SFX frame actually advancing. `LogCamera()` now takes the camera as a parameter and is called from `SFXDataMesh.cs` `Runtime.Render()`, co-located with `LogFrame()` — same `SFX.frameIndex` per row, and it reuses that method's own already-resolved `Camera.main`-or-`"Battle Camera"` local (no second lookup). The old `SFXDataCamera.cs` call site is removed (comment explaining why left in its place). (2) **`SFX_GetPrim` output capture (`CapturePrims=1`)** — hooks the TOP of `SFXRender.Add()`, the single funnel every native-decoded PS1 GPU primitive passes through BEFORE it gets batched into a keyed `SFXMesh`; richer than the MESH rows (individual triangle/quad/line/tile/sprite, own tag + depth-sort order, not yet key-merged). Reads vertex0 generically (every primitive struct places `x0,y0` at the same fixed byte offset 8 — no per-type cast needed), logs `PRIM,...`/`STATE,...` (drawable vs. DR_TPAGE/DR_AREA/DR_OFFSET/DR_MOVE state tags) rows, with two volume-bounding controls (`PrimSummary=1` collapses to one `PRIMSUM` row/frame; `PrimCap=<N>`, default 200000, hard-caps row count with one truncation marker) — a `FlushPrimSummary()` call in `SFXDataMesh.cs` `Runtime.End()` flushes the last frame's pending summary row. Touches `SfxMeshProbe.cs`, `SFXDataCamera.cs`, `SFXDataMesh.cs`, `Global/SFXRender/SFXRender.cs`; no csproj change (no new file); does not touch any debug-menu file. Gate: `git apply --check` clean from the s47 tip, verified byte-exact round-trip (before → patch → after) offline. The msbuild withheld that round (to avoid clobbering unrelated in-flight debug-menu edits sharing the same working tree) has long since landed — s50, s52, and s53 were built on top of this patch and deployed, so the ★ BUILT + DEPLOYED stamp above is the live status. |
| `s53-sfx-model-census.patch` | **THE MODEL CENSUS + COMPOSED TRANSFORM + NATIVE CAMERA (★ BUILT + DEPLOYED 2026-07-23; the s52 CORRECTION). Two files (`Memoria/Battle/SFX/{SfxMeshProbe.cs, SFXDataMesh.cs}`), built on s52; round-trips byte-exact onto the s52-tip; 0 errors, both arches sha `3cca581c…`, `LogModels`/`CaptureModels`/`WriteNativeCamera`+`PSXCAM`/`MODEL`/`BONES` literals verified in metadata; pre-build DLL backup `20260723-110746`.** Adds `SfxMeshProbe.LogModels()` (hook at `SFXDataMesh.cs` beside `LogSummonRoot`), gated on new `[SfxProbe] CaptureModels=1` (+`Enabled=1`), logging three row types: **PSXCAM** (x64) = the native world→view matrix `M` @`0x1C1DC8` + GTE screen params OFX=160/OFY=120/H (`0x211FA0/A4/A8`) + `PsxCtx[+0x14]` tamper check — the FORMAT round found the creature projects through *this* native GTE, not the managed VIEW/PROJ (retracted, failed 88.7% of frames); **MODEL** (both arches) = per-frame identity+flags+draw-anchor+**composed** matrix (node 0 of `*(DATA+0x38)`, the real drawn transform, not the s52 `+0x40` anchor) for the summon slot + all 32 eff slots; **BONES** (opt) = summon node AABB+centroid. Makes reprojection a zero-free-parameter native identity (`p_view=M·bones[0].t`; `SX=OFX+((sat16·((H<<16)/z))>>16)`); read-out protocol in `studies/custom-summons/thomas-swap/disasm/FORMAT.md` §5.4 + `D1-creature-id-probe.md` §4. Provenance: staging/choreography only (same class as the camera track); NO geometry, NO per-bone matrices; no DLL modified. Off by default. |
| `s52-sfx-summon-root.patch` | **THE SUMMON ROOT-TRANSFORM PROBE (★ BUILT + DEPLOYED 2026-07-22; 0 errors, 186 pre-existing warnings, exit 0; `Output\Assembly-CSharp.dll` == both deployed `x64`/`x86` copies, sha256 `4343c3da…`; `LogSummonRoot`/`CaptureRoot`/`GetModuleHandle` + the module-name/ROOT literals all present in the deployed metadata). Two files: `Memoria/Battle/SFX/{SfxMeshProbe.cs, SFXDataMesh.cs}`; no csproj change (no new file), no debug-menu file touched. Pre-build full DLL backup: `20260722-234755` (`py tools/restore_memoria_dll.py 20260722-234755` reverts the whole engine).** Adds `SfxMeshProbe.LogSummonRoot()`, called from `SFXDataMesh.cs Runtime.Render()` co-located with `LogFrame`/`LogCamera` (same `SFX.frameIndex` per row), gated on a new `[SfxProbe] CaptureRoot=1` sub-flag (default 0; also needs `Enabled=1`). It logs the summoned creature's TRUE per-frame WORLD transform — the one datum neither the MESH bounds (pool-polluted to the origin) nor the PRIM stream (already screen-projected by the plugin's own GTE) could recover — as `ROOT,effectId,frame,active,m00..m22,tx,ty,tz`. Mechanism (decoded by the 2026-07-22 disasm round, `studies/custom-summons/thomas-swap/disasm/FINDINGS.md`, and independently re-verified here): a stock summon renders entirely inside `FF9SpecialEffectPlugin.dll`; its one-slot summon-model record → `SummonData` block holds the creature's root world MATRIX (32-byte libgte MATRIX, 3×3 s16 rot /4096 + s32 translation), rebuilt every `Hi_DrawSummonModel`. Zero-on-disk, crosses no managed boundary, but a plain mapped read of the plugin's own runtime state: `GetModuleHandle("FF9SpecialEffectPlugin.dll") + recRVA → +0x00 → root`. **Arch-correct** (both shipped builds, adversarially cross-checked): x64 recRVA `0x220830`/active `+0x50`/root `DATA+0x40`; x86 recRVA `0x20869c`/active `+0x4c`/root `DATA+0x24` (selected by `IntPtr.Size`). Provenance-clean: reads the ROOT transform ONLY (choreography/staging, same class as the camera track already logged) — never the per-bone array (that would reconstruct stock skeletal animation); patches no DLL, calls no export. **Why:** closes FLIGHT v7's staging gap — one instrumented cast yields Bahamut's real metric trajectory to hang the Thomas puppet on; the offline reprojection check (`studies/custom-summons/thomas-swap/root_reproject.py`) validates each ROOT against the same frame's VIEW/PROJ vs the PRIM centroid. Arming + read-out: `studies/custom-summons/thomas-swap/PROBE.md` §10. Gate: `patch -p1` applies to the s50-tip pristine of both files and reproduces the live edits byte-for-byte (round-trip verified). Built with FF9 closed via `msbuild Assembly-CSharp.csproj /t:Build /p:Configuration=Release /p:SolutionDir=C:\gd\FFIX\Memoria\ /m` (the trailing-backslash SolutionDir is required); `AfterBuild` auto-deployed both arches. |
| `s50-sfx-camera-matrix.patch` | **THE REAL CAMERA -- worldToCameraMatrix + projectionMatrix capture (built 2026-07-22; s50 is FREE -- the concurrent vehicle session renamed its own s50 to s51). One-file follow-up to s48: extends SfxMeshProbe.LogCamera to also log VIEW (world->camera) + PROJ (the hand-built off-center PSX frustum, -Y flip included) rows per frame, 16 floats each, row-major m00..m33. Root cause it fixes: s48s CAM row logged Camera.transform, which native SFX_PLUGIN playback NEVER updates per frame (SFX.cs:1603-1604 overrides worldToCameraMatrix/projectionMatrix directly while .transform stays frozen at the BattleMapCameraController Awake pose) -- so the 561/561-identical CAM rows were a frozen-transform artifact, not a static camera. The two matrix rows carry the true per-frame camera: MVP=PROJ*VIEW, invert to place any mesh at any target NDC (the Bahamut->Thomas placement solve). Gated on [SfxProbe] Enabled; inert when off. Deployed live via the concurrent vehicle-sessions 17:10 rebuild of the shared tree (which already contained this edit -- verified: worldToCameraMatrix + VIEW,/PROJ, strings present in the deployed DLL); reverse git apply -p1 clean on live. |
| `s58-sfx-hybrid-drive.patch` (authored as s54; renumbered at the 2026-07-24 master merge — the netsync reliability round claimed s54–s57 first) | **THE SFX HYBRID DRIVE (★ BUILT + DEPLOYED 2026-07-24; ships inert, `[SfxHybrid] Enabled=0`). The first SFX feature PAST the passive s47–s53 probes — it RENDERS content AND performs ONE runtime write into plugin state (the body-hide mask).** 3 files, 482 insertions: new `Memoria/Battle/SFX/SfxHybridDrive.cs` (the feature class, 469 lines) + csproj `<Compile Include>` (alphabetical, line 510) + two hooks in `SFXDataMesh.cs` (`Runtime.Render()` after `SfxMeshProbe.LogModels()` → `if (SfxHybridDrive.Enabled) SfxHybridDrive.Drive();` at :667; `Runtime.End()` after `FlushPrimSummary()` → `SfxHybridDrive.EndCast();` teardown at :701 — the lifecycle complement so a cast can't leave a frozen puppet). **Build: 0 errors, 186 pre-existing warnings; `Output\Assembly-CSharp.dll` == both deployed `x64`/`x86` copies, sha256 `2A9C8B148824FF82C0350C492FDE150C54A744C2D822EBE23A080C05B6BB23CE` (all three identical). Pre-build full DLL backup `20260724-091433` — `py tools/restore_memoria_dll.py 20260724-091433` reverts the whole engine (both arches, all 3 DLLs). Metadata verified in both deployed arches: identifiers `SfxHybridDrive`/`EndCast` (#Strings/UTF-8) + literals `SfxHybrid`/`HideNative`/`HideMask`/`NodeCount`/`ApplyColumnScale` (#US/UTF-16); the s53/s52/s47 probe symbols (`LogModels`/`CaptureModels`/`LogSummonRoot`/`SfxMeshProbe`/`PSXCAM`/`CaptureRoot`) ALL still present — the stack composes, nothing regressed.** Per frame, while `Enabled=1` and the configured `EffectId` (default 227/Bahamut) is casting, it reads the plugin's freshly-composed per-node world matrices (`*(SummonData+0x38)`, the s52/s53 read path, arch-split by `IntPtr.Size`: x64 recRVA `0x220830`/active `+0x50`/bones `+0x38`/mask `+0x20`; x86 `0x20869c`/`+0x4c`/`+0x20`/`+0x14`), converts each to Unity world via the M0-verified calibration (position `(tx,-ty,tz)` scale EXACTLY 1; rotation `B·R·B`, `B=diag(1,-1,1)`, det<0 column-2 flip guard via `Quaternion.LookRotation` — `Matrix4x4.rotation` does NOT exist on Unity 5.2.3; `m0/CALIBRATION.md`), and writes it onto our model's bone `k` for `k<min(model.bones.Length, NodeCount=93)` — so our `SkinnedMeshRenderer` rides the dragon's real motion through the same `Camera.main` (M1a camera-inheritance proven). `HideNative=1` asserts the total-hide mask (`SummonData+0x20`/`+0x14`, default `HideMask=0x3` = Bahamut's 2 meshes; D4) — the ONE plugin-state write. Model loaded once/cast via `ModelFactory.CreateModel` (SFX texture-filter, Animation disabled, `updateWhenOffscreen=true` — the s46 render-rig culling law); stale-arena ROOT guard (skip a frame whose node-0 `|t| > 1<<24`, hold last good pose); `ApplyColumnScale=0` escape hatch OFF (position spread already carries the authored scale sweep — writing localScale doubles the creature, `CALIBRATION.md §5`). Whole body in try/catch — **self-disables for the process on the first fault** and releases anything half-built (a go/no-go render feature must never take the real battle render down). Default OFF: a normal install creates nothing, reads/writes no plugin memory, pays one cached-bool check. Provenance-clean: engine code only; the creature loads by path from the user's OWN mod folder (never a FileList in the donor's `ef###/`, `m0/FBX-PATHS.md`). Gate (this build): `git apply --check` + `--check --binary` clean on the s53-tip clone; `--binary` apply landed exactly the 3 expected paths; DWIXNoDeploy compile-check clean (0 errors) BEFORE the deploy build. **NOT yet cast in-game — armed by the arming agent (`Memoria.ini` untouched here); M1b in-game playtest pending.** Milestone 1b of `disasm/TRANSPLANT.md` §2.4; spec + risks `studies/custom-summons/thomas-swap/m0/S58-DRAFT.md`. |
| `s76-sfx-state-payload.patch` | **THE DR_MOVE / DR_TPAGE PAYLOAD (★ BUILT + DEPLOYED 2026-07-30; a LOG-FIELD round — additive only, zero behavior change, no new ini key). One file (`Memoria/Battle/SFX/SfxMeshProbe.cs`), built on the s53 tip; 3 hunks, +67/−6; 0 errors / 186 warnings, both arches sha `b6ce810fa9b710b8…` == `Output\Assembly-CSharp.dll`; all 8 new literals + the retained s47/s48/s53/s58 markers verified in metadata (UTF-16 `#US` for literals, UTF-8 `#Strings` for symbols); pre-build DLL backup `20260730-222511` → `py tools/restore_memoria_dll.py 20260730-222511`.** WHY: the U1 second-array cast came back VISIBLE_UNBANDED (column-640 geometry drew, neither marked texel cell sampled) and its probe log shows **641 `DR_MOVE` packets on ef038 in two blocks (effect frames 86–138, 237–380) where the static direction-law census records ZERO** — the leading mechanism is a runtime VRAM blit wiping the marks, but the log recorded DR_MOVE **without its rects** and DR_TPAGE **without the code word** (41,561 bare rows), so it could neither be confirmed nor tied to a primitive. Now `LogPrim()` dispatches per-type on the raw code, mirroring `SFXRender`'s own decode field-for-field: **`DR_TPAGE` (225)** appends `tpage,tx,ty,abr,tp,len` — `tpage` = low16 of `code[0]` exactly as `SFXRender.cs:430` feeds `SFXKey.SetCurrentTPage`, bit fields per `PSXGPU.cs:25-43` (`tx=&15` ×64px, `ty=(>>4)&1` ×256px, `abr=(>>5)&3`, `tp=(>>7)&3`), so the marked page at VRAM (640,256) is simply `tx=10,ty=1` — this also settles the U1 study's open twin-pair co-draw question, which is why it rides the same edit; **`DR_MOVE` (231)** appends `rx,ry,rw,rh,x,y,path,len` — SOURCE rect origin+size and DEST origin exactly as `SFXRender.cs:455-462` decodes them (`ry` signed), with `path` recording the engine's own branch at `:464-472` (**`MV`** = `SFXMoveImage`→`PSXTextureMgr.MoveImage`, the real VRAM blit; **`SS`** = `SFXScreenShot`, a framebuffer grab, taken when `rx<320 && ry<240`) — so "did something blit over (640,256) before the scored surfaces drew" becomes a one-grep answer instead of an offline re-derivation. Both log `tag->getLen()` because `PSXGPU.cs:607-609` calls code 225 with `len==2` **DR_MODE** while `SFXRender.cs:302-309` routes every 225 to `DR_TPAGE()` regardless — a `len!=1` row must not be read as a page select. SAFETY: reads are per-type and never generic — DR_TPAGE's word is inside the 8 bytes `LogPrim` already dereferences (P_TAG's `r0/g0/b0/code` word **is** `DR_TPAGE.code[0]`), and DR_MOVE's `code[1..3]` are dereferenced by `SFXRender.DR_MOVE()` on the same pointer two statements after `LogPrim` returns, so the probe cannot fault where the frame would not; body stays inside the existing `try{}catch{}`. Row **COUNT** is unchanged (fields APPENDED to the existing `STATE` row), so the `PrimCap` contract and its truncation marker are byte-identical; measured cost ≈ **+2.9 %** on a 23 MB cast log (+0.65 MB DR_TPAGE, +17 KB DR_MOVE). Rides the existing `[SfxProbe] CapturePrims` gate — **`Memoria.ini` NOT touched** (already armed), `PrimSummary=1` still returns early, off by default for a normal install. ⚠ **CONSUMER HAZARD — `STATE` rows are now RAGGED by label.** All 3 committed STATE parsers (`thomas-swap/m0/{camera_match,verify_camera_match/indep_verify,verify_depth/scan_structure}.py`) are positional through `p[2]`/`p[5]` with `IndexError` guards and are unaffected; every other committed reader filters to PRIM/MODEL/ROOT/MESH/BONES — **0 committed parsers break** (verified by grep, incl. zero uses of the `endswith(label)` idiom). The one break is the SCRATCH throwaway `castread/adversarial/scripts/a22_drawproof.py:37` (`line.rstrip().endswith('DR_MOVE')`) — the very script that produced the 641-packet finding. It fails **SILENTLY to zero**, the dangerous direction (a zero census would read as "mechanism refuted"); fix is one line: `',231,DR_MOVE' in line`. Header now documents the ragged shape and says *parse positionally, never with `endswith(label)`*. BUILD-RECORD NOTE: the patch was cut from a **before/after snapshot pair** in scratch, not `git diff` in the shared clone — `SfxMeshProbe.cs` is a staged `A` there and the tree carries 73 files of concurrent-session edits, so a clone-side diff would have emitted the whole file plus foreign hunks; reverse-checked with `git apply` onto the before-snapshot, reproducing the live file **byte-for-byte** (`35602c6c…`). Provenance: engine code only, reads the packet header the engine itself decodes on the same frame; no asset bytes, no geometry, no SE data — same class as the s48 output capture. Interpretation caveat: a block of `SS` rather than `MV` packets would **refute** the leading mechanism, not confirm it. |
| `s77-sfx-uv-range.patch` | **THE UVR ROW — per-mesh UV ranges (★ BUILT + DEPLOYED 2026-07-31, owner-authorized after a classifier stop: the patch was cut and reverse-checked byte-identical onto the s76-tip snapshot `35602c6c…` → `6900c2d6…`, then applied + built — 0 errors, Output == x64 == x86 sha `44d4b97473ec69e9…`, the UVR literals + retained s47-s76 markers verified in the deployed binary; pre-build DLL backup `20260731-192628` → restore `py tools/restore_memoria_dll.py 20260731-192628`).** WHY: U1 cast 2 proved a +128-texel u displacement on ef038 (0.84) with the labelling at 0.68 and the v axis unresolved; the impact scoping gated any kit modelling behind THIS instrument. One file (`Memoria/Battle/SFX/SfxMeshProbe.cs`), 5 hunks, additive-only, rides `CapturePrims`: a NEW rectangular row type **`UVR,effectId,frame,index,keyHex,src,prims,uMin,uMax,vMin,vMax,tpage,clut`** — one row per TEXTURED MESH row, joined 1:1 on (effectId,frame,index,keyHex); per-(key,frame) MIN/MAX of the primitives' OWN u,v bytes, accumulated in `LogPrim` BEFORE the `PrimSummary` early-return and the `PrimCap` drop (either placement below would truncate SILENTLY — the dangerous direction) and emitted beside the MESH row in `LogFrame`. Offsets per struct from `PSX_LIBGPU.cs` (clut +14 everywhere; tpage **+22 on FT / +26 on GT** — the per-vertex rgbc words shift GT; a generic table would misread half the textured population); keys mirror `SFXRender.Add()`'s dispatch exactly (36/44/52/60 plain, 76 `\|FILTER_POINT`, 68/72 `\|FILTER_BILINEAR\|FULL_BLUR_TXTURE`, SPRT 100/116/124 via the current-page key with the DISPATCH w/h span) — the two key builders are VERBATIM MIRRORS of `SFXKey.cs:19-45` **minus the `tmpABR` write** (`SFXMesh.cs:85` reads it; an instrument must not poke shared render state). Accumulator: pass-stamp reuse, ~20 entries ever, zero steady-state GC, repeat-Render() re-emits correctly. Volume: UVR ≤ MESH by construction, ≈+1.6 % (~8,300 rows/cast); per-prim would have been 24×. MESH rows stay RECTANGULAR (all 7 committed MESH parsers dispatch on `p[0]` with no bare `else` — 0 break; a new row type touches nothing, the opposite trade from s76's ragged STATE for the row type this repo parses most). THE PRE-REGISTERED READ (fixed BEFORE any cast, from the cast-2 archive's own key census): ef038's two `tx=10,ty=1,tp=1` polygon keys (`00BABDC0` 1,086 rows / `00BABD40` 384) ARE the marked-page readers invisible to the `DR_TPAGE` sprite census, and the `DR_MOVE` band covers only `u∈[0,127]` — so **`uMin≥128` on those keys** = the +128 baked into the prims, cast-1's null explained mechanically, labelling confirmed at the mechanism; **`uMax<128`** = displacement happens PAST the prim stream (sampler-side), and the offset hunt moves into `PSXTextureMgr`; **split/point-sample rows** are scored per the `prims` field before anything else. One stock log-only Shiva cast closes it. **★ RESULT (2026-07-31, one cast, 8,587 UVR rows): THE STRONG BRANCH — the displacement is BAKED INTO THE PRIM STREAM, not sampler-side. The second array is a per-slot texel displacement: pair position 0 onto u, position 1 onto v, 0x80 = +128 texels each; 0.97 on ef038, all four `(pos0,pos1)` cells on DISJOINT reader populations with zero residue, adversarial critic `ok=true` with ZERO gaps.** The instrument is a keeper and stays permanent — it reads any container's EFFECTIVE sampling from one log-only cast. ⚠ The pre-registration above is recorded as **UNDER-SPECIFIED**: both enumerated branches (`uMin≥128` AND `uMax<128`) were observed, split by FAMILY — which is the in-cast control doing exactly its job, but neither branch as written anticipated it. Read + limits: `studies/custom-summons/tier-w/U1-SECOND-ARRAY-CAST.md` §12. |
| `s59-debug-warp-settle.patch` | **WITHDRAWN SAME DAY (2026-07-25; playtest: "it just flickers black") — a mis-diagnosis, kept as dead history.** The warp-arrival drift it chased was the BENCH TOMLS missing `entry_settle` (the importer never emits it; the mechanism itself is warp-PROVEN — field-entry rung 7's waystation check was literally a warp) — a one-line DATA fix, now in the bench generators. The engine hold was redundant AND wrong: re-asserting `FF9Wipe_FadeOutEx(1)` per frame fights the destination's own fade-in = the flicker. Source reverted to the pre-s59 snapshot, engine restored from backup `20260725-150924`, s59 is NOT in the live stack. Lesson: when a report names a kit mechanism ("entry settle isn't long enough"), check the DATA before minting engine behavior. **The data fix ★ CONFIRMED same day ("warp works clean now, entry settle covers it") — the engine never needed anything.** |
| `s67-rig-probe.patch` | **REMOVED 2026-07-29 at ladder close (reverse-applied clean; the rebuild deployed sha `928880F4933ADBA2…` — Output == both arches, all three DLLs; the same closed-game build trued up the s66-era Memoria.Prime x64 timestamp; pre-removal DLL backup `20260728-233405`). Was:** the s63 descendant rig-camera diagnostic — fired while any scripted EYE/AIM rig was designated, ANY world (+ a 90-frame disarm tail), dumping BOTH rig position domains (canonical `Actor.pos[]` vs the epoch-correct wmActor transform), `WEye/WAim`, and the REAL render camera (`WMWorld.MainCamera` — `w_frameCameraPtr` is a parked decoy). Its first run diagnosed the wrap-epoch camera bug (s68) in ONE playtest. Patch kept as reusable history — re-apply if a rig bug ever needs live eyes. |
| `s69-minimap-visible-state.patch` | **THE MINIMAP STATE SYNC (★ IN-GAME PROVEN 2026-07-29, owner-confirmed after relaunch — closed-at-boarding arrives with no stray panel, open-at-boarding hides through both cutscenes and re-arms live in its corner; built + deployed both arches sha `421356C202D850C4…`).** One hunk in `WorldHUD.cs SetMinimapVisible` — the upstream Fix #670 state-sync, applied only on world 9005 (Hilda Garde 1), generalized to EVERY world exactly as its own comment muses ("Maybe it should be always done like that"), guarded to the two HUD states so a scripted toggle can never yank `currentState` out from under FullMap. Root cause it fixes (owner playtest, the rung-3c voyage): `RunWorldCode(2,1)` at scene close activates the minimap panel WITHOUT the HUD state machine — `WorldHUD.Update()` lays out / animates the minimap only while `currentState == HUD`, so a player who sailed with the minimap CLOSED arrived to an inert panel at the raw prefab position (and the Select toggle desynced). Identity on 9005's fixed case; pairs with the rung3c v2 script bracket (cache `Global.Byte[100]` → `Map.Byte[53]`, force `w_naviMode=0` via `RunWorldCode(4,0)` in the prologue so the HUD's DEFERRED `Show()` keeps the panel closed through both cutscene halves — the (2,0)-then-Show-reopens race the owner saw — restore + conditional re-open behind the final black). The script bracket REQUIRES this patch or its close re-open goes inert. Pre-build DLL backup `20260729-080314`. |
| `s68-rig-publish-epoch-frame.patch` | **THE RIG EPOCH FIX (★ IN-GAME PROVEN 2026-07-28, scene-ladder rung 1a — the composed shot landed; sha `B7300CF4EA5425A8…` both arches; backup `20260728-181259`).** The world draws in a WRAP EPOCH that shifts as the player traverses seams; every actor's TRANSFORM stays epoch-correct via `SetAbsolutePositionOf`, but `ProcessEvents.cs:186-202` published the scripted camera rig from canonical `actor.pos[]` — probe f=257: `CAM=(12,-5.6,-1182)` while the drawn quay sat at ~(800,-656), ~a half-map from anything streamed. Fix: publish from the wmActor transform ×256 (canonical fallback when no wmActor); stock scene worlds load fresh (epoch == canonical) → behavior identical. Also REFINES s66's rationale: the drag bug was a FRAME mismatch (render-frame aim written as canonical player pos = the (797,−656) ejection at the ship's render coords), not ×256 scale. |
| `s79-selfheal-safe-fallback.patch` | **THE SAFE-FALLBACK SPAWN MOVES OFF THE SEA (b18 release round, 2026-08-06 — one file, `ff9.cs`, two constants + the comment).** Resolves s39's author TODO with a measured verdict: `kWorldSafeFallback` (768, −640) — the map centre — was **verified OPEN SEA** with the kit's engine-faithful ground query (`world/placement.py` vs the stock disc-1 blocks: the point is the corner of blocks [11][9]/[12][9]/[11][10]/[12][10]; three do not exist, the fourth grounds on Sea4 topograph 57). New value **(1092, −788)** = Block[17][12], the **Dali plain** — the first overworld ground a new game walks: a single walkable sheet, ground_y 11.4, dominant plains topograph 49, gentle slope, ~30u clear of the Dali entrance trigger. Gate: applies `-F0 --binary` on the post-s66 `ff9.cs` and the output is byte-exact vs the b18 proof tree. In-game exposure is the degenerate-spawn recovery path only — ordinary play never reads it. ⚠ 2026-08-26: this patch had been LOST from the shared clone `C:\gd\FFIX\Memoria\` — its `ff9.cs` still held the pre-s79 open-sea `(768, −640)` and the original TODO comment, so any rebuild would have silently regressed the fix while looking clean. Re-applied to the clone (`git apply`, clean) and reverse-check verified. Shipped in the b19 bundle, so the bundle was never affected; only a fresh build from the clone was at risk. If you rebuild, confirm `kWorldSafeFallbackX = 1092f` first. ✅ The 2026-08-26 rebuild (see s80) shipped it into the live DLL — `(1092, −788)` verified as IEEE-754 constants in the deployed metadata, so live and bundle now agree on this patch. |
| `s66-world-aim-drag-dummy-guard.patch` | **THE AIM-DRAG DUMMY GUARD (BUILT + DEPLOYED 2026-07-28, scene-ladder rung 1a — Output == both arches sha256 `63B8FF83956DB472…`; pre-build backup `20260728-173327`; built while FF9 ran: Assembly-CSharp/UI landed both arches, Memoria.Prime x64 copy was blocked (file mapped) — Prime source is UNCHANGED since s65 so the block is cosmetic; trued up 2026-07-29 by the s67-removal closed-game build).** `w_cameraSetEyeAim`'s aim-rig else-branch (ff9.cs:2912) wrote the fp-domain `w_cameraWorldAim` (fed from `GetControlChar().pos[]`, ×256; wrap constants 393216/327680 = 1536·256/1280·256) through the RENDER-domain `pos0/1/2` WMActor transform setters — a ×256 domain mismatch, every frame an AIM rig is armed. Stock never renders it (scene worlds run controlUID=0 → `w_moveActorPtr` = the inert `w_moveDummyCharacter`), but a free-roam world with a CONTROLLED player teleported the PLAYER ~256× away, map-wrapped (rung 1a round 1: the (797,−656) ejection; heal exonerated by log). Guard = drag only when `w_moveActorPtr == w_moveDummyCharacter` (the s61 :4678 idiom, inverted) — stock scene worlds byte-identical; scripted AIM rigs become legal in controlled worlds (the scene ladder's 1b gate). |
| `s65-fork-disc1-member-gates.patch` | **THE MEMBER-DONOR GATE SWEEP (★ BUILT + DEPLOYED 2026-07-28, the F3 Lindblum round — 0 errors, Output == both arches, sha256 `104C7ADAAAEED326…F424A20C`; pre-build DLL backup `20260728-093626`. The build ran while FF9 was still open — the copy landed anyway (Unity doesn't hold the file), the running session kept its old in-memory engine, and the game closed right after: the next launch loads s65).** 73 sites / 17 files: every census gate (`docs/FORK_IDGATE_MAP.md`) still RAW whose donor is a CURRENT opening-campaign member (or imminent Gizamaluke 701-707 / Burmecia 750-769), wrapped with `EffectiveFieldId`. Alias assignments wrapped once per function (the s30 form: EMinigame ×15, EventCollision ×6, EventInput :88, FieldMap ×5 incl. the NarrowMapList/widescreen `map`, FF9Snd :339 = the 613/615 festival phrase-skip, AllSoundDispatchPlayer :134 = the 503 cargo BGM); inline wraps for the rest — BGSCENE 552 Main-Street overlay-17 sprite separation, EBin 705 Bell-Room moogle / 657 Marsh pond / 3009, CommonSPS 1206/1223 SPS visibility + 50/51 candle scale, SPSEffect 1216/1808, SceneDirector 60, ETb :424 + EMinigame MappingATEID inner ids 206/253/204/262/306/**554/552/565** (the Lindblum compulsory ATEs) + hunt/theater achievements (600 ViviWinHunt, 1850, 2457…), EventInput 606 telescope, FieldMap 3009-3011/2161, FieldMapActor 661/2107/2102/3002, FieldMapActorController 1204/2050/350/1315/3010/**576 festival Cid neck**/116/406/1348-cluster, ProcessEvents 257/52, EventEngine 2161, TileSystem 312 (`_fieldMapNumber`). Deliberately skipped: AndroidTV-dead ETb :114/:401, `&& false` EventEngine :774, non-member-gating aliases (Dialog :1600/:1631, FMAC :537, ETb :289, EventInput :220, FF9Snd :1393, SPSEffect :254) — extend when their zones fork. TWO CENSUS ROWS CLOSED WITHOUT CODE: `fldLocNo` CARRIES on forks (`fldLocNo = eventIDToMESID[fldMapNo]`, and a verbatim fork registers its donor's mes id — live-verified `FieldScene 6248 … 525`), so MappingATEID's outer `fldLocNo` branches and ALL of `UniversalTextId` (keys on the text area) work unmodified; only inner `fldMapNo` compares needed wraps. That wrong-inner-id path (wrong mapped ATE id → wrong avail bit cleared) is the leading suspect for the F2 Dali ATE dup-row residual. |
| `s62-vib-name-key-fork.patch` | **THE VIB/ANIM NAME-KEY FORK FIX (★ BUILT + DEPLOYED 2026-07-27, sha `1cbc23b00ccb5595…` all three copies).** A FOURTH member of the s31/s33 name-key class, found via a live stack trace: `HonoluluFieldMain` loads the EventAnimation preload list AND `vib.LoadVibData` by the field's OWN event name (`FF9DBAll.EventDB[MapNo]`) -- a fork's minted name misses both. The anim miss = the benign `EventAnimation/EVT_OPEN_*.txt not found` log spam (anims lazy-load; possibly the 6003 cutscene walk-snap). The VIB miss is FATAL-per-frame: `vib.Parms.vibPtr` stays null and the donor script's rumble ops THROW `Nullable.get_Value` inside `DoEventCode`, aborting the whole event frame -- hung cutscene actors + skipped story writes (logged twice in the forked cargo-ship fields at the F2 run). Fix: resolve the DONOR's event name via `EffectiveFieldId` for both loads (faithful: forks get the donor's real rumble + preload list), PLUS null-guards on the three unguarded vib entry points (`VIB_setTrackActive`/`VIB_setTrackToModulate`/`VIB_getTrackActive`) so a genuinely-vibless field no-ops instead of throwing. Stored patch = the cumulative two-file diff off the stack (regenerate standalone if rebasing). |
| `s64-selfheal-cutscene-hard-gate.patch` | **THE 9001 ROOT-CAUSE FIX (★★ OWNER-CONFIRMED 2026-07-27 — "it worked! the ship flew to Lindblum"; final probe-free engine sha `da742f227cdc1929…` both arches; the stored patch is the standalone s64-only diff).** The s63 probe caught the mechanism LIVE: `w_worldSelfHealControl` fired at world-frame 1 of the 9001 flight — **the s61 index gate is defeated in scene worlds because the script's invisible EYE/AIM camera-rig entries carry no SetObjectIndex and default to index 0**, reading as "player-class". The heal then `SetControlToLeader`-bound control to the first cid-4 obj = **the cargo ship itself** (probe: ctl 0→3 at f=1), and its step (3) re-seated the ship at the SAVE's standing spot (probe: ship pos (299553,−231893)→(201249,−35285) at f≈5 = the Dali-area world pos), tearing the flight off its waypoint frame — ship pinned in a ~50u box forever (the "circling"), `Byte[26]` stuck at 0 (waypoint 8 unreachable → no fade → no Field(503) → scene never ends), title thrashing off the wandering index-0 rig. Every A/B'd engine contained the heal (it landed 2026-07-04) — why no A/B ever changed the symptom. Fix, two hard gates at the top of the heal: `if (ff9.IsEventWorldMap) return false` (the engine's own {9001,9004,9006,9012} list) + a rig scan (`actf & (actEye|actAim)` on any active cid-4 → a script-owned camera → never heal; covers CUSTOM scene worlds). The s61 index gate stays as the free-roam belt; its falsified census claim is corrected in-comment. Also: both heal log lines moved to `Log.Message/Log.Error` (the s63 lesson — plain Debug.Log never reaches Memoria.log, which is exactly how the heal escaped the earlier "never logged" exoneration). |
| `s63-world-scene-probe.patch` | **REMOVED 2026-07-27 after the confirm round — did its job in exactly two rounds (round 1 taught the Log.Message channel, round 2 caught the heal live); patch kept as reusable history (★ was sha `91de1f5cf6a0f0ea…` both arches (v2: emit via Memoria.Prime Log.Message -- plain Debug.Log NEVER reaches Memoria.log, a fact that also weakens any earlier `no heal lines = heal never ran` reading); pre-probe s62 DLLs archived `backups/dll-pre-s63probe.20260727-175241`).** Diagnostic only, zero behavior change: `ff9.Ff9mkWorldSceneProbe()` at the end of `w_frameUpdateEvent` dumps, in the four cutscene worlds (9001/9004/9006/9012), a burst on frames 0-20 then 1 dump/60: director `Map.Byte[26]` + cloud counter, EYE/AIM designation (`GetEventEye/GetEventAim`), `controlUID`, transport `gEventGlobal[190]`, SC, `w_moveActorPtr` identity (DUMMY?), scripted camera eye/aim, RENDERED camera position, and every cid-4 actor (uid/sid/index/model/actf E-A flags/pos/wmActor pos). Try/catch-sealed so the instrument cannot perturb the frame. Authored AFTER the F2 static sweep closed every offline lead: stock WORLD01 fully decoded (the 0xB7/0xB8 EYE/AIM camera rig -- entries 1/2, invisible, default index 0; the ship uid 3 idx 15 sets Byte[26]=1 at waypoint 8; exit = white FadeFilter + SYSVAR[200] wait + Field(503)), fork 6125 byte-diffed clean vs stock 503 (only 3 Field remaps; WorldMap(9001) intact), the post-s62 run's Memoria.log shows ZERO exceptions, and controlUID resets to 0 on every map init (EventEngine.cs:628) so stock ALSO enters with no control -- the rig IS the camera. One probe round discriminates: rig-not-designated / ship-not-advancing / camera-elsewhere / exit-wait hang. |
| `s61-selfheal-scene-world-gate.patch` | **THE SCENE-WORLD HEAL GATE (authored 2026-07-27; the F2 9001 regression).** `w_worldSelfHealControl` (the 2026-07-04 save-brick recovery) fired in SCRIPTED world scenes (9001 cargo-ship flight / 9004 / 9006), which legitimately run with NO controlled actor: it hijacked control onto a scene actor via `SetControlToLeader`, sky-cast the whole airborne cast to the ground (`w_movementChrInitSlice`), and bound `w_moveActorPtr` against the script's own RunWorldCode(6-10) camera -- the observed 'cargo ship circling at South Gate under a white sky, title thrashing' at the fork's first 9001 crossing (the June-18 run predated the heal; every DLL backup on disk postdates it, which is why the backup bisect could not clear it). Fix: heal ONLY when a player-class actor exists (any active cid-4 obj with actor index <= 2 -- every free-roam dispatcher 00/02/03/05/07/08/09/10/11 (+12) inits index 1/2; the scene worlds 01/04/06 never do -- dispatcher census in the F2 record). Also correctly no-ops the ~ 'Rebuild player actor' button in scene worlds. One hunk in `ff9.cs` (the gate before `SetControlToLeader`); the stored patch file is cut from the stack diff (regenerate standalone if rebasing). ★ BUILT + DEPLOYED 2026-07-27 (0 errors; Output == both arches, sha256 `af979e2142914849…`). |
| `s60-encounter-table-hole.patch` | **THE ENCOUNTER TABLE HOLE (★ BUILT + DEPLOYED 2026-07-26, owner-approved; Output == both deployed arches, sha256 `79935c1bfdbaafcf…`; build lesson: from Git Bash use DASH-style MSBuild switches — MSYS path-converts `/t:`/`/m` into garbage, which is also why the owner's manual run failed).** 2 files, 2 functional lines: `ff9.w_worldGetBattleScenePtr` returns **null** on a (zone, topograph, fog) lookup miss instead of stock's slice-LAST-record fallthrough (ff9.cs:9209 — the line that made an authored table hole impossible: unlisted terrain rolled the zone's final row, found in-game when area-14-stamped kit grass still fought zone 6's brush set); `EventEngine.SelectScene` returns 0 on null (= no battle, the contract ProcessEncount already honors). The engine half of THE TABLE IS THE LAW (the kit's SAFE-ROAD area-14 stamp is the data half, southern-ring REVERT.md §26). Stock blast radius censused BEFORE authoring: exactly ONE spot map-wide exercises the fallthrough — 22 stray area-10 topo-0 tiles in blocks (20-22,14) (plains slivers inside the zone-4 mountain region, today fighting rec 43's brush set there) — which become encounter-silent; every other walkable stock triple has a real record. Pre-build full DLL backup `20260726-162739` (`py tools/restore_memoria_dll.py 20260726-162739`). |

| `s80-debug-warp-timer-teardown.patch` | **THE DEBUG-WARP COUNTDOWN TEARDOWN (★ BUILT + DEPLOYED 2026-07-28 as s69 on its authoring branch; RENUMBERED s80 at the 2026-08-14 master cherry-pick — s69 was since taken by minimap-visible-state. ⚠ NOT in the shipped bundle `dwix-custom-memoria-1.0.0b19.zip` — verified 2026-08-26: its `Assembly-CSharp.dll` (sha256 `A03A0050CE6F793B…`) has no `KillCountdown`, so a bundle user's ~ debug warp still leaks a countdown. The live dev DLL DOES have it (built 2026-08-02 from the shared clone, sha256 `2C03884D049B2B82…`, both arches matched) — the earlier "absent from the live DLL" note here was wrong. THE TWO ARTIFACTS DIVERGE IN BOTH DIRECTIONS: the bundle carries s79 (safe-fallback spawn, authored 2026-08-06) which the 08-02 live DLL predates and lacks, while the live DLL carries s80 which the bundle lacks. Neither was the full stack. ✅ RESOLVED for the live engine by the **2026-08-26 rebuild** (`build_memoria.py --label pre-s80`, 0 errors, Output == both arches, all 3 DLLs; new `Assembly-CSharp.dll` sha256 `10BE2052818ADB41…`, x64 == x86; pre-build backup `20260826-140108`, revert `py tools/restore_memoria_dll.py 20260826-140108`) — the FIRST artifact carrying the complete s22–s80 stack, `KillCountdown` + s79's `(1092, −788)` both verified in the deployed metadata. ★ **PLAYTEST-CONFIRMED 2026-08-26** — first in-game proof of this patch (the 2026-07-28 build was only ever metadata-verified): bench **30416** (the bundled `examples/siege` field, `timer = 60`, deployed to a free scratch id after the original 30415/30417 siege benches were retired) → clock running → `~` warp out → owner verdict "gone". The leak is closed on the live engine. ⚠ **OPEN RELEASE OBLIGATION (owner call 2026-08-26: fold into the NEXT release cut, no engine-only bundle now).** The shipped `dwix-custom-memoria-1.0.0b19.zip` still lacks s80, so a bundle user's ~ warp keeps leaking the countdown. The next bundle MUST be built from an engine carrying s80, and its `INSTALL.txt` patch-range list needs s80 added — that list currently enumerates s79 but stops short, so a copy-paste re-cut would ship the fix while documenting it as absent; 0 errors, Output == both arches sha256 `BFD83F2118F6633E…`; `KillCountdown` verified in the deployed metadata; pre-build DLL backup `20260728-190920`; reverse `git apply -R --check` clean on live. Built while FF9 ran — the running session keeps its in-memory engine, next launch loads the rebuilt stack.)** The fort-condor siege leak: a field's `.eb`-armed countdown (`ShowTimer`/`RunTimer`) writes the SAVE-PERSISTED mirrors `_ff9.timerDisplay/timerControl`, and `EventEngine.StartEvents` re-stamps `TimerUI` from them on EVERY map load — deliberately cross-field in stock (the Festival of the Hunt spans Lindblum), so nothing but an explicit `ShowTimer(0)`/`RunTimer(0)` ever clears it. A ~ debug warp bypasses the field's own scripted exits, so a bench clock rode onto the overworld and every later field (owner repro: siege bench 30415/30417 → ~ warp → "0:11, 0:10…" on the world map). Fix: new `KillCountdown()` in `Ff9mkDebugMenu.cs` (mirrors the `.eb` disarm pair exactly as `DoEventCode` applies it, try/caught so a half-initialized TimerUI can never break the warp), called from `ServicePendingWarp` (the field→field warp AND Reload — the destination's Main_Init re-arms a fresh clock, preserving the documented "~ Reload = clock reset") and `WorldWarp` (overworld→field, so a countdown already leaked into an old save clears on the first debug hop). One file. The kit half of the fix (every COMPILED exit of a `[behavior] timer` field disarms before its transition — gateways/worldmap/ATE/choice/ferry/ladder/elevator) is pure `.eb`, runs on stock, and ships in `build_script` (THE COUNTDOWN EXIT LAW, `tests/test_timer_teardown.py`). |
| `s81-journal-menu-m0.patch` | **THE COMPLETION-JOURNAL MENU, rung M0 (★ BUILT + DEPLOYED 2026-08-26, `build_memoria.py --label s81-journal-m0`, 0 errors, Output == both arches; pre-build backup `20260826-152511`; UNPLAYTESTED until the owner opens it). The Journal takes Folklore's main-menu seat** (owner ratification 2026-08-26: Folklore HIDDEN entirely; tabs STORY/SIDE QUESTS/RECORDS; unreached sections "????"; merged story tab -- design + rungs M0-M5: `studies/completion-journal/MENU-DESIGN.md`). New `Global/JournalUI.cs`: an M0 skeleton UIScene (background + fade clones, one GenericInfoPanel framed pane captioned Journal, cancel returns via the ItemUI StartSubmenuTweenIn idiom; key routing needs no armed group -- UIKeyTrigger dispatches OnKeyCancel by UIState) + `JournalConfig` (`Memoria.ini [Journal] Enabled`, DEFAULT ON -- the off switch also restores the s45 Folklore row: MainMenuUI's row mint reads both gates, `JournalConfig.Enabled ? "Journal" : "Folklore"`, and the descend picks `UIState.Journal`/`UIState.Folklore` accordingly -- one ini flip re-exposes Folklore whole). UIManager: `UIState.Journal` APPENDED after Folklore (the serialized-ordinal law), `[NonSerialized] JournalScene` built at runtime in `Start()` on the UI layer (the s45 invisible-shell find), the hand-maintained `OnLevelWasLoaded` sweep line, `TryGetSceneFromState` case, `IsUIStateMenu` widened. csproj include seated in the quiet `Global\Dialog` neighborhood (NOT beside s45's -- the adjacency law). Gates: reverse `patch -R -p1 -F0 --binary` clean on live; forward `-F0 --binary` clean on a fresh base+stack replay. NEXT RUNGS: M1 STORY tab (sections list + NOW banner off `JournalPatch.txt`, the s45 FolklorePatch.txt asset-stack idiom -- the DLL never bakes catalog content), M2 live marks, M3 side quests, M4 records, M5 missables. |
| `s82-journal-menu-m1-story.patch` | **THE JOURNAL MENU, rung M1 -- THE STORY TAB (★ BUILT + DEPLOYED 2026-08-26, `build_memoria.py --label s82-journal-m1`, 0 errors, Output == both arches; pre-build backup `20260826-154024`; UNPLAYTESTED). M0's placeholder pane becomes the STORY tab:** the NOW banner (a wide framed pane, caption "Now", body = the current main-story objective -- `FF9StateSystem.EventState.ScenarioCounter` bucketed over the spine's section anchors, the SAME ladder the kit's offline `journal report` and the bench `.eb` expression compute, all generated from ONE catalog) + the scrollable 44-row section list (the s45 KeyItemListPanel recipe verbatim: PRESERVE-THE-BAKE capture, ChangeDims reshape, the scroll-sum fixpoint, the row-0 template seat, the scene-level pointer rect, MuteActiveSound insurance; header rail reads STORY). Rows honor the ratified spoiler policy: index <= the ladder index renders the title (current row YELLOW), past it renders "????" gray. **Data = `JournalRegistry` parsing the mod-shipped `JournalPatch.txt`** (TAB-separated records via `Split` -- ⚠ NOT JSON: the game's Unity-5.2 Mono profile throws `TypeLoadException` the moment a method referencing the Managed Newtonsoft-13 types is JITted, measured in-game 2026-08-26 in Memoria.log; the first M1 cut shipped JSON and blanked the whole screen. `TryFindAssetInModOnDisc` low->high, whole-document replace, per-folder isolation; each Show refresh is exception-isolated so one dead panel can never blank the other again. The NOW banner is seated at (-260,400,980x200), clear of the collapsed title plate's baked top-right region) -- emitted by the kit at every deploy from `data/journal_catalog.toml` (`journalcatalog.render_patch` + `build._emit_journal_patch` + the deploy_field copy lane), so THE DLL NEVER BAKES CATALOG CONTENT and catalog authoring iterates with zero engine rebuilds. **M2 IS IN THE SAME PATCH (rebuilt `--label s82-journal-m2`, backup `20260826-162210`): the section-detail CHECKLIST** -- a second KeyItemListPanel clone at the right seat (column center +385), hidden until a REACHED section with authored entries is hovered (OnItemSelect re-aims it; an unreached or empty section hides the pane -- an empty sheet reads as a bug). Rows = live gEventGlobal latch marks (`OK  `/`--  ` + LAW-5 runtime names via `ETb.GetItemName`; gil literal; collected white / uncollected gray); the header rail = section title + N/M tally; the locator prose rides the stock help bubble (`HelpDetail.Text`). Confirm on a section DESCENDS the group ladder into the checklist (parent held secondary -- this is what makes CLICKING a section do something); Cancel walks back up one tier; an unreached section refuses with the stock buzz. The registry parses E records (18 columns) alongside S; entries travel with their sections on the whole-document replace. Gates: reverse `-R -F0 --binary` clean on live; forward `-F0 --binary` clean on a fresh base+stack replay (s63 skipped -- retired history). One file: `Global/JournalUI.cs`. **M3 (`--label s82-journal-m3`, owner-confirmed 'looks good'): the SIDE QUESTS tab** -- L1/R1 page the tabs (SFX 1047, caption rail renames, MuteActiveSound across the rebuild); the 11 side arcs listed by first availability, SC-gated ????; the detail pane renders COUNTER rows from the kit's C records (the DLL implements five primitive reads -- byte/bit/bitcount/field/sysvar -- the kit names every offset). **M4 (`--label s82-journal-m4`): the RECORDS tab** -- three always-open groups (Tetra Master / Party / Combat & Meta) through the same pane via rec.* C records whose kinds call the ENGINE'S OWN statics (QuadMistDatabase.MiniGame_Get*, party.gil, rare_item_obtained.Count, Settings.time h/m, FieldEntrance u16). **M5 (`--label s82-journal-m5`, backup 20260901-203940, UNPLAYTESTED): the missable column + polish** -- LAW 4 rendered (a closed-window uncollected row wears FF9TextTool.Red and its help bubble leads with the verdict: 'Window likely closed' at derived confidence, 'PERMANENTLY MISSED' only at owner confidence, nothing otherwise -- `JournalUI.MissableVerdict` mirrors `journalcatalog.missable_verdict`, E-record cols 13/14); a reached section row shows its own N/M tally; L1/R1 glyphs (FF9UIDataTool.ButtonGameObject, the live binding) flank the tab name on the caption rail. **M5 fix 1 (`--label s82-journal-m5-fix1`): the red rows + verdict bubble OWNER-CONFIRMED; the L1/R1 glyph hint REMOVED (rendered as empty boxes over the list; ruled non-stock), and section-row labels shrink-to-fit so the tally is never clipped. Fix 2 (`--label s82-journal-m5-fix2`): fix 1 widened the non-Left-pivoted donor label in place and spilled text past the left margin -- the label is now re-anchored Left at its BAKED left edge before widening. Fix 3 (`--label s82-journal-m5-fix3`): still spilling -- the row tally is REMOVED (owner: no counter on the selector list) and the donor label is left exactly as baked; the section list is the M4 state again.** |
| `s83-debug-flag-batch-keyitem.patch` | **THE FLAG-BATCH KEY-ITEM OP -- `k<i>` (★ BUILT + DEPLOYED 2026-08-27, `build_memoria.py --label s83-keyitem-batch`, 0 errors, Output == both arches; pre-build backup `20260827-233011`; AWAITING PLAYTEST). One file, `Ff9mkDebugMenu.cs`.** The ~ menu's Flags-tab BATCH grammar gains a fourth op kind: `k9` grants key item 9, `!k9` (or `k9=0`) removes it, by IMPORTANT id (8 = Continental Map, 9 = World Map; == generic item id 256+i), applied via `ff9item.FF9Item_AddImportant`/`RemoveImportant` -- the exact state the event `ITEMADD` (0x48) grant path writes (`rare_item_obtained`, a HashSet on the save), so the grant persists and every `B_HAVE_ITEM` gate sees it. Key-item ops skip the gEventGlobal bounds pass (parse caps the id at 0-255); the all-or-nothing contract holds. Plus ONE code-side built-in preset row above the saved ones -- `* World map key items (k8 k9)` -- so a scratch-bench save can open the Select large-map view without an offline `ff9mapkit keyitem` save edit (the 2026-08-27 seam-walk playtest gap: the bench save lacked the World Map key item, blocking the large-map torus check). Grammar hint + the parse-error op list updated to name `k<i>`. Gates: forward GNU `patch -p1 -F0 --binary` onto the pre-edit snapshot reproduces the live file byte-exactly; reverse `-R -F0` dry-run clean on live; new string literals probed present in the deployed x64 DLL. |

## The AUTOMATED TEST HARNESS — NEW capability

| File | What it adds |
|---|---|
| `s83-harness-agent.patch` | **THE IN-GAME TEST HARNESS — the agent half of an automated play/verify loop (★ DEPLOYED + PLAYTEST-PROVEN 2026-08-27: an injected 120-frame `hold up` walked Zidane 1014 units across bench 30801, owner-witnessed and screenshot-confirmed; the driver half is `tools/harness/` + `tools/play.py`, guide in `ff9mapkit/docs/TEST_HARNESS.md`).** Lifts the project's oldest hard constraint — *the agent cannot play the game*. Before this, a change could be built, deployed and screenshotted (`tools/game_snap.ps1`) but never *exercised*: no button could be pressed, the character's position was unknown, and on-screen dialogue was unreadable, so every behavioural claim cost a human playtest. New `Memoria/Harness/HarnessAgent.cs` (+ csproj include): a `DontDestroyOnLoad` MonoBehaviour bootstrapped once per frame from `UIKeyTrigger.Update` (alive from the TITLE screen onward, so a scenario can drive New Game, not just an already-loaded field). It reads a line-oriented `ff9harness/req.txt`, publishes `ff9harness/state.json` every other frame, appends `events.jsonl`, and captures PNGs into `shots/`. **Input is injected at FIVE points in `HonoInputManager`** — `IsInput`/`IsInputDown`/`IsInputUp` (the choke point every consumer reads: field, world, battle, menus), plus `GetAxis` and `CheckPersistentDirectionInput` — so the harness needs **no window focus**, does not steal the keyboard, and OR-s with real input so a human can drive the same session by hand. **DORMANT BY DEFAULT and that is load-bearing, not politeness:** with no `ff9harness/arm` file `HarnessAgent.Active` is a false static, the three hooks early-out on one predictable branch, nothing is written, and behaviour is identical to an unpatched engine — which is what makes it safe on an install shared by ~26 concurrent worktrees. **Five design points that are the difference between a harness and a flake generator.** (1) **Absolute-frame scheduling**: Unity does not order component Updates, so a press scheduled for "now" could be sampled by `HonoInputManager` either side of our own Update and be seen once, twice or never; every schedule is an absolute `Time.frameCount` decided at least one frame ahead, so all consumers in a frame agree regardless of order. (2) **The ack is latched on PERSISTENT state, not a frame-local "was the queue busy"** — when the final step blocks (press, wait, warp, shot: almost all of them) the queue empties one frame before the block elapses and a frame-local flag is already false, losing the ack forever and hanging every command until the driver's timeout. Caught by the offline stand-in before it ever cost a game launch. (3) **`WriteAtomic` swallows the sharing violation instead of retrying** — Windows fails the replace whenever the driver has `state.json` open, a real collision at 30 publishes/sec against a 30 Hz poll; spinning to win a file race inside the frame loop would stutter the very game being measured, and skipping is free because the next publish is two frames away and the driver retries torn reads. (4) **Warping is NOT reimplemented** — `Ff9mkDebugMenu` gains three thin `Harness*` adapters that push arguments into the same text boxes a human types into and call the same private methods the buttons call, so there is exactly one implementation of the proven fade-then-change-map sequence (`ServicePendingWarp` runs from `Update()` whether or not the menu is open, so a harness warp completes with no window ever shown). (5) **THE KEY BOOLEANS ARE NOT ENOUGH — the harness must drive the ANALOG AXIS too, and this cost the arc's only failed playtest round.** With `[AnalogControl] Enabled=1` (the shipped default) `FieldMapActorController.MovePC` reads `analogVector = GetAxis()` and ends with `if (analogVector.magnitude <= stickThreshold) actualMoveVec = Vector3.zero;`. `GetAxis()` returns **Unity's own** Horizontal/Vertical axes, which a physical keyboard feeds and an injected press does not — so with only the three `IsInput` hooks the axis stayed `(0,0)` and the move vector was zeroed *downstream of every signal a diagnostic could reach*: `movingUp` true, `ccSMoveKey` true, `GetUserControl()` true, `dash_inh` 0, and the actor even **rotated** (that block is gated on `movingUp`, not on the move vector). Six benches all read as "not walkable" and the harness looked correct at every probe point. `HarnessAgent.TryGetAxis` now synthesises a unit vector from the held directions (falling through to the real axis when the harness holds nothing, so a human still drives an idle session), and `CheckPersistentDirectionInput` — the engine's raw `GetAsyncKeyState` "is a direction key physically down" probe — answers truthfully for harness-held directions so injected input reads as a **keyboard, not a stick**. That second hook is not cosmetic: `FieldMapActorController` derives `isStickMovement` from it, and with `UseAbsoluteOrientation=3` (walkpath for keys, absolute for sticks) a scenario would otherwise walk a *different direction* than the same press does by hand. **The general law: hooking the accessor a diagnostic reads proves nothing about the value that decides the outcome.** Also adds `TitleUI.HarnessStartNewGame` — a scenario could blind-press Confirm on the title, but the highlighted entry depends on whether a save exists, so the same script would start a new game on one machine and resume someone's save on another. Every state read is individually try/caught and degrades to `null`: the file is sampled at the title, mid-transition and mid-battle, where half these singletons do not exist. Five files touched (`HonoInputManager.cs`, `UIKeyTrigger.cs`, `Ff9mkDebugMenu.cs`, `TitleUI.cs`, the csproj) plus one new. <br><br>**★★★ REVISION 4 (2026-08-31, protocol `v` 3 -> 4) — THE BATTLE IS PLAYABLE, not merely visible (★ `scenarios/battle_play.py` 9/9 and `scenarios/flee_check.py` 4/4 in-game, both core-suite members).** rev 3 could SEE a fight and not take one turn in it. Two obstacles were named then; **one of them was wrong and the correction is the more valuable half.** ❌ NOT an ATB deadlock: the claim was that `btl_sys.CheckEscape` needs `IsNativeEnableAtb()` and WAIT mode freezes the gauge while a menu is open, but that method returns `CurrentPlayerIndex == -1 || ActiveGroup == "Battle.Command" || ActiveGroup == ""` — the TOP-LEVEL command list, which is exactly where the trace found the cursor, leaves it ENABLED (only a SUBMENU freezes it). The gate was open the whole time. ★ **THE REAL CAUSE WAS A ONE-FRAME HOLE IN THE HARNESS'S OWN HOLD**: `Schedule` set `_downFrame = frameCount + 1`, so re-issuing a hold un-pressed the button for the frame the request landed on — invisible to anything SAMPLING it, fatal to anything counting CONTINUOUS held time. `BattleHUD._runCounter` is exactly that and gates the escape roll at 1.0 UNBROKEN seconds, so a flee re-issued every 0.8s NEVER ROLLED ONCE while `btl_escape_key` (set before the counter is tested) kept the character running on screen — owner-observed as "the flee animation for a couple of seconds but he didn't actually leave". New `Extend` lengthens a live hold instead of restarting it. ★ The OTHER obstacle was real and dissolved by the UI's own teardown: `SendNetCommand` opens `if (playerIndex == CurrentPlayerIndex) return false;` (it replays a REMOTE co-op command and must lose a race against the local menu), and **`SetIdle()` is public** — what `OnTargetConfirm` calls the instant a command is sent — so calling it FIRST hands the slot back; the menu had to close anyway. ⚠ **THE STALE TURN, found by the SUITE and invisible to two green standalone runs:** `CurrentPlayerIndex`/`ReadyQueue`/`InputFinishList` are reset by `InitialBattle()`, which runs LATER than `IsBattleScene()` goes true — so through the opening camera of the SECOND battle in a session they hold the PREVIOUS fight's contents and `turn.slot` publishes a plausible "your move" for a battle asking nobody. `NetMenusReady` cannot catch it either (it is a ContainsKey, and InitialBattle clears the dict's VALUES not its KEYS). A command injected there FROZE the fight: no HUD, no ATB, the intro camera held four minutes. Gated now on **`FF9BMenu_IsEnable()`** — `turn.slot`/`ready`/`done` publish through it with `slot_raw` kept for diagnosis, and `battlecmd` + `menus` refuse a battle that is not asking. New: **`menus <slot>`** (the co-op mirror `CollectNetMenus`, published as `battle.menu` — commands / abilities / items with names, MP, enabled and the exact `sub` argument; ⚠ a VERB not a publish field, because collecting it WRITES the HUD's ability cache and an instrument may not mutate what it observes; stamped with slot+epoch so a stale read is visible), **`battle.escaping`** (`cmd_status` bit 0 — the roll actually WON, as opposed to `escape_held`, which only means the bumpers are down; between them is a dice roll of `200 / avgEnemyLevel * avgPlayerLevel / 16` percent per second), and `DescribeSlotRefusal` naming which of SendNetCommand's three tests said no. ★ **THE WHOLE REVISION TOUCHES ONE FILE**: every seam it needed — `SetIdle`, `SendNetCommand`, `CollectNetMenus`, `NetCommandForMenu`, `NetFirstAbilitySub`, `NetMenusReady`, `FF9BMenu_IsEnable` — was already public, most of it built for co-op in s37. No hunk lands in `BattleHUD.*`, so s83 and s37 never meet.<br><br>**★★ REVISION 3 (2026-08-31, protocol `v` 2 -> 3) — THE BATTLE BLOCK, the pillar the state channel was 100% dark on (★ 14/14 in-game, `scenarios/battle_check.py`, a core-suite member).** Deferred out of rev 2 on a stated precondition — *adding it without extending the stand-in reproduces the menu lane's defect at ten times the surface* — now met. Each field is written around the trap it carries, because almost everything in `FF9Battle` is AMBIGUOUS or STALE rather than absent, which makes a plausible WRONG answer the default failure mode: **`epoch`** (`party.battle_no`, the ONLY unambiguous battle-started edge; monotonic and save-persistent, so only deltas mean anything); **`result`** (⚠ 0 both DURING a battle and BEFORE any has ever run, and it persists afterwards — meaningless without the epoch); **`active` + `debug`** (⚠ `IsBattleScene()` is true for the DIORAMA too, which runs under `isDebug` where the auto-end is suppressed and the fight CAN NEVER END, so a result assertion there is green having observed nothing); a **unit roster** with HP published TWICE — logical `hp` and raw `hp_raw`, because `CurrentHp` routes through `btl_para.GetLogicalHP`, which subtracts 10000 for a `FLG_NON_DYING_BOSS` enemy under `CustomBattleFlagsMeaning=1`, so the HUD shows one value while the AI's B_MEMBER (36)/(35) reads the other; **`alive`** as the Death STATUS BIT rather than `hp == 0` (a DeathChanger unit sits at 0 HP alive, and that is the HUD's own liveness test); **`name` + `name_raw`**, the same rendered-vs-source split the dialogue needed, found again when an enemy came back as `[STRT=27,1]Fang[ENDN]`; plus ATB, MP, level, targetable, status, the command cursor by GROUP and BUTTON, `scene_info.runaway`, the battle message and the rewards. ⚠ **`phase`/`units`/`turn` are MID-BATTLE ONLY** — the engine leaves them holding the previous fight's contents, so publishing them on a field would hand a scenario a complete, plausible, entirely historical battle. ⚠ **`BattleUnit.GetIndex()` IS NEVER CALLED**: it is `while (1 << index != Data.btl_id) ++index;` with no bound, so a `btl_id` of 0 spins forever inside the state publisher and hangs the very game the harness exists to observe — the slot is computed with a bounded loop instead. Verbs: **`battle <sceneId> [group]`**, which is the field-warp adapter with `nextMode = 2`, since `SetBattleScene` (what the `.eb` ENCOUNT opcodes call) and the public `SetNextMap` are the same method (`FF9ChangeMap`) — so there is no second copy of a proven transition; and **`battlecmd`** onto `BattleHUD.SendNetCommand`. ⚠ Deliberately NOT `NetSyncDiorama.Boot`, which is cheaper and sets `isDebug`. **DRIVING a battle to a RESULT was left OPEN here and is CLOSED in rev 4** — see that row. ⚠ The two obstacles recorded in this row were half wrong: the ATB-deadlock reading of `IsNativeEnableAtb()` is FALSE (it returns true for the top-level `Battle.Command` group, which is where the cursor was), and the real cause was a one-frame hole in the harness's own re-issued hold. The `SendNetCommand` half was right, and `SetIdle()` dissolves it. At this revision the harness could SEE a battle in full and not play one.<br><br>**★★ REVISION 2 (2026-08-31, protocol `v` 1 -> 2) — BUILT, DEPLOYED AND PROVEN IN-GAME (49/49 checks across ten scenarios; sha `217410de06c089ca…`, Output == both arches, 0 errors; pre-build backups `20260831-144736` and `20260831-150435`). A DEFENSIVE BATCH: eleven changes, nine of which stop the agent LYING, corrupting its own channel, or acking a step it did not perform.** From an adversarial audit of both halves (51 confirmed defects after a refutation pass). **E11 THE SAVE SANDBOX is the one that matters most and it is MEASURED, not suspected:** `scenarios/save_untouched.py` ran against a live game and an ordinary `newgame(); warp()` — the opening EVERY scenario shares — changed both live save containers, because `EventEngine` autosaves on field entry and `Configuration.SaveFile.DisableAutoSave` is 0 here. Manual slots survived; the autosave did not. While armed, `SharedDataBytesStorage.MetaData.DirPath`/`.FilePath` now point into `ff9harness/save/` (redirecting `FilePath` moves BOTH containers — the Memoria sidecar path derives from it), restored on disarm and on a fault, retried every frame until it takes (the arm transition can land before the save statics resolve), and **published as `save_path`/`save_sandboxed` so the driver VERIFIES the redirect instead of trusting it** — a sandbox nobody checked is a check that cannot fail. **E3 no partially built section can reach `state.json`:** `-1 >> 3` is `-1` in C# too, so a negative watch bit passed the old shifted bound test and threw on the array read with its key already in the shared buffer; the catch did not roll back, so every later document was invalid JSON — which the driver reads as *the agent never published*, i.e. *this engine predates s83*, about a perfectly healthy game, and a re-arm did not clear it. Each section now builds into its own `StringBuilder` and is appended only on success, and the bound is tested on `n`. **E4 `texts` is now the RENDERED dialogue** (`CurrentParser.ParsedText`), with the source kept alongside as `phrase_raw`: `Dialog.Phrase` is `InitialText` — raw sub-page source, every `[STRT=…]`/`[TIME=…]` tag intact, before variable substitution — so `expect_text` was matching a string the player never sees. **E5 the choice publishes `active`/`disabled`:** a script-disabled line is physically REMOVED from the parsed text while `SelectChoice` still counts the ABSOLUTE list, so `options()[i]` was not the option `select(i)` lands on and a story scenario could confirm one branch while reporting green for another. Nothing outside the engine can reconstruct that mapping. **E6 refusals throw:** `HarnessWorldWarp`/`HarnessWorldTeleport` (and `WorldWarp`/`WorldTeleportTo`) return `Boolean`, and `HarnessStatus` — which existed and was read by nothing — is published as `debug_status`; seven refusal paths used to ack CLEAN while doing nothing, after which the driver timed out blaming a destination the engine never attempted. **E1 the error latch is per-REQUEST** and stamped with `error_seq` (it was cleared only on re-arm, so one bad warp made every later healthy step raise on a stale message — a sweep reported every remaining field as unreachable). **E2 `release` cannot press:** it wrote only `_upFrame`, and `ClearAllButtons` leaves `_downFrame` at `-1`, so `f >= -1 && f < f+1` made a defensive release inject a one-frame phantom press and a phantom cursor step. **E7 `timescale` is clamped to 0.1–8 and restored on disarm/quit:** at 0 the engine runs zero LOGICAL ticks while `Block`/`IsHeld`/the ack keep advancing on render frames, so every step acks against a game that executed nothing — and nothing restored the scale, leaving the SHARED install frozen after a crashed run. **E8 disarm and fault are observable and TERMINAL:** `armed` is published, the disarm branch emits a final document saying so (otherwise *disarmed*, *hung* and *slow* are indistinguishable), and `Update`'s catch now latches `_faulted` — before, a fault disarmed, dropped the in-flight request, released every held button mid-action, then silently re-armed 30 frames later having wiped the error that explained it. **E9 the `reset` verb** — the isolation primitive: `hold` is non-blocking and frame-counted, so a scenario that raises mid-hold left a button DOWN into the next one, and buttons were only cleared on an arm transition. **E10 publishes the menu `group` and `ButtonGroupState.ActiveButton`:** `ui_state` does not move for a menu SUB-screen, so nothing published could verify a menu was entered or LEFT — and `UIKeyTrigger` reads `ActiveButton` first while the harness was publishing the `UICamera.selectedObject` fallback. **Deliberately LEFT OUT:** the whole battle state block and a `battle <sceneId>` verb (ten times the surface, and two crash-class reads sit in the obvious implementation), a `noencounter` booster, and driving `Block` off a logical-tick counter. New in-game acceptance: `scenarios/rev2_proof.py`, every check on an OUTCOME (the character did not move, the channel is still parseable, the position is unchanged) because *the step acked* is what all of these defects could already fake. **E12 `Application.Quit()` DOES NOT QUIT THIS GAME — found by running it, not by reading it.** Every harness run ended `quit did not land -- terminating`: the step was accepted, executed and ACKED at frame 842, and the agent was still publishing 900 frames later. `UIKeyTrigger.OnApplicationQuit` calls `Application.CancelQuit()` unless its private `quitConfirm` is set, and pops the in-game "really quit?" dialog instead — which nobody answers in an unattended run. The agent now calls the game's own `UIKeyTrigger.ConfirmQuit()` (sets the flag, broadcasts `"OnQuit"`, then quits through the branch that actually closes the window), falling back to a bare `Quit()` where there is no `UIKeyTrigger` to cancel it. **A run is 23s instead of 38s** and the harness no longer hard-kills the process it opened. Worth keeping: the driver's message was ACCURATE the whole time and nobody read it. **In-game proof:** `scenarios/save_untouched.py` ★ RED → GREEN (all 18 containers byte-identical after a full `newgame(); warp(); walk`), `rev2_proof.py` 12/12, `dialogue_render.py` 6/6 — E4 shown on one box in both forms (`rendered '== SCRIBE CONTROL…'` vs `source '[STRT=10,1][TAIL=UPR][NTUR]== SCRIBE CONTROL…'`) and E5 at the exact historical failure point (`option_index("Tetra Master") -> 3`, the engine's own `SelectChoice -> 3`, the option there reads `Tetra Master`; asking for index 3 against the RAW array reads "Minigames") — plus the pre-existing suite unbroken (walk 2/2, flag 7/7, goto 5/5, menu-pick 6/6, talk 3/3, cutscene 4/4, gateway 3/3). Gates: `git apply -R --check` clean on the live tree, AND forward-apply onto the reconstructed baseline reproduces all 8 files BYTE-EXACTLY — reverse alone is not enough, it once silently passed a patch that had dropped the `WorldWarp` void→Boolean conversion. <br><br>**★ REVISION 5 (2026-09-04, protocol `v` 4 → 5) — THE `netsync` VERBS: the F1–F3 co-op solo benches, run UNATTENDED (★ `scenarios/coop_dialogue_lockstep.py` 40/40 in-game — the first run of F3's benches ever).** The benches were ~ debug-menu buttons — IMGUI, which no injected controller input can press. `netsync selftest 1|0` · `bench 1|0` · `l1 1|0` · `advance` · `choice <i>` · `unmatched` call the SAME static entry points the buttons call (`NetSyncClient.HarnessInjectDialog` → the bench Impl; `Ff9mkDebugMenu.HarnessFrontmostOpenDialog`), every refusal carries the engine's own reason, and a new `netsync` state block publishes the L2 observables (enabled/role/selftest/forced/bench/l1/l1_pinned/l1_forced_control/suppress/align_win/align_text/applied_seq/wait_armed/wait_ms/wait_limit_ms/pending). **`selftest 1` is a PROCESS-LOCAL override (`NetSyncClient.HarnessSelfTest`)**: it sets the statics `LoadConfigIfChanged` would for `Enabled=1 Role=selftest` and creates the object as `Ensure()` would — Memoria.ini is never written (the shared install; a leaked ini edit would hand the next player a ghost); the mtime-gated reload never re-reads an unchanged ini so the override holds; a live host/client session is REFUSED; a genuine ini edit mid-run takes precedence. Released by `ReleaseNetsyncOverride()` on disarm, fault and `reset` (the same teardown a live `Enabled=0` edit takes — proven in-game: `reset` with L1 pinning control hands control back with co-op disabled). `NetSyncClient.cs` joins the patch's file set (the entry points + observables sit between the F3 bench Impl and the F1 teleport section, so this revision applies AFTER `s84`). Two bench laws the run taught: a `[NFOC]`/`[TIME]` window ignores `OnKeyConfirm` by design and a real host never emits for one — never fabricate a frame for it; and a still-typewriting window eats the first Confirm as a fast-forward. Regenerated via `tools/memoria_stack_replay.py`: 19 hunks, 9 sections (the csproj hunk carried verbatim); forward onto the pre-s83 baseline (base→…→s57→s84→…→s83-keyitem) under BOTH `git apply --check` and `patch -p1 -F0 --dry-run`; `git apply -R --check` on live clean; the full replay reproduces every file byte-exactly. Live DLL `a2d69edd057d982f…`, pre-build backup `20260904-163811`. |

## ✅ STACK HEALTH — the stack replays END TO END again (2026-07-16; s43 verified on top 2026-07-20)

> **2026-09-04 — the replay is a TOOL, and the live stack is at ZERO fuzz.** `py tools/memoria_stack_replay.py
> diag` rebuilds every netsync/harness/dialog file from base `6b8bb2d5` through the ordered live stack
> (dead `s12`/`s18`/`s21`/`s59` skipped; `s84` pinned after `s57`) and reports `18 of 18 files byte-exact`.
> Every one of those patches lands under `git apply --binary` with no fuzz at all — the "s22 needs
> `patch -F3`" note below dates from replays that still applied the dead `s21` first. `emit` writes a
> capture with canonical headers, CRLF intact and the DebugMenu BOM matched; `snapshot --stop-after`
> gives the baseline to gate it on. This is how `s84` was captured and `s83` regenerated.

> **★★ b18 FULL-SET REPLAY (2026-08-06) — the FIRST end-to-end replay of the complete live set, 50/50 patches
> at `patch -p1 -F0 -s -f --binary`, zero rejects.** Fresh proof worktree of base `6b8bb2d5` + the live set
> (everything except the retired s12/s18/s21/s35/s59/s63/s67; s71 included — s73 depends on it; the two s48s
> disambiguated by filename, `-debug-vehicle-rows` first) reproduces the live tree **byte-exactly** except:
> (a) the 6 files carrying the deliberately-uncaptured **F3 dialogue-lockstep round** (wire v12 —
> `NetSyncClient/Relay/Socket.cs`, `Dialog.cs`, `Ff9mkDebugMenu.cs`, `UIKeyTrigger.cs`; excluded from b18 on
> purpose, capture it as its own patch when it proves), and (b) `BGSCENE_DEF.cs`, EOL-only (the live copy is
> LF, off-convention — a historical text-mode application; content identical).
> **Twelve patches were REPAIRED to get here** (each gated per file: repaired patch onto the law-1
> reconstructed baseline == its stack-position target, chained byte-exact to live): `s45` (un-corrupted — a
> staging normalizer had `\r`-ified its no-final-newline csproj context line; the pristine file was fine, the
> lesson is in the normalizer note below), `s48-sfx`/`s60` (blank context lines stored with no leading
> space — malformed under strict `patch`), `s52/s53/s58/s76/s77` (pure cascade from s48-sfx, now CRLF-stored),
> `s61` (**regenerated** — the stored hunk was an insertion-only diff of the whole `w_worldSelfHealControl`
> function that could never apply on top of s39), `s62` (**regenerated** — 3 of its 4 hunks re-added s33/s36/s37
> changes it was mis-diffed against), `s64`/`s65` (nonstandard absolute/backslash headers rewritten to `a/ b/`
> form). Every live-set patch is now stored **CRLF-bodied** (matching the checkout convention), so the
> documented `--binary` replay needs no per-file EOL chores. ⚠ NORMALIZER LAW: a CRLF pass over a patch must
> NEVER add `\r` to the line before a `\ No newline at end of file` marker, and a blank context line must stay
> `" "`-prefixed — both classes produced this round's false "malformed patch" failures. Two cosmetic `.orig`s
> remain on replay (`BGSCENE_DEF.cs`, `Ff9mkDebugMenu.cs` — offset-applied hunks; re-base at leisure).

> **2026-07-20 status:** `base + s22..s41 + s43` replays at zero fuzz / zero rejects / zero `.orig`, and the
> s43-owned files (`UIKeyTrigger.cs`, `Ff9mkDebugMenu.cs`) come out byte-identical to live. **s42 is now
> CAPTURED** (`s42-netsync-config-split.patch`, same day): with it in sequence, `NetSyncClient.cs` reproduces
> live byte-exactly (gated via a single-file lineage replay — s36/s37/s40/s41's `NetSyncClient.cs` hunks +
> s42 == live bytes; reverse dry-run `-F0` clean on live). **s44 CAPTURED same day** (the F1 field-co-op
> round, 9 files incl. the new `NetSyncField.cs`): gated via the live-minus-the-change sidestep — forward
> `git apply` onto the pre-round snapshots (`backups/preF1-snapshots.20260720`, taken BEFORE any F1 edit)
> == live bytes on all 9; reverse `-F0` clean on live; forward GNU dry-run clean on the pre-tree.
> (`NetSyncField.cs` was LF→CRLF normalized with the emit, the s40 convention.) No uncaptured netsync
> work remains.
> 
> **s54–s56 CAPTURED 2026-07-23** (the host-silent diorama · the unpair/reconnect guards · the
> blip-grace round; 8 distinct files, `NetSyncClient.cs` split across s55/s56 — the one mixed
> `_ghostLastPairedTick` line hand-separated, every other hunk classifies cleanly by its `s55`/`s56`
> marker). Gated via the baseline-reconstruction path (the law-1 authority): each file’s pre-round
> baseline was rebuilt by replaying its captured prior-patch lineage from base (`s33`+`s41` for
> `BattleSwirl`; `s36`..`s44` for the Netsync files) at ZERO rejects, and each baseline’s diff-to-live
> matched the round’s scope EXACTLY (no unrelated drift on the 8 files). Forward `s54→s55→s56` onto
> the pre-round baselines reproduces all 8 live files byte-for-byte under BOTH `git apply --binary`
> and `patch -p1 -F0 -s -f --binary`; the full reverse cascade (`live −s56 −s55 −s54`) lands back on
> the pre-round baselines at `-F0`; `patch -R --dry-run -F3` vs live is clean for `s54` + `s56`
> (stack-top for their file sets), `s55` reverses mid-stack in the cascade (NetSyncClient is
> last-touched by s56).
>
> **s57 CAPTURED 2026-07-23** (the netsync transition-intent lane + dialogue L1, wire v10→v11;
> 5 files, 18 hunks: `NetSyncSocket.cs` · `NetSyncState.cs` · `NetSyncClient.cs` ·
> `EventEngine.DoEventCode.cs` · `Ff9mkDebugMenu.cs`). Gated via the same law-1 baseline path: each
> file's pre-F2 baseline was rebuilt from base `6b8bb2d5` by replaying its captured lineage NOW
> INCLUDING s54-s56 (Socket `s36/s37/s40/s41/s56` · State `s37/s44` · Client
> `s36/s37/s40/s41/s42/s44/s55/s56` · DoEventCode `s30/s38/s44` · DebugMenu `s22/s40/s43/s44/s48/s54`)
> with `git apply --binary` at ZERO fuzz, and each baseline's diff-to-live matched F2's scope EXACTLY
> (no unrelated drift; the reconstructed DebugMenu baseline blob `08becb4` == s54's own output blob,
> an independent check). EOL fidelity: the live tree is `core.autocrlf=true` (LF blobs, CRLF working
> tree), so the base `DoEventCode` blob was CRLF-matched to the tree and the LF-stored `s48` patch was
> CRLF-normalized on replay — every other lineage patch is already CRLF. Forward `s57` onto the pre-F2
> baselines reproduces all 5 live files sha256-exact under BOTH `git apply --binary` AND
> `patch -p1 -F0 -s -f --binary`; reverse `patch -p1 -R --dry-run -F3` vs live is clean (s57 is
> stack-top for all 5 files; `git apply -R --check` also clean). No `\ No newline` markers, so the
> PRE-MARKER CR RULE is trivially green. No uncaptured netsync work remains.

**`base + s22..s41` applies at ZERO fuzz, ZERO rejects, ZERO `.orig`** under the documented
(s40 emitted + gated 2026-07-16: fresh full replay reproduces the live tree byte-exactly except the
FieldMapActorController exclusion below; reverse dry-run clean. s41 emitted + gated same day via
the live-minus-the-change sidestep: forward `git apply` onto the pre-round snapshots == live bytes
on all 6 files; GNU `patch -R -p1 --dry-run -F0` clean on live; forward dry-run clean on the
pre-tree.) Original round:
`patch -p1 -F0 -s -f --binary`. The whole stack reaches the end — the first time it has.

**THE MODEL IS DECIDED: `s12`/`s18`/`s21` are DEAD — never applied.** (User call, 2026-07-16.) They are
three early drafts of one feature: s18 = "F6 reloads the field" (in `FieldMap.cs`); s21 = the same idea moved
to `UIKeyTrigger.cs` because the first spot missed keypresses, plus F10 = reset flags; **s22 = the finished
tabbed F6 menu that replaced both**. `s12` is an unrelated grab-bag (an overlay texture cache that later
*became* `s35`, a retired New-Game debug shortcut, and the `Attack9999` auto-cheat).

Why the question existed at all: later patches carried instructions to **undo** them (s37 removed s21's F6/F10
block and s12's `Attack9999`), so the stack added code and then took it back out — **the engine is identical
either way**, and the only question was whether the recipe takes the detour. Dead wins on evidence: applying
s12 **collides with s35** (it still contains all 14 of s35's lines byte-for-byte), it would resurrect the
retired New-Game debug, and nothing in s22 removes s18's F6 hotkey (so the raw hotkey and the menu would both
grab F6). The fix was to strip the orphaned removals from s37 — done, and that is what closed the stack.

⚠ **Consequence — do not "restore" s12/s18/s21 casually.** They are kept for the build record ONLY. The live
set no longer contains anything that removes their code, so applying one now injects code nothing takes out.

Two rounds got here (below): round 1 (during `s38`) found the drift; round 2 (2026-07-16) captured every
*stable* gap, corrected three of round 1's diagnoses, and stripped s37.

**REPAIRED — s22 was contaminated.** Its csproj hunk added `Memoria\Netsync\NetSyncClient.cs` and
`NetSyncSocket.cs` — s36's lines, in the F6-debug-menu patch (leaked in by an earlier regen emitted
against a live tree that already had s36). s36 adds them too, so applying s22→s36 collided and **the
live set had never replayed cleanly from base**. The two duplicate lines were removed from s22; the
stack now replays **s22..s36 at ZERO fuzz, zero rejects**. ⚠ Note `s22` *still* carries
`Memoria\World\WorldMeshOverride.cs` (s34's line) — **left in place deliberately: it is the ONLY patch
adding that Compile line**, so removing it without moving it into s34 breaks the build. Misattributed,
load-bearing, flagged.

**REPAIRED — an EOL defect in the live tree.** `EventEngine.DoEventCode.cs` was **LF** while the repo
checks out **CRLF** (`core.autocrlf=true`) — a past text-mode `patch` application rewrote it. Converted
back to CRLF (pure EOL, delta = exactly the line count, zero content change); its diff vs the
reconstructed baseline dropped from a phantom **6943-line rewrite to a real 152/138**. **LAW: apply
these patches with `patch --binary`** — plain text mode silently rewrites CRLF files as LF and seeds
exactly this drift.

**RECAPTURED (2026-07-15, round 2) — the stable drift is now in the stack.** Every file the round-1 note
listed as uncaptured/stale is captured. `base + s22..s36 + s39` replays under the documented
`patch -p1 -F0 -s -f --binary` at **zero fuzz, zero rejects, zero `.orig`**, and the four files below are
**byte-identical to live** (`cmp`):

| File | Was | Now |
|---|---|---|
| `Global/UI/UIKey/Ff9mkDebugMenu.cs` | "live ~2.7 KB smaller, s22 stale" | **s22 regenerated** — real drift was 54 add / 4 del lines |
| `Global/WM/WMWorld/WMWorld.cs` | custom-ocean loose-texture hook, uncaptured | **s34 regenerated** — hook captured |
| `Memoria/World/WorldMeshOverride.cs` | "live ~1.7 KB larger, s34 stale" | **s34 regenerated** — `TryLoadTexture` (35 lines) captured |
| `Global/ff9/ff9.cs` | `w_worldSelfHealControl`, uncaptured | **`s39-world-selfheal-control.patch`** (NEW) — 86 lines / 3 hunks |

Three of round 1's findings were **misdiagnoses, and all three had the same root**:

- **The KB deltas were the EOL trap this very section warns about.** Live's `Ff9mkDebugMenu.cs` was LF:
  2453 lines → 2453 missing CR bytes ≈ the "2.7 KB smaller" (and the sign pointed the wrong way). Same for
  `WorldMeshOverride.cs`. Real content drift was 54/4 and 35/0 lines. **LAW: never size-compare across an
  EOL boundary — normalize first, then diff.**
- **`DataPatchers.cs.orig` was never a leftover to delete.** It is not in the live tree. GNU patch's
  default is `--backup-if-mismatch`, so *any* hunk applying at an offset writes a `.orig` — and s33's
  DataPatchers hunk #2 applied at **offset 17** (authored before s24/s31 added lines above it). The `.orig`
  was **regenerated on every replay**. s33 is now re-based (change-content identical, line numbers only) and
  applies at zero offset, so the artifact is gone at the root.
- **`s24` was never stale.** A first cut diffed `ff9.cs` against `HEAD` (the BASE commit) instead of the
  s24-applied state, pulling s24's own `ForkSiblingField` redirect into the diff. s24 already carries it.
  **LAW: a mid-stack patch must be diffed against ITS stack position, not `HEAD`.**

**The EOL defect is a CLASS, not one file.** Every **untracked** file in the live tree is LF; every tracked
one is CRLF. `autocrlf` normalizes tracked files on checkout but never touches untracked ones — so the files
a patch **creates** are exactly the ones a past text-mode `patch` run left LF. `Ff9mkDebugMenu.cs` and
`WorldMeshOverride.cs` were converted back to CRLF (pure EOL: delta == line count exactly, content
byte-identical). ⚠ **Still LF, deliberately untouched:** all 8 `Memoria/Netsync/*.cs` — they belong to the
deferred s36/s37 regen below. Fix them in that round, not by hand.

**Gate correction.** "Reverse-dry-run against live" is only valid for the **top** of the stack (s38). A
mid-stack patch cannot reverse against live — later patches sit on top of its files. The correct gates for a
mid-stack patch are: forward-applies at its stack position at zero fuzz; its files are `cmp`-identical to
live after a full replay; and forward+reverse returns the tree to its pre-patch state.

**RESOLVED — `s37` now applies.** It had failed on 3 files because it carried **orphaned removals** of
s12/s21 code that a clean replay never adds. Stripped (`SettingsState` −6→−1, `UIKeyTrigger` −34→−0, csproj
−2→−0); everything s37 owns is untouched (all four new-file bodies byte-identical). Proof the removals were
no-ops: with only its real `SettingsState` hunk applied, the file is **already byte-identical to live**. The
regen also shed a **phantom near-total rewrite** of the Netsync files (`NetSyncClient` `+1048/−652` on a 1052-line
file → `+419/−23`): s37 had been diffed against the LF live tree while s36 produces CRLF. Third instance of
that trap. Round 1 never caught any of this — it only ever verified `s22..s36`.

**OPEN — deliberately deferred, not forgotten:**
| Item | Why deferred |
|---|---|
| the `s22`→`s34` csproj line move | `Assembly-CSharp.csproj` is being actively written by the in-flight B3 diorama work. s22 still carries s34's `WorldMeshOverride.cs` Compile line — misattributed, load-bearing, flagged (unchanged from round 1). |
| ~~`SetControlToLeader` / `DebugClearControl` live in **s37**~~ | **RESOLVED 2026-07-16 — moved to `s22`.** They are *called* by s22 (F6 menu) and s39 (self-heal) but were *defined* in the battle-co-op patch. Folded into s22 (the earliest consumer, where `great-williams` also places them); s24/s29/s33/s37 regenerated to rebase their EventEngine.cs line numbers. `base + s22..s41` still reproduces live byte-for-byte (EventEngine.cs `cmp`-identical); a partial stack stopping before s37 now compiles. |
| ~~B3 diorama files~~ | **RESOLVED 2026-07-16 — captured as `s40-netsync-diorama.patch`** (23 files incl. the new `NetSyncDiorama.cs` + the csproj line; both gates pass). |
| ~~the EOL-only `Memoria/Netsync/*.cs`~~ | **RESOLVED 2026-07-16 — all 8 live Netsync files normalized LF→CRLF with the s40 emit** (pure EOL, content byte-identical; the deployed DLL predates the normalization — same IL). |
| ~~`Global/Field/Map/Actor/FieldMapActorController.cs`~~ | **✅ RESOLVED + IN-GAME PROVEN 2026-07-18.** The known regression (live had been reverted **wholesale to stock**, losing all s24/s29 gates — Iifa 1751, Dali 404, Prima Vista 205/Steiner, L.Castle 2150, Treno 900, off-mesh 2504/105/2605, Oeilvert 2207). User green-lit the restore: the s24/s29 sections were applied to the live tree (stock backed up), rebuilt+deployed, and A/B-proven via a verbatim fork of field **105** (softlock severity) in an isolated slot — **softlocked pre-fix, played after**. Identity-safe: base already handled the real ids with raw `fldMapNo`; the gates only wrap them in `EffectiveFieldId(...)` so forks inherit it (vanilla was never affected — which is why it hid). |

**The exact residue** (measured on a full clean replay `base + s22..s39` — every entry attributed, nothing
unexplained):

| Residue | Nature |
|---|---|
| `NetSyncBattle/Party/Relay/Socket/State/Visitor.cs` | **EOL-only** (live LF vs patch CRLF) — content identical |
| the csproj `Diorama` line, `NetSyncClient.cs` (+4), `NetSyncDiorama.cs`, `btl_sys`/`btl_scrp`/`btl_stat`, `BattleActionCode.cs` | in-flight B3 diorama |
| `FieldMapActorController.cs` | user decision (above) |

**Everything else — every file the live set owns — is byte-identical to live.**

Two things this measurement settled: `SettingsState.cs` **converges** (s37's stale removals are no-ops — the
content isn't there to remove, so the end state matches live), and `EventEngine.cs` is **fully s37-owned**
(byte-identical once s37 applies) — verified, not assumed from the filename.

✅ **A misattribution RESOLVED (2026-07-16).** `EventEngine.SetControlToLeader` / `DebugClearControl` were
**defined in s37** but *called* by **s22** (the F6 menu's control-restore) and **s39** (the self-heal) —
dev-tooling and overworld depending on the *battle-co-op* patch for a symbol definition, the same leak class as
s22 carrying s34's csproj line. **Now defined in s22**, the earliest consumer (where `great-williams` also
placed them). Because the definitions move ~39 lines earlier in `EventEngine.cs`, the four other patches that
edit that file between s22 and s37 — **s24, s29, s33, s37** — were regenerated to rebase their hunk line
numbers (change-content byte-identical, only `@@` headers moved; s37 additionally drops the definition hunk).
A fresh `base + s22..s41` replay stays clean and `EventEngine.cs` is `cmp`-identical to live; a partial stack
that stops before s37 now compiles.

Consequence: **a from-patches rebuild now reproduces the live tree** for everything the live set owns. The only
gaps left are the in-flight B3 work, the EOL-only class, and the `FieldMapActorController` decision.
→ `project-ff9-memoria-conflict-forensics`.

⚠⚠ **PRIOR ART — an unmerged branch solved this with an INCOMPATIBLE stack model. Reconcile before merging
either.** `claude/great-williams-4052f0` (commits `48e27cd`, `708b6ff`, `1bdb4b9`, `d0d8e46`, `856f4ea`;
recorded in `project-ff9-memoria-conflict-forensics`) ran its own drift census and captured the SAME two
features — but under the **older stack model where `s12`/`s18`/`s21` are LIVE**, not superseded:

| | `great-williams` (unmerged) | this branch (the s38-era model) |
|---|---|---|
| replay baseline | `s12..s37` — s12/s18/s21 **applied** | `s22..s39` — s12/s18/s21 **superseded, not applied** |
| the self-heal | folded into **s22** (+ `EventEngine`/`FieldMap`) | its own **s39** |
| the texture override | folded into **s34** | folded into **s34** (same call) |
| s18's `FieldMap.cs` hotkey block | s22 carries a **removal** of it | never applied, so nothing to remove |
| live-tree EOL | **accept LF**, keep gates EOL-agnostic | **repair to CRLF**, gate byte-exact (`cmp`) |

**RESOLVED 2026-07-16 — the s22+ model won (user call); `great-williams` is SUPERSEDED.** Measured, not
argued: replaying `great-williams`' own `s12..s37` from pristine base gets to **s35 and then fails s36+s37 on
the csproj** — its s22 still adds the duplicate `NetSyncClient`/`NetSyncSocket` lines (the contamination the
s38 round repaired), which breaks s36's anchor. Its "applies cleanly" note was verified with `git apply`, not
the stack's documented `patch --binary`, and does not reproduce. Meanwhile the s22+ model now replays
`s22..s39` end to end.

⚠ **Do not merge `great-williams`.** It predates `s38` and does not contain it, and its s22 would REINTRODUCE
the csproj duplicates. **Port, don't merge** — the one thing it gets right is putting
`SetControlToLeader`/`DebugClearControl` in **s22** (see the misattribution note above). Its self-heal and
texture captures are already carried here (s39 / s34).

## Superseded / historical — kept for the build record, do NOT apply

Earlier dev-iteration patches, retained so the engine history stays legible. They are superseded by the
live set above (the F6 menu replaced the single-key reload/reset hotkeys).

| File | Superseded by |
|---|---|
| `s12-engine-edits.patch` | early scratch; obsolete |
| `s18-field-reload-hotkey.patch` | `s22` (F6 → Reload field) |
| `s21-dev-hotkeys-f6-f10.patch` | `s22` (the tabbed F6 menu) |

## Upstream candidates & optional polish — NOT in the bundle

| File | What it is |
|---|---|
| `upstream/fieldcreator-png-export-path.patch` (+ `upstream/UPSTREAM.md`) | A standalone Memoria bug fix (FieldCreatorScene PNG export path). Not required by the kit's CLI pipeline; offered upstream. |

---

> **License / provenance.** These patches modify MIT-licensed Memoria (© Albeoris) and contain **zero**
> Final Fantasy IX game data. The compiled bundle is likewise MIT (© Albeoris) plus the Dream World IX
> patches. See [`../ff9mapkit/docs/PROVENANCE.md`](../ff9mapkit/docs/PROVENANCE.md).
