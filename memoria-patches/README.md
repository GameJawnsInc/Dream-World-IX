# `memoria-patches/` — engine patches for Dream World IX

Dream World IX runs **novel** fields on stock [Memoria](https://github.com/Albeoris/Memoria) and needs a
small **custom engine** only for **forked** fields. The full rationale is in
[`../ff9mapkit/docs/ENGINE.md`](../ff9mapkit/docs/ENGINE.md). These files are unified diffs against a Memoria
source clone — apply the *live* set and compile `Assembly-CSharp`, or just grab the pre-built
`dwix-custom-memoria-<version>.zip` from the GitHub **Releases** page.

This README is the per-file status map (which patches are live, which ship in the bundle, which are dead
history) so a reader isn't left guessing what each file is for.

## Live fork-fidelity set — SHIPPED in the engine bundle

These wrap the hardcoded `fldMapNo == N` engine gates with an *effective field id* so a custom fork inherits
the original field's tuned behavior. This is the set that makes a forked field play faithfully.

| File | What it restores |
|---|---|
| `s23-narrow-map-fork-width.patch` | A forked narrow field gets the donor's exact tuned screen width (no letterbox loss). `s23-narrow-map-fork-width.PR.md` is its upstream write-up. |
| `s24-fork-donor-remap.patch` | The fork→donor remap suite (`EffectiveFieldId` / `ForkSiblingField` / `IsForkField`) — off-mesh exemptions, the fake-battle return field, overworld→fork entry, the no-encounter wrap, and the rest. Folds in the intermediate s25–s28 milestone steps; there is **no separate s25–s28 file**. |
| `s29-fork-donor-softlocks.patch` | The same `EffectiveFieldId` wrap on the remaining late-game (disc 2–4) softlock gates (Iifa / Burmecia / Gulug / Oeilvert / Esto Gaza / Ipsen / Epilogue). Disc-1 gates are in-game proven; these late-disc gates are still being playtested as those zones are forked. |
| `s30-doeventcode-fork-walk.patch` | `EventEngine.DoEventCode.cs` — the file the s24 gate census MISSED. Its per-field event gates use a local alias `Int16 mapNo = fldMapNo` (not the literal `fldMapNo`), so a `grep fldMapNo` census skipped all ~150 `mapNo == N` gates. Adds `Int32 effMapNo = EffectiveFieldId(mapNo)` and routes every gate comparison through it (the 2 non-gate uses — the VoicePlayer arg + `sOriginalFieldNo = mapNo` — stay raw). Restores the per-field scripted-walk destination corrections (the `MOVE`/Walk fixups), e.g. field 53's "Blank jumps in hole" reposition (`destX 250→330`) that puts the falling characters over the hole. ★ IN-GAME PROVEN — fixed the character *positioning* for the 6003 fall (the *occlusion* needed s31). |
| `s31-fork-name-keyed-overlay-offset.patch` | The fork-gate census also missed a whole **NAME-string-keyed** class: `FieldMapExtraOffset` (`mapExtraOffsetList.txt`) tunes per-field overlay z-offsets keyed on the FBG **name**, not `fldMapNo` — so `EffectiveFieldId` (id-based) never reached it. A fork's deployed name (`FBG_N11_PRIM_TSHP_TH_MET`) misses the donor's entry (`FBG_N00_TSHP_MAP006C_TH_MET_4,-1,29,0,-100`), so the floor overlay keeps its raw depth during the fall-move and stops occluding the falling characters. Adds `DataPatchers.EffectiveFieldName(name)` — the name-space analogue of `EffectiveFieldId` (resolves a fork's FBG name → its donor's via the global id↔FBG-name table) — and routes `SetOffset` / `UpdateOverlayOffset` through it on a dict miss. Fixes the WHOLE name-keyed-offset class for every fork, not just 6003. ★ **IN-GAME PROVEN 2026-06-23** — the 6003 jump-into-hole now occludes the falling characters, matching real field 53. |
| `s32-fork-name-gates.patch` | The **rest** of the name-keyed gate class, from a follow-up engine census (the sibling sweep s31 flagged): 7 more sites where engine behavior is keyed on the FBG name a fork doesn't share, wrapped with the same `EffectiveFieldName` lever (generalized in this patch to resolve by the *passed name* so it is correct even at cross-field call sites like the SPS `ChangeFieldOrigin`). **Tier-A (gameplay-breaking on a fork):** the Iifa telescope/harp minigame player- + menu-control unlock (`UIManager.cs`, 2 sites — without it the fork freezes the player), the Conde-Petie space-scene menu unlock (`UIManager.cs`), the Iifa rain-particle world offset (`FieldRainRenderer.cs` — else rain spawns at origin), the Oeilvert star-display opaque-shader override (`BGSCENE_DEF.cs`). **Tier-B:** the per-field SPS sprite z-offsets (`FieldMapSPSExtraOffset.cs`, name-keyed dict like s31) and the Treasure-Hall +8px overlay nudge (`FieldMap.cs`). All identity-safe for real (non-forked) fields. |
| `s33-fork-fldmapno-arg-lookups.patch` | A **THIRD** fork-gate class the censuses missed: `fldMapNo` used as a per-field **LOOKUP ARGUMENT** (not a `== N` comparison like s24/s30, nor a name key like s31/s32). Found via the blank menu **LOCATION** field on a fork. The place-name is looked up by raw field id in `loc_name.mes` (field-id-keyed → a fork's custom id returns empty → blank) at **two** sites: `EventEngine.cs:536` (`mapNameStr`, the debug title) and `MainMenuUI.cs:497` (the in-field menu label — a *separate direct call* the title-bar fix alone missed). Adds `FF9TextTool.FieldLocationName(fldMapNo)` — try the field's **own** id first, else the fork's **donor** — and routes both through it. **Authorable title:** a new `LocationName <id> <title>` DictionaryPatch directive (`DataPatchers.CustomLocationNames`, consulted first by `FieldLocationName`, survives language reloads) lets the kit's `[field] location = "…"` set a from-scratch field's place-name **or override** a fork's inherited donor title. A sweep found 4 sibling argument-lookups, also wrapped: the field→battle **BGM** fallback (`BattleSwirl.cs` + `HonoluluFieldMain.cs` `GetMusicForBattle` — the kit's per-scene BattlePatch override still fires first, so this only changes the *fallback* to the donor), the **mesh-combine** render path (`BGSCENE_DEF.cs` — Crystal World / Iifa Roots / Oeilvert / Pand. Maze), and the **smooth-cam** exclusion (`FieldMap.cs`, ×2). **Deferred** (low value / fiddly): the moogle auto-save eligibility (`IsMoogleField` — gates only the optional `AutoSaveOnlyAtMoogle`, *not* manual save), the cosmetic field-audio preset, the custom-model swap. ★ The menu LOCATION fix is the reported bug; the sweep siblings are IN-GAME UNVERIFIED, verify opportunistically. |

**Area-title gates — investigated, NO fix (a false alarm, resolved 2026-06-23):** the two deferred gates were *not* an asset risk. `FieldMap.SetFieldMapAtlasName` (the `atlas_<lang>` rename) has **zero runtime callers** — dead code — so the missing-texture premise never existed; the runtime atlas load is a flat `Load("atlas")` (`BGSCENE_DEF.cs:839`), and the title lettering is baked into the base `atlas.png` every fork ships (a native Mognet-Central fork renders the card, in-game proven — `FORK_FIDELITY.md`). So English area titles already work on forks (synth forks correctly *hide* them; `--verbatim` shows them via the carried donor `.eb`). `FieldMapLocalizeAreaTitle.GetInfo` only gates the **non-English** `_<lang>.bgs` title-*geometry* swap (`LoadLocalizationInfo` returns early for US), which a native/editable fork can't use without shipping 7 localized `.bgs` sidecars per fork — a cosmetic, non-English-only follow-up, intentionally left (BG-borrow + same-name forks already get it via the donor bundle).

## Custom-overworld (Path C) — NEW capability

| File | What it adds |
|---|---|
| `s39-world-selfheal-control.patch` | **The overworld black-screen SELF-HEAL** (`ff9.w_worldSelfHealControl`, captured from the live tree 2026-07-15). Recovers the "no controlled actor" state (`project-ff9-overworld-actor-brick`): editing overworld geometry under a saved spawn leaves the leader with no `wmActor` / no control designator → `ff9.GetControlChar()` null → `w_frameUpdate` keeps `w_moveActorPtr` on the DUMMY → the camera has nothing to follow → **BLACK, baked into the save**. Called from `w_frameUpdate` case 0 ONLY when `GetControlChar()` is null; rebuilds the control body, ensures every active world actor has a `wmActor` (`w_movementChrInitSlice` dereferences it with no null check), force-grounds via a sky-cast, and binds the camera actor. **SELF-TERMINATES** (once it binds a real actor the else-branch never fires again — no one-shot flag) and is try/catch-wrapped so recovery can never crash the frame loop. Never touches the Bee scene. ⚠ Carries a live **TODO from its author**: the degenerate-spawn fallback `kWorldSafeFallback` (768, −640) is the map centre and **"may be sea"** — confirm it is walkable LAND before release. |
| `s34-worldmap-mesh-override.patch` | Load a world-map block TERRAIN mesh from a loose **`.ff9mesh`** file in a mod folder instead of the baked Resources mesh — so overworld geometry can be edited **without** an AssetBundle repack (FF9 is Unity 5.2.3; `AssetManager.LoadFromDisc<Mesh>` is unsupported, so there is no other loose-mesh hook). New class `Memoria.World.WorldMeshOverride` (reads the kit's [`ff9mapkit.world.mesh`](../ff9mapkit/ff9mapkit/world/mesh.py) format) + a hook in **`WMWorld.RegisterBlockComponent`** — the RUNTIME block-stream path. ⚠ The first cut hooked `WMWorldPrefabMaker.LoadMesh`, which is reachable only from `LoadModelAsset` — an **editor-only prefab-builder with ZERO runtime callers** (the overworld loads a *pre-built* WorldDisc prefab and streams blocks via `WMWorld.LoadBlock`), so it never fired. The live hook reads `block.InitialX/InitialY` (grid coords) + `transform.name` to build the override path, and swaps the `MeshFilter.sharedMesh` (render) **and** the `mesh` fed to `WMBlock.AddWalkMesh` (collision + the `tangent.x` entry id) — one object. Identity-safe: no override file → stock mesh. **Also add `<Compile Include="Memoria\World\WorldMeshOverride.cs" />` to the csproj** — ⚠ that Compile line currently lives in **`s22`**, misattributed but load-bearing (it is the only patch adding it); moving it here is deferred while the csproj is in flight (see STACK HEALTH). **Also carries the custom-ocean loose-TEXTURE override** (recaptured from the live tree 2026-07-15, previously in no patch): `WorldMeshOverride.TryLoadTexture` + a render hook in `RegisterBlockComponent` that clones the material and swaps `mainTexture` when a loose custom texture exists for a cell+part, **keeping the sea shader** — so a custom ocean renders as a continuous gradient instead of discrete tile shades. Cell-scoped; identity-safe (no override file → stock material). ★ Built + deployed 2026-06-30 (re-hooked + rebuilt clean); IN-GAME UNVERIFIED — awaiting the `world-deploy --spike` playtest. The foundation for Path C (geometry-edit the real world) and Path D (a minted continent's geometry). → `project-ff9-worldmap-feasibility`. |

## Multiplayer co-op (experimental) — NEW capability

| File | What it adds |
|---|---|
| `s36-netsync-ghost.patch` | **Exploration co-op "ghost sync"** — two players see each other walk a shared field in real time. New namespace `Memoria.Netsync`: `NetSyncClient` (a `DontDestroyOnLoad` MonoBehaviour bootstrapped from `HonoluluFieldMain.HonoAwake`) + **two transports behind `INetTransport`**: `NetSyncSocket` (direct LAN TCP host/client, background read/write threads, lock-guarded latest-state slots, staleness timeout) and `NetSyncRelay` (a hand-rolled RFC-6455 WebSocket **client** for the public rendezvous relay — handshake, client-masked binary frames, ping/pong, reconnect, a 75s pre-pairing receive window matched to the relay's 60s pair timeout); plus one `HonoAwake` line and three csproj `<Compile Include>`s. Reads a `[Netsync]` section from `Memoria.ini` (`Enabled` / `Role`=selftest\|host\|client / `PeerAddress` / `Port` / `TargetField` / `RelayUrl` / `SessionCode` / `InsecureTls`); `RelayUrl` set → relay transport, empty → direct TCP; **OFF by default** (`Enabled=0` → nothing is created, zero footprint on a normal game). **`TargetField = 0` (the default) = co-op EVERYWHERE** — the transport dials once and lives for the whole session, each side broadcasts its true location every frame (a field-0 sentinel during menus/battle/transitions — which also fixed a latent bug where the 30 Hz keepalive re-sent a stale field id and FROZE the departing player's ghost on the peer's screen), and ghosts appear on any screen both players share (`gMode==1` — battle and the overworld are out of scope); a nonzero `TargetField` restricts to that one field (the original hangout mode). The overlay names the peer's current field ("your friend is on another screen — field N"). ★ Everywhere-mode solo tier IN-GAME PROVEN 2026-07-12 (selftest ghost follows into stock fields; nothing on the world map or in battle; clean reconnect on field re-entry); the cross-machine bits (peer-elsewhere overlay, leave-a-field despawn) await the next two-machine session. A relay **host with no `SessionCode` auto-generates a random namespaced `ff9-XXXXXXXX` code and persists it into `Memoria.ini`** (the code is the session's access gate; a guest with no code refuses to dial). **`[Netsync]` HOT-RELOADS**: the client watches `Memoria.ini` (mtime-gated file stat every ~2s + a parsed-value signature, so its own code-persist isn't an "edit") and applies changes to a RUNNING game — transport redialed, ghost/overlay reset; enabling co-op from OFF applies at the next field load (nothing is created while disabled). The relaunch-per-edit rule is dead for this section (★ in-game proven 2026-07-12: enabling from off appeared at the next field transition; a live disable vanished the overlay in ~2s, no relaunch). ⚠ The game's old Mono has no TLS 1.2, so `RelayUrl` should point at the local [`tools/netsync-bridge/`](../tools/netsync-bridge/README.md) (`ws://127.0.0.1:49201`), which carries the connection to the relay over TLS. On a shared field it spawns the peer as a "ghost" **wearing the peer's OWN model** (wire v2, 38 bytes: `{field,anim,model,meshVis,pos,rot}` — the model id resolves through `FF9BattleDB.GEO` exactly like the event engine spawns actors, re-dresses live when the peer changes character, and falls back to Zidane ONLY when the id can't resolve/load, e.g. a modded character this install lacks; the peer's own 16-bit per-submesh visibility mask drives sheathed weapons etc., replacing the old local-player index-mirror; v1 peers are length- **and** version-rejected → mixed engine versions silently don't sync). The model recipe is `FieldMap.AddPlayer` minus the controller — renderers enabled + masked, `GeoTexAnim` texture anims, huge skinned-mesh bounds so the PSX-shader-projected model isn't frustum-culled — and the transform + animation interpolate with exponential smoothing (`LerpRate=12`) to hide network jitter (anim ids are global, so the peer's clips bind natively on their rig — the upside-down-Zidane bug is dead). ★ Non-Zidane ghosts solo tier IN-GAME PROVEN 2026-07-12 (a correct right-side-up Garnet mirror, re-dress on a mid-session character swap, Zidane regression clean); cross-machine awaits the next two-machine session. NO field/battle/story-state sync (each save is independent). ★ Direct-TCP **IN-GAME PROVEN 2026-07-08** — two machines over WiFi, each rendering the other's live Zidane ghost moving + animating. ★ Relay path **IN-GAME PROVEN 2026-07-11** — two machines each through a local bridge to the public relay over TLS, paired by an auto-generated session code; the ghost appeared ~1 s after field load (`test_relay_roundtrip.py` re-proves the bridge+relay path offline, without the game). An **on-screen status overlay** shows the session code + pairing state in game — "waiting for your friend… code ff9-XXXX" while unpaired, a bridge-not-running hint if the transport can't connect (`INetTransport.IsConnected`), an F6-warp nudge when co-op is on but you're in another field — and hides the moment the peer's ghost is up (★ in-game proven 2026-07-12 — including cross-version pairing: an overlay-less peer DLL paired fine, the wire protocol is unchanged). **Ghost lighting** (the last cosmetic gap, closed): registered characters get a flat per-char `_Color` tint each frame — `charClr(model) + lightClr(floor)` from the field's `mapconfig` (`fldmcf.ff9fieldMCFService` computes, `fldchar` applies) — which the ghost, living outside the char table, never received; the ghost now replicates the same math at spawn (its OWN model's char color + the LOCAL player's floor light, default light as fallback) — ★ lighting IN-GAME PROVEN 2026-07-12 (the selftest mirror matches the scene's shading). No known cosmetic gaps remain. → `project-ff9-multiplayer-injector`. |
| `s37-netsync-battle.patch` | **Battle co-op, rungs B0+B1 (EXPERIMENTAL — applies on top of s36)** — the "networked controller 2" design from `studies/battle-coop/`: the battle always runs on ONE machine; the peer spectates it live and (optionally) commands assigned party slots. **Wire v3**: every message becomes a typed frame `[magic][ver=3][type][len][payload]` on both transports — type 0 position (the old 36-byte payload, latest-slot + keepalive), type 1 battle state (latest-slot), type 2 battle commands (**FIFO, never collapsed** — the TCP/WS streams are already reliable+ordered), type 3 control; v2 peers are version-rejected (the established mixed-versions-don't-sync fail-safe; update both machines). New `NetSyncBattle` (+ csproj include): **B0** — in battle, a ~150 ms sampler serializes the roster (name markup stripped, logical-HP accessors) into type-1 frames; the peer renders a left-side OnGUI panel (enemies numbered, party HP/MP/ATB%, ready markers) and the idle overlay quiets while it shows. **B1** — `[Netsync] GuestSlots = <bitmask of party slots 0-3>` (default 0 = spectate-only) offers those slots to the peer: the local HUD skips them (guards in the ready loop, `SwitchPlayer`, the two switch-rotation paths), the peer's panel turns into a one-keystroke command menu on their turn (`[1-8]` = Attack the Nth listed enemy, `[0]` = Defend), and the arriving command frame is validated (guest slot, alive, ATB-ready, `cmd[0]` free; dead target → first live enemy) then queued through a new `BattleHUD.SendNetCommand` — modeled verbatim on the engine's own `SendAutoAttackCommand`/`SendCommand` (duplicate guard, `sel_mode=1`, `InputFinishList`, blink clear; never touches `CurrentPlayerIndex`). **ATB feel**: a `RemoteMenuOpen` control flag (staleness-guarded, ~2.5 s decay — a hung/disconnected guest can never freeze the host) is OR'd into `IsNativeEnableAtb` (vanilla WAIT) and the Turn-Based `isMenuing` — so Wait/Turn-Based hosts get their native gauge-freeze during remote turns while Active/Dynamic stay untouched; on guest stale/disconnect the slots revert to local control (the skip guards read the same liveness). **Selftest = the solo proof**: with `Role=selftest` + `GuestSlots` set, your own battle loops back through the FULL codec — the panel appears over the real fight and the guest slots are commanded by the assist keys instead of their menu (PS1 2-player battle, played solo with both hands). Assist digits are SWALLOWED from the game's key handlers while the panel awaits a command (`SwallowAssistKey`, an F6-pattern early intercept in `UIKeyTrigger.Update` — found in playtest: Alpha1 is Memoria's default help/Select binding and double-fired). ★ SOLO TIER IN-GAME PROVEN 2026-07-12 (panel live, keypress commands, WAIT-mode gauge freeze on remote turns, kill-renumber retarget, clean digits after the swallow fix). **ABILITIES + ITEMS (wire v4, same day, ★ solo-proven):** a type-4 ROSTER frame streams each guest slot's menu content (learned-ability ids + MP + live enabled state + target type, battle-usable items + counts — ids only, names resolve from the guest's own install) built by `BattleHUD.CollectNetMenus` (mirrors DisplayAbility/GetAbilityState/DisplayItem per slot); the guest panel is a digit-menu state machine (Root → paginated ability/item lists → target pick; All/Self/Random auto-resolve; ForDead abilities list the DEAD — Phoenix Down proven); the host re-resolves the command per slot (trance-aware) and revalidates LIVE at injection (`NetAbilityUsable`; closed a real unlearned-cast cheat path found in playtest — an empty-list single-shot now requires the first USABLE ability). **`[Netsync] GuestWaitMs`** (default 30s, 0=unlimited) caps an idle guest's Wait/Turn-Based gauge freeze per turn — a fallback, not a rush timer; the host adjusts. Excluded v1: Throw, double-cast/mix, monster-transform. **VISITOR MODE (wire v5, same day; ⚠ dressing+suppression solo-provable, follow-warp two-machine-only):** the stock-play vision — the guest is a presence in the HOST's game. New `NetSyncVisitor` (+ csproj include): (a) **ghost dressing** — `[Netsync] GhostAs = auto|<playable>` dresses the PEER's ghost as the party member of my first GuestSlots bit (the character they command in battle is the character they appear as); anim ids are rig-partitioned, so the pos frame gains a sender-classified **rig-neutral loco byte** (idle/walk/run/turnL/turnR/other, classified against the sender's own rig via the kit's proven home-field clip table, 8 mains; the receiver resolves it on the dressed rig — gestures fall back to idle; the wire submesh mask is skipped when dressed, it's only index-valid on the peer's own model); (b) **`FollowHost = 1`** (guest side) — auto-warp my game to the peer's broadcast field id, debounced 1.2s against door transitions, registration-guarded (`eventIDToFBGID` — an unregistered field shows "your friend is on field N, which this install doesn't have" instead of a black screen), using the exact F6 deferred-fade warp recipe; (c) **encounter suppression** — while following a connected host (or selftest), `SettingsState.IsNoEncounter` ORs in `NetSyncVisitor.SuppressEncounters` (one line; the booster property all three encounter sites already consult) so only the host's battles exist; link down → normal encounters instantly. ★ VISITOR SOLO TIER IN-GAME PROVEN 2026-07-12 (dressing, party-order re-dress, GhostAs hot-reload cycling incl. auto↔explicit, encounter pause + hot-reload, dressed-ghost lighting incl. per-frame light-zone tracking, mask parity "no dagger", and the camera clean through reorders). The proof round fixed three real bugs: dressed-ghost lighting FALLBACK (a dressed model has no entry in the host field's mapconfig → now worn-model → the LOCAL PLAYER's entry → default) and CONTINUITY (the engine re-tints registered chars per frame; the ghost now re-applies at ~7 Hz, riding the local player's floor light), and — the big one — **the HonoBehavior teardown law**: the ghost's `GeoTexAnim` is a `HonoBehavior` (self-registers at Awake), and `HonoBehaviorSystem.Update` SWEEPS its list (`IsEnabled()`) BEFORE processing queued removals — so a raw `Object.Destroy` of a HonoBehavior-carrying object MID-SCENE leaves one tick that dereferences the destroyed component → a per-frame NRE that aborts the sweep forever and freezes every later HonoBehavior (the field camera pipeline; found via a frozen camera after a party reorder re-dressed the ghost mid-field). Sanctioned teardown: `UnregisterHonoBehavior(dispose: true)` — the system removes every child behavior synchronously and destroys the GameObject itself. Field-transition despawns never hit this (the system rebuilds per scene), which is why it lurked. FollowHost's warp remains the one two-machine-only untested piece. **THE COOP CELLS (V2 `[[coop]]` authoring; ★ SOLO-PROVEN 2026-07-12 — the two-plate gate fired ONCE on step-in at field 4003, proving the broadcast, the expression compare, AND the once-latch in one report):** while Enabled, every frame writes the PEER's presence + walkmesh position into kit-reserved `gEventGlobal` bytes 2032-2039 (`WriteCoopCells`: presence byte @2032, X/Z Int16 LE @2034/2036 — byte-addressed exactly as `EBin.GetVariableValueInternal` reads, beside the kit's mask scratch at 2040) — kit-compiled `[[coop]]` two-plate gates read them as ordinary GLOB vars in region Range bodies (engine = mechanism, `.eb` = policy; the engine knows nothing about plates). Never written unless Enabled (a vanilla save is untouched); presence zeroes on unpair/out-of-scope/config-change. Selftest writes the mirror's position (+250 x), so a gate with plates 250 apart is solo-provable. **STATE-MIRROR RUNG 1 (wire v5→v6, regenerated into this patch 2026-07-15):** the authoritative-host substrate — a type-5 STATE frame carries the HOST's `gEventGlobal` (section-tagged payload `[id u8][len u16 LE][bytes]`; section 0 = the 2048-byte story heap; the coop cells 2032-2041 are masked BOTH ways so the guest keeps its own `[[coop]]` view), published ~7 Hz by the host; a FOLLOWING guest applies it at the field-load boundary (`HonoluluFieldMain.ff9InitStateFieldMap` → `NetSyncClient.ApplyStoryBeforeEvents()` immediately before `StartEvents`) so its fields stage the HOST's story instead of scenario-zero. New `NetSyncState.cs` (+ csproj include): snapshot/apply (`ApplyStoryTo` is scratch-targetable) + capture/restore + a selftest that round-trips the REAL wire codec (`BuildFrame`/`TryParseHeader` → the real section-walk apply into a scratch buffer, never the live array). **Session save-safety (two guards + an exit ramp):** while mirroring, the guest can neither manual-save (`SaveLoadUI.OnKeyConfirm` refuses on `NetSyncClient.IsMirroringStory`) nor autosave (the `EventEngine` guard below), so the guest's DISK stays pristine all session; and a session that ENDS exits through **the autoload ramp** (`NetSyncState.ExitMirrorToOwnSave`): the guest's OWN autosave is reloaded — the verbatim moogle-menu load transition; no autosave yet → title screen — NEVER an in-place flag restore, which would leave the guest's own story standing at the HOST's location (a sequence-break/softlock frankenstate the base game can't produce). Link-drop arms the ramp debounced (3s, relay-blip-proof); a config change arms it immediately; the mirroring flag stays true until the ramp fires so the save-block stays armed through the gap; the ramp fires only free-standing on a field/world map; app teardown skips it (the disk is already coherent). (Kit-side, `coop host` forces `FollowHost=0`.) Two battle-menu fixes ride the same round: `CollectNetMenus` SKIPS an Ability command with zero learned abilities (no more empty "Swd Art" → dead target prompt on the guest), and `BuildRoster` waits quietly via the new `BattleHUD.NetMenusReady` probe until `InitialBattle()`→`ManageAbility()` seeds the ability dict — the battle-intro `KeyNotFoundException`/`roster build failed` log spam (pre-existing since B1) is gone, same wire behavior. v5-and-older peers are version-rejected; NOTE the transport asymmetry: `NetSyncSocket` logs+disconnects on a version mismatch, the relay just never pairs — check both DLLs before debugging "no sync". ★ SOLO TIER PROVEN 2026-07-15 (selftest codec OK ×3 launches, Sword Art absent in-game, zero roster errors + a guest Attack through the quiet path). **★ TWO-MACHINE PROVEN same day (Phase 6): fail-safe (v5+v6 never pair), RENDER-MATCH (field 354 rotating cast re-stages from the host's ScenarioCounter — the authoritative-host headline), Twin Altar incl. the held door, battle B0+B1, follow-warp.** The session also found the last save-safety hole: **AUTOSAVE bypasses the SaveLoadUI block** — `EventEngine`'s field-load `serializer.Autosave` writes the Continue slot on every transition, so a following guest's Continue collected the host's mirrored story + the followed-to field. Fixed in this patch (`EventEngine.cs` joins the set, 16 files): `noSave \|= NetSyncVisitor.SuppressEncounters \|\| NetSyncClient.IsMirroringStory` — a FOLLOWING guest never autosaves (encounter-suppression's exact scoping: following + connected, or selftest; vanilla on link-drop; hosts and non-following guests keep vanilla autosave). Save-safety re-test = the remaining Phase 6 box (`studies/battle-coop/PLAN.md`). |
| `s38-netsync-spectator-field.patch` | **ROAD A — SPECTATOR-STRICT (applies on top of s37)** — the correctness half of the SPECTATOR-FIELD paradigm: *a following co-op guest has no interaction authority, no local state authority, and no local escape.* Closes **a live, present-tense exploit**: the guest's flags are mirror-owned and TRANSIENT (all 2048 gEventGlobal bytes overwritten from the host on every field load) while its bag is guest-owned and PERSISTENT (the mirror never touches `FF9.item`) — so a guest opening a chest the host hasn't looted banks the item in its REAL bag, the next field load reverts the "opened" flag from the host's snapshot, and the chest is closed again → **an unbounded item farm, one item per field re-entry**. Three gates, all keyed on `NetSyncClient.IsMirroringStory` (the same predicate as the save gates; its "arming gap" — false until the first mirrored field load — is CORRECT, since until then the guest's own story is still authoritative): (1) **`AchievementManager.ProcessAchievementReport`** — the guest's stray writes are all discarded by the autoload exit ramp *except* a Steam achievement report, which leaves the machine and no ramp can retract; this is the single funnel every channel (item/battle/ability/minigame) crosses before the native call. Do NOT gate `FF9Item_Achievement` instead — `ITEMADD` fires five `EMinigame` achievements that bypass it. (2) **ENCOUNT `0x2A` + ENCOUNT2 `0x8C`** (`DoEventCode`) — a mirrored script was booting a LOCAL battle the host isn't in, whose result screen writes the guest's real gil + item drops + Steal; `SuppressEncounters` never covered these (it reaches only `ff9.IsNoEncounter` = the random-step and worldmap rolls). Returns **0** (inline continue), and **the guard MUST sit below every `getv`** — `commandDefault2` branches on `gArgUsed` and REWINDS the IP one byte if no arg was consumed, re-executing the opcode forever. (3) **the menu** — all four `UIKeyTrigger` `IsMenuControlEnable` reads, incl. the Alt+F2 party / Alt+F5-F9 save-load shortcuts that bypass `OnKeyMenu` entirely. Gated at the READ, **not** via an `IsMenuControlEnable` getter: `FieldMapActorController:167` reads that flag to decide an INACTIVE actor should idle, so forcing it false drops those actors into movement processing. **Deliberately NOT blocked:** tread regions (tag 2) — the `[[coop]]` plate gates and the Twin Altar's held door live there, and a gateway only moves the guest (follow-warp overrides it, the ramp discards location); and dialogue/`B_KEYON`, which the guest needs to advance its own mirrored cutscenes. **No wire change (still v7)** — so a stale peer still pairs: **the gates are guest-side, so BOTH machines need this DLL for symmetric protection.** ★ SOLO REGRESSION + TWO-MACHINE PROVEN 2026-07-15 (menus refuse incl. both Alt lanes; Evil Forest Plant Brain + plant-spider scripted fights skipped on the guest while the host fought; no regressions in coop gates, battle B0/B1, cutscenes, or the exit ramp). **Known residue (rung 2, not shipped):** a real chest on a *verbatim fork* is proximity-dispatched with its confirm read INSIDE the script (`B_KEYON`), so it still opens — but post-A the item lands in a bag nothing reads and the ramp discards, i.e. theater, not corruption. Closing it needs a pad mask (risks wedging any script that polls a button) → `studies/battle-coop/inventory-authority.md`. |
| `s40-netsync-diorama.patch` | **THE BATTLE DIORAMA (B3.0-B3.5, wire v9) — applies on top of s38** — the authoritative-host battle headline: a FOLLOWING guest's game boots the HOST's battle scene and PLAYS it — the host's party seated (scratch-PLAYER dict swap), enemies from the same (scene, pattern), live HP/death/revive/trance/visibility reconciled from the type-1 lane every ~150 ms, and the host's ACTIONS (exact damage/heal/miss/crit figures + flinches + caster swings) from the new **type-6 FIFO action lane** fed by a CalcResult emit (whole-body try + finally backstop; the seq-HORIZON consumes the sampler's `_seq` so stale type-1 samples skip gated units by construction). NEW `Memoria/Netsync/NetSyncDiorama.cs` (the containment predicate `Active` = armed+booted+`loadedLevelName`; the gil/battle_no/kill-counter/battle-field bracket; the reference party bracket; boot/leave/teardown; the reconciler; the action handler; the state/figure/wire benches) + the engine containment gates all keyed on `NetSyncDiorama.Active`: `btl_sys.SavePlayerData`+`CheckBattlePhase`+`CheckForecastMenuOff` (definitions), the `btl_stat` OnOpr dispatch + `UpdateAbnormalStatus` achievement write, `BattleActionCode`'s gEventGlobal write, `btl_scrp` op-37 SetNextMap, `battle.cs` no-enemy-left auto-end, `SettingsState.SetTranceFull`, the `BattleHUD.Unity` L+R escape combo, `btl_para`'s Overload checkpoint dispatch, `BattleUI.OnGUI` — plus the `EventEngine` noSave + `SaveLoadUI` blocks on `Booted` (a selftest guest has no other save guard). Wire v7→v9 along the way: v8 = the type-1 DIORAMA BOOT BLOCK (patNum/startType/flags/nonce; mapNo==battleMapIndex is one id space) + the guest watcher (`[Netsync] Diorama`, default 1); v9 = the action lane + the type-1 trance-gauge byte + the section-1 party v9 block (basis/status/trance/SA — closes the default-SA and 9999-clamp seat gaps; equip ContainsKey→NoItem guard; per-slot containment; NEVER Validate* in a seat). Also carries THE TICK-BASELINE LAW sweep (TickCount wraps negative at 24.86 d uptime — 8 timer baselines re-founded; the entire first two-machine failure) and the mirror/watcher telemetry (every gate that can decline says so). ★ B3.0-B3.5 ALL SOLO PROVEN 2026-07-15/16; B3.3 two-machine proven ("host battles pull the guest in"); B3.4/B3.5 two-machine boxes ride the 20260716 laptop package README. The deployed DLL `6F999C4575266592` == base+s22..s40 (minus the FieldMapActorController exclusion below; built pre-EOL-normalization, content-identical). → `studies/battle-coop/diorama-lane.md`, `b34-recon.md`, `b35-recon.md`. |
| `s41-netsync-diorama-polish.patch` | **B3.6 — THE POLISH ROUND (wire v10) — applies on top of s40** (6 files, no new sources). The diorama boots THROUGH the real battle swirl playing the HOST's music: the type-1 boot block gains `songId` u16 (host samples the COMPUTED id at both `BattleSwirl` branch heads so BattlePatch `Music:` + s33 remaps ride along; sentinels 0xFFFF unknown → guest-local fallback, 0xFFFE host-silent → guest silent; `[Netsync] DioramaSwirl` default 1, hot-reloadable, 0 = the plain-fade lever) + the pre-swirl field-audio replicate + a SwirlScene watchdog and stranded-flags fences. Poison/Regen **TICK figures** ride the type-6 lane (marker bit7; guest pops via `Btl2dStatReq`, host figure verbatim — closes B3.5's most visible accepted gap; nonce = payload byte 0). The B0 spectate panel hides while the diorama is live (assist menu always; the panel survives every no-diorama state). The F6 **opt-out replay bug** fixed both halves — the staleness flicker no longer clears the skip, and **THE LEAVE-INTENT LAW**: who initiated a teardown is recorded AT THE INITIATOR (`NetSyncDiorama._leaveTick`, read as `LeaveRequestedRecently` <5 s — a tick with decay, never a wedgeable Boolean; user/watchdog leaves SETTLE = stamp the skip, watcher/dropped-boot RETRY). Guest-side `ValidateWireBoot` PatNum guard (sync scene pre-read BEHIND the 1/s rate limit; durable refuse = skip-nonce + one line). `[Netsync] SelfTestOffset = dx,dz` (default 250,0; quoted form legal — `GetSetting` returns quotes verbatim and FolderNames trains quoting; the first real edit arrived quoted and silently kept the default). Build shape: recon 6/6 CORRECTED → delegated build → 7-skeptic verify → repair → all NINE leave paths re-walked clean. ★ SOLO PROVEN 2026-07-16 (swirl+leave, bench `song 156`, random-encounter `song 0` = the generic-Battle-Theme law, opt-out functional, vanilla battle, SelfTestOffset mirror-move; poison ticks piggyback later); two-machine boxes = the 20260716b laptop package. Deployed DLL `0537BC580F0D68C7` == base+s22..s41 (same exclusion). → `studies/battle-coop/b36-round.md`, `diorama-lane.md`. |
| `s42-netsync-config-split.patch` | **The session-hardening round (built 2026-07-19, ★★ two-machine PROVEN 2026-07-20; wire UNCHANGED at v10; one file: `NetSyncClient.cs`).** Fixes the two verified root causes of the 07-19 two-machine round (11-agent source review, `wf_3c741130`): **(1) THE CONFIG-SIGNATURE SPLIT** — the old single config signature made ANY `[Netsync]` edit fire the full teardown (transport drop + `SuppressEncounters` forced off for the reconnect window + a mirroring guest exit-ramped to their own autosave). Now `_cfgTransportSig` (enabled/role/peer/port/relay/code/tls → full `ApplyConfigChange`) vs `_cfgBehaviorSig` (targetField/guestSlots/guestWait/ghostAs/followHost/selfTestOffset/dioramaSwirl → new `ApplyBehaviorChange`: session KEPT; arms the exit ramp only on FollowHost-off-while-mirroring); `LoadConfigIfChanged` returns 0/1/2. **(2) THE SAME-FIELD KICK** — a guest co-located with the host at pair time never crossed a field-load boundary so the mirror never armed (next host battle = a diorama seated with the guest's OWN party); `SameFieldKickTick` fires ONE forced fade+reload of the current field through the proven deferred-fade warp, gated on role=client + !mirroring + !rampPending + FieldHUD + a host snapshot on the wire, re-armed via `RearmSameFieldKick` when a load skipped its mirror-arm — CAPPED at 3/session with one give-up log. **(3) the `[Netsync] Diorama` knob DELETED** (3/3 judge verdict — its only realized effect was this defect class; stray ini lines ignored). Verify-round fixes: an in-flight follow-warp ALWAYS completes (`ServiceFollowWarp` moved ABOVE the `!_enabled` gate; `ApplyConfigChange` no longer clears `_followWarpPending` — the old clear stranded a black screen if an edit landed inside the ~0.9s fade) + `DioramaTick` declines to boot while a warp is pending. Also carries the s43-round overlay string ("meet your friend: ~ menu > Warp"), captured together as planned. ★★ PROVEN: same-field kick / live play-style edit (session kept) / transport edit reconnects — all on the real link. Gate 2026-07-20: single-file lineage replay (s36+s37+s40+s41 `NetSyncClient.cs` hunks) + s42 == live bytes; GNU `patch -R -p1 --dry-run -F0` clean on live; forward dry-run clean on the pre-tree. Kit side (--diorama flag delete, Workspace checkbox) landed at `521661a`. |
| `s44-netsync-field-coop.patch` | **F1 FIELD CO-OP — the spectator-field round (built + ★ SOLO BENCH 6/6 PROVEN 2026-07-20; wire UNCHANGED v10; 9 files, one new: `NetSyncField.cs`).** The spectator-field paradigm's field half (`studies/field-coop/DESIGN.md` + `F1-BUILD.md` = the full decision record): a FOLLOWING guest never starts interactions, never self-navigates, and can always recover to the host. **The predicate**: `NetSyncField.SpectatorField` = `IsLiveFollowedSession \|\| IsMirroringStory \|\| (IsSelfTestRole && BenchGates)` (closes the pre-mirror AND post-link-death-pre-ramp windows; `BenchGates` = the ~-menu Field-gate bench lever, the selftest arm — no `[Netsync]` knob). **The gates**: `EventCollision.CheckQuadInput`/`CheckNPCInput` at the would-start points (chests, NPC talk, card starts) · the `MENU` opcode (both `getv1()` consumed first) · **THE GATEWAY REDIRECT** — MAPJUMP keeps the vanilla instruction flow (`return 0` was refuted by an 817-field census: 4,400+ live-code post-`Field()` followers incl. two `while(1)`) and rewrites dest to a same-field reload via `RedirectGuestFieldJump` (follow allowance: dest == `LastHostField` passes; a 3-in-20s BOUNCE BUDGET lets auto-transition fields through; the opcode's return-4 gets `nextMode=1`/`attr\|=8` from HonoluluFieldMain case 4 — transition-mode-identical to the s42 kick) · **THE KEYON MASK** — `FilterScriptButtons` strips Confirm\|Special from `B_KEYON`/`B_KEY` script reads ONLY while `SpectatorField && GetUserControl()` (stock loot/chest tread-bodies poll control-ON and mask; the census's 271 blocking Confirm-waits are cutscene beats that run control-OFF and pass — built when the first playtest surfaced a lootable field-206 Phoenix Down). **Teleport-to-host**: F11 tap / held-L3 (session-gated intercept — vanilla never reads the keys) + AUTO recovery (floor 640/hardCap 1200/dwell 4.5s/cooldown 10s seeds; the ratified "gap keeps GROWING" steering override with a persistent arm baseline; snap via `fieldmap.playerController` SetPosition nearest-Y, honest off-mesh verdict). **The snapshot-taint fix** (~ menu): `_snapshotMirrored` = mirroring \|\| `OwnSaveReloadInFlight` (the ramp's async window, cleared in the autoload CALLBACK) \|\| selftest-bench; tainted restores post-session are discarded with a visible reason. Build shape: 12-agent recon-verify → implement → 7-skeptic adversarial verify → repairs → focused re-verify (`wf_86eb27dc`/`wf_e66632e8`/`wf_7c682de1`). Gates: forward `git apply` onto the pre-round snapshots (`backups/preF1-snapshots.20260720`) == live bytes ×9; reverse `-F0` clean on live; forward GNU dry-run clean on the pre-tree. Deployed DLL `7F3E17BAF6EC0401`. Two-machine boxes pending (real-guest reachability, redirect on the live link, auto-TP with a real host, snapshot leak end-to-end). |

## The Folklore codex (player-facing feature) — NEW capability

| File | What it adds |
|---|---|
| `s46-folklore-render-rig.patch` | **THE RENDER RIG, rung 1 — ★★ IN-GAME PROVEN + CLOSED 2026-07-21 (3 same-day playtest rounds: mechanism 4/4 incl. the field+battle leak snaps + the world-map open → transparency/facing → the Equip-sentence split, "looks good"; 1 file: `FolkloreUI.cs` only — deliberately NO csproj change, so no adjacency risk). **RUNGS 2+3 ★ IN-GAME PROVEN + CLOSED same day over 3 playtest rounds** (round 1's "red circle then blank" = `GEO_MON_B3_187` IS a ring-shaped battle PROP — the model-preview/offline-eye law; round 2 "looks good" proved wire+swap+idle; round 3 = **the LIVE-SILHOUETTE FIT**, "good" — bind-pose-only framing clipped tall idles, replaced by a live renderer-bounds envelope: `updateWhenOffscreen=true` (the ghost-hack's deliberate opposite), first live sample REPLACES the static seed (the 2-skeptic round's headline: an expand-only envelope seeded from the bind pose can never zoom IN), candidates NaN/Inf/MaxExtent-validated pre-commit (poison-proof), the settle window in `Time.deltaTime` seconds (VSync makes frame-counts refresh-rate-dependent), grow epsilon `max(2, 1% of envelope)`; deployed DLL **`3000418F860C843B`** both arches). **+ THE BODY-BLEND ZOOM (★ IN-GAME PROVEN — "the gale sprite is way bigger now"):** the flavor-pass Dragon Fly rendered as a speck — the live fit frames the MOTION VOLUME and a flyer's hover-bob makes that ~3.4× its body (settle-log-measured); now `folkloreFitBodyMax` (per-frame instantaneous half-extents, componentwise max) blends `body + (envelope − body) × 0.4` aimed at the flight center — ~1.7× zoom on flyers, grounded creatures unchanged (envelope ≈ body); the skeptic's catch: body growth must OR into the refit gate (envelope growth stabilizes after one cycle — a late wing-spread would crop while the log lied); static path bit-identical through `ApplyFolkloreFitCore`. **+ THE TURNTABLE YAW (rung-5 garnish, built + captured + DEPLOYED same day, DLL `BA5DE4A838DE4AB9` both arches; ★ playtest pending):** 25°/s vertical-axis rotation (`AngleAxis(yaw,up)·AngleAxis(20,right)` — explicit product, same matrix as the Euler form) + **THE ROTATION-INVARIANT SWEEP FIT** (the settle window observes only a partial arc before freezing, so the fit computes the swept cylinder analytically — aim recentered on the axis, horizontal half-extent = swept radius; the skeptic's HIGH: sweep the RAW envelope and blend RADII, never the pre-blended box — only the envelope union carries the every-yaw safety proof); levers `FolkloreRigTurntable`/`FolkloreTurntableDegPerSec`; honest diagonal trade (rotating creatures render slightly smaller).** → `studies/folklore-codex/RENDER-RIG.md` §RUNGS 2+3.** **Round 2 (playtest-driven, recaptured into this patch):** yaw 180→**0** (the guess showed the creature's back; 0 = the viewer's own default), **transparent clear** `(0,0,0,0)` replacing the opaque black box (the rung-5 probe pulled forward — field-look Cutout writes clean 0/1 alpha; retreat lever = `Color.black`), and portrait depth `textDepth−1` (=5): the census log MEASURED the pane (Shadow 3 / Body sheet 4 / body label 6 / Border 8 / Caption 9) and round 1's `textDepth−2` "safety margin" was an EXACT TIE with the Body sheet — the skeptic's feared instability, created by the margin itself. Ledger: awake 4ms · mint 7ms · frames 21/3/15. **Round 3 (same day, user-directed — "split the right panel like Equip"):** THE EQUIP-SENTENCE SPLIT — the single 780×880 detail pane becomes TWO stacked bordered windows via the file's own `BuildFramedPane` (both stock GenericInfoPanel skin, column footprint byte-preserved): TOP 780×500 portrait window (caption = entry name) over a 20-unit gap over BOTTOM 780×360 lore window (caption blank); portrait re-seated 680×400 == the resized RT (1:1); framing knobs `FolkloreRigCamDist −700` / `FolkloreRigAimY −80` (the round-2 snap: low-pivot model high in frame, legs clipping the narrow aspect); depth now scoped to the TOP pane's own Body sheet (+1) per the round-3 skeptic (the min-of-both-labels form coupled the panes). Recorded, not fixed: the column's right edge 800 vs the ±771 pillarbox comment (inherited from 16 approved rounds — a someday 4:3 check). Look-check pending. The s46 display window's core mechanism (`studies/folklore-codex/RENDER-RIG.md` = the build plan): when the detail pane shows an OWNED entry, a lazily-minted offstage rig renders ONE hardcoded creature (`GEO_MON_B3_118`, the Model Viewer's own test class) into the pane — stage GameObject at local (0,−10000,−10000) under the scene's own transform, layer 10 stamped recursively, an ALWAYS-DISABLED camera (`cullingMask 1<<10`, `depth −4096` parking, opaque-black SolidColor) driven by ONE manual `Render()` per entry-show into a persistent `RenderTexture(640,720,24)`, displayed through a runtime `NGUITools.AddWidget<UITexture>` seated with the pane's own SetAnchor(null)+pivot+SetRawRect idiom at depth `min(labelDepths)−2` (behind the lore text, above the sheet). No animation, no per-frame drive, no registry parse (`Entry.Display` stays unread until rung 3); locked entries render nothing (the spoiler convention). Fail-safe ladder: try/catch mint+render → `Log.Error` + text-only pane; a failed rig never retries within the visit. Built via a 9-agent ground→design→3-skeptic-verify→repair workflow; the verify round caught 2 BLOCKERs pre-compile (teardown deactivated-but-never-destroyed the portrait GameObject = one leaked widget per codex visit; mid-mint exceptions left every `this.folklore*` field null so the catch's teardown was a no-op = a permanent half-built-rig leak under the DontDestroyOnLoad tree) + the lazy-mint promise being defeated by `Show()`'s own synchronous `RefreshDetail()` (fixed: a one-frame coroutine defer for exactly the first mint) + `UIManager.OnLevelWasLoaded`'s direct `SetActive(false)` sweep bypassing `Hide()` (fixed: an `OnDisable()` teardown backstop) + the HonoBehavior-dispose branch destroying the wrong GameObject (fixed: dispose is an extra step, the ancestor `Destroy(stage)` is unconditional). Teardown destroys portrait GameObject + RT + stage whole, idempotent + exception-safe, called from Hide AND OnDisable. Grounding drift worth keeping: the viewer's pose is `Euler(20,0,0)` on the model + yaw on a SEPARATE drag-driven wrapper — the rig's single composed `Euler(20,180,0)` is a declared simplification, yaw not load-bearing; `SetActive(false)`-before-Destroy has NO in-tree precedent (a new discipline, labeled as such in-code). Gates: reverse `patch -R -p1 -F0` clean on live (TEXT mode); forward `-F0 --binary` onto the pre-round snapshot (`backups/preS46-snapshots.20260721`) == live bytes, zero fuzz; zero `\ No newline` markers so the PRE-MARKER CR RULE is trivially green. Rung-1 playtest checklist = RENDER-RIG.md §2 (creature framed right-side-up · clean open/close · the field+battle leak snaps · world-map open). **Rungs 2+3 (same day, one round, 12-agent Sonnet workflow `wf_746aa256`, 3-skeptic verified):** THE LIVING IDLE — viewer-recipe clip discovery re-implemented against the bare GEO name (the viewer's own is private), `AnimationCullingType.AlwaysAnimate` + `updateWhenOffscreen` (the tree has ZERO cullingType precedent; renderer culling would freeze an offstage never-seen model), the render pump in **LateUpdate** (legacy Animation samples between Update and LateUpdate — Update would render last frame's pose), the re-Play loop GATED on a non-empty clip list (the viewer's own un-gated block would spam `Play(\"\")` warnings); THE REGISTRY WIRE — `ResolveFolkloreDisplay` (`model:` scheme or colon-less shorthand; digits → `FF9BattleDB.GEO.TryGetValue` only, the indexer throws; `GetGEOID != -1`; `GEO_SUB_W0` refused), the mint split per-VISIT rig vs per-ENTRY `EnsureFolkloreModel`/`DestroyFolkloreModel` (dispose loop + unconditional Destroy, finally-cleared fields), per-entry failure isolation (a bad token degrades ONE entry text-only, the rig survives; `folkloreRigFailed` stays infrastructure-only), and AUTO-FRAMING from 8-corner bind-pose `sharedMesh.bounds` in stage space skipping DISABLED renderers (isBattle:false disables battle_model renderer components, not GameObjects), margin 1.2, degenerate → the round-3 proven pose re-roled as fallback; retreat lever `FolkloreRigLiveIdle=false` = rung-1 static behavior. Skeptics folded 1 BLOCKER (`ResolveFolkloreDisplay` un-guarded while `GetGEOID`→`Path.GetFileNameWithoutExtension` throws on `"`/`<`/`>`/`\|` in a hand-edited token) + 2 HIGH (the fade-window race — `UIScene.Hide` deactivates only after the fade, a pending defer coroutine could resurrect the rig post-teardown → the `folkloreClosing` flag; the lever not gating auto-framing). Gates re-run: reverse `-F0` TEXT clean on live; forward `-F0 --binary` onto preS46 == live bytes; 9 hunks. Kit rung 4 (display grammar) shipped the same session — 63 folklore tests, suite 3444/0. Playtest wiring: live FolklorePatch.txt = 80 small/83 proven-medium/84 large + 81 token-less + 82 garbage. |
| `s45-folklore-submenu.patch` | **THE FOLKLORE CODEX SUBMENU, Phase A (built + ★ IN-GAME GATE PASSED 2026-07-20; 3 files + 1 new: `Global/FolkloreUI.cs`).** The first-class main-menu entry for the Folklore codex (`studies/folklore-codex/SUBMENU.md` = the build-ready design; the data spine is the kit's DLL-free `[[folklore]]` P0). **Gated on `Memoria.ini [Folklore] Enabled` (default 0 = fail-safe vanilla — no row, no scene, no construction; the parity review verified bit-identical behavior when off, incl. IEEE-float equality of the generalized tween expressions).** What it does: `UIState.Folklore` APPENDED at ordinal 28 (`state`/`prevState` are serialized — a mid-enum insert would shift baked ordinals) + `IsUIStateMenu` widened; a `[NonSerialized] FolkloreScene` field (the WMWorld baked-layout law) constructed at RUNTIME in `UIManager.Start` (inactive GameObject on the UI layer + UIPanel depth 5 + AddComponent — a C# patch cannot add a serialized scene ref) + the `GetSceneFromState` case + the hand-maintained `OnLevelWasLoaded` sweep line; the **Folklore ROW** in `MainMenuUI.Awake` via the shipping Party-row runtime-clone precedent (Card → [Party] → Folklore → Config; nav splice symmetric in both Party configs; the generalized `SetRawRect` layout loop + `shiftFactor = 7/(7+extras)` + Config's `588+98·extras` shift); `FolkloreUI : UIScene` = the Phase-A SHELL (cloned `ScreenFadeGameObject` HonoFading; cloned `GenericInfoPanel` frame — child 2 IS the frame, the root is a bare container; the ItemUI return idiom incl. the **`StartSubmenuTweenIn()` in the Hide override** — omitting it leaves the row column collapsed for the session, the review's HIGH catch). Labels resolve via `Localization.GetWithDefault("Folklore"/"FolkloreHelp")` — ship rows in the mod folder's `StreamingAssets/Data/Text/LocalizationPatch.txt`. Also adds the csproj `AfterBuild` **`DWIXNoDeploy` condition** (compile-check without auto-deploying — pass `/p:DWIXNoDeploy=true`). The 4-lens adversarial review caught 4 real defects pre-deploy (the tween-in omission, the layer-0 culled scene root, the frame-stripping child loop, a help-toggle clobber) — all fixed; vanilla-parity lens fully clean. **Phase B (built over 16 playtest rounds, functional ★ 2026-07-21; +`ItemUI.cs`):** the real codex screen — LEFT = the KEY ITEMS LIST donor (`ItemScene.KeyItemListPanel` clone, name-only window-bar rows, forced 1-column via the engine's own `GOSubPanel.ChangeDims`; the donor's authentic `CaptionBackground` dressing (sheet + capped rails) resurrected with the CATEGORY name on the header rail; owned = white name + the stock `New!` bang (cleared by Confirm via `FF9Item_UseImportant`, save-persistent), locked = grey `???`) + RIGHT = a bordered detail window (frame caption = entry name, lore body ShrinkContent) + the stock `item_bg` backdrop + L1/R1 category paging + `FolkloreRegistry` (mod-root `FolklorePatch.txt`, `<keyItemId> <category> [displayRef]`, folders low→high override per id) + the ItemUI Key-Items filter keyed on the REGISTERED set. The 16 rounds minted the NGUI-menu law set (→ `~/.claude` memory `project-ff9-ngui-menu-construction` + `studies/folklore-codex/SUBMENU.md` §Phase B): clones must live in the DontDestroyOnLoad tree (an orphan itemPrefab dies on field load and RefreshPool wipes-then-throws forever); anchors re-assert EVERY frame (SetAnchor(null) or re-target every repositioned clone; the GenericInfoPanel FRAME child anchors to MAIN-MENU transforms — re-anchor it onto your pane root, the ControlPanel.InstantiatePanel recipe); window color = an imperative per-row `DisplayWindowBackground` sweep; separate UIPanels sort by PANEL depth; the finger renders in a pooled panel at per-group depth DEFAULT 5 (`SetPointerDepthToGroup`) and its limit rect reads `localPosition±size/2` in SCENE space; NGUI scroll state = panel transform + clipOffset counter-sliding (a clone carries the donor's arbitrary scroll state — force the SetDragAmount(0,0) FIXPOINT: clip at panel origin, content top-left on it); the cell TEMPLATE must sit in the table at the row-0 seat (spawned clones inherit its position and SetDragAmount caches bounds from them). **The Option B REDESIGN (★ IN-GAME PROVEN 2026-07-21 — BOTH open defects CLOSED):** the menu-shape-language study (`studies/menu-shape-language/VOCABULARY.md`) ratified the composition as the Chocograph sentence, and the rebuild minted **THE PRESERVE-THE-BAKE LAW** — capture the donor's solved geometry BEFORE mutation and COPY it: the scroll frame keeps the BAKED clip center (Phase B zeroed it — the rows-over-rail mechanism, with the formula-refit dressing canceling the error except at the rail); the dressing seats by `bakedDressingPos − bakedPanelPos − bakedClipOffset` — **THE SCROLL-SUM INVARIANT**: the live donor's three state values are individually wild (clipOffset −1930 on the pristine bake!) and only their SUM is the scroll-invariant window anchor (the 3-lens adversarial review's blocker, fixed pre-playtest and vindicated by the bake log); dressing children keep baked y/height/pivot/active with a width-only refit; `rowCount` DERIVED = round(bakedClipH/rowH) (the stock invariant inverted — absorbs the pre/post-widescreen clone-time variance). The first-open hang measured GONE (awake 4ms, first frames 8/14/18ms; instrumentation now also brackets the Invoke-deferred/LateUpdate window). L1/R1 = SFX **1047** with `ButtonGroupState.MuteActiveSound` held across the 2-frame settle (the rebuild re-seats the cursor and fires 103 on top otherwise; strand-insurance unmute in Hide since deactivation kills coroutines). NOTE: the `FolkloreUI.cs` csproj include is seated after `UIWidget.cs` (NOT beside s22's `Ff9mkDebugMenu.cs`) so the capture has no cross-patch adjacency. **The 9-row overflow fix (2026-07-21):** with the Party hack (`[Hacks] AllCharactersAvailable`) AND Folklore both on, 9 rows overflowed the command window's baked clip — Config drawn outside the window, collider mouse-unreachable. The s45 layout loop now derives its pitch from the row count against the proven 684-unit span (8 rows = Memoria's exact 79/78@−39 walk byte-faithfully; 9 rows = 69/68@−34; colliders track the widget h). **Same round, the row-children seat:** the shared `LayoutSubMenuRow` helper also runs `child.SetAnchor(row.transform)` on each row child (Label / Spliter / `dialog_hilight`) — the Transform overload keeps the baked rel/abs anchor values and executes OnAnchor immediately against the squeezed rect (both the stock Party block and the Folklore block). **And THE TITLE-PLATE COLLAPSE fix (the playtest's "misaligned card" — a stock Memoria bug on Form Party):** every sub-screen's top-right titled window IS the live collapsed command window (`HideAfterClip=0`; bake `TweenClipTo=(0,74)` / `TweenSpriteTo=(0,104)` sized to crop a 90-tall row, aimed by `ShiftContentClip=9+98·rowIndex`). A relaid list breaks both inputs (rows land ~5-11px high; a 69/79-tall row underfills the clip → neighbor slivers). All 8 collapse sites now call `CollapseSubmenuTo(row)` — shift `= 9 + (bakedItemRowY − rowY)` from the row's CURRENT seat, landing the row's center on the collapsed clip's center per the bake's own anchor law; stock `9+98k` bit-for-bit on the untouched vanilla bake; the `shiftFactor` approximation is deleted. The plate keeps its BAKED 74/104 minima (scaling them moves the collapsed clip center off the landing slot — a measured ~8px misalign, reverted). And the Party/Folklore descends set the collapsed state via `ResetToMinimum()` directly (`collapseInstantly` consumed in Hide) — their Show paths kill the collapse coroutine mid-flight, which is why stock Memoria's Form Party title plate freezes half-collapsed; stock screens keep the stock tween. Root-caused offline by dumping the baked UI scene (level2 via UnityPy + TypeTreeGeneratorAPI). Recapture gates (re-run same day): GNU `patch -R -p1 -F0` clean on live in **TEXT mode** + forward `-F0 --binary` clean on a fresh `base+s22..s44` replay, all 5 files byte-identical to live, zero fuzz/offsets — **the forward gate is now TRULY binary-exact: the old "phantom fuzz-1" was a real byte mismatch** (a no-EOL final line has no CR, so the CRLF patch's line before a `\ No newline` marker must be CR-less; the OLD capture failed forward `--binary` identically when re-run — its "clean" record never reproduced). The display window (render rig) = s46. |

## Performance / quality — shipped in the bundle, not a fidelity patch

| File | What it fixes |
|---|---|
| `s35-overlay-texture-cache.patch` | An overlay-PNG **decode cache** for pure-`.bgx` scenes. `BGSCENE_DEF.ProcessMemoriaOverlay` builds a fresh `BGSCENE_DEF` per field load and re-decodes every overlay PNG from disc on EVERY (re)entry + battle-return — a slow, visibly see-through fade. Adds a `static Dictionary<String, Texture2D> MemoriaOverlayTextureCache`; the `Image` overlay op reuses the decoded `Texture2D` by resolved path (the static reference also keeps the textures alive across the battle scene change, so `Resources.UnloadUnusedAssets` won't free them). Self-heals: a destroyed texture reads Unity-null and reloads. Identity-safe (same pixels, just cached) → smoother field loads + battle returns. Was `deferred-overlay-texture-cache.patch`; promoted 2026-07-07. ★ **IN-GAME PROVEN 2026-07-07** — the slow see-through overlay fade on field re-entry / battle-return is gone. Built into the full custom engine (stock + s22–s34 + s35), deployed x64+x86 (DLLs backed up `preCACHE.20260707-221642`). |

## Dev tooling — shipped in the bundle, but NOT a fidelity patch

| File | What it is |
|---|---|
| `s43-debug-menu-ux-tilde.patch` | **The hotkey remap + UX round (2026-07-20).** The debug menu moves **F6 → `~` (tilde/BackQuote)** — stock Memoria already binds F6 as the **LvMax cheat** hotkey (`HandleBoosterButton`), which the old early intercept swallowed; BackQuote is bound to nothing engine-wide. Unlike F6 the key TYPES a character, so the toggle is suppressed while the menu is open with a text box focused (`GUIUtility.keyboardControl != 0` — a closed menu skips the guard, and `SetOpen(false)` clears stale focus). UX pass in the same round: one prefs row (Disable control + Font), tab switches reset the scroll, the diorama bench moved below the everyday Go actions, boxed accent status bar, bold section headers, full-color readouts, whole-header drag, a "~ toggles" header hint, ASCII-safe preset star + batch-hint glyphs (the FF9 fonts lack `·`), and the batch "bad index" error now prints the op grammar (re-landing the fix a 2026-07-16 regen silently dropped). Two files: `UIKeyTrigger.cs` (line-count-neutral, so s37/s38 hunks stay put) + `Ff9mkDebugMenu.cs`. Gate: `base + s22..s41 + s43` applies at zero fuzz/artifacts and reproduces live byte-exactly. **s42 CAPTURED 2026-07-20** (`s42-netsync-config-split.patch`, see its row) — this round's one-string overlay edit in `NetSyncClient.cs` ("meet your friend: ~ menu > Warp") rode with it as planned. |
| `s22-debug-menu-f6.patch` | The in-game debug menu (Warp / Move / Cheats / Flags / Time, in field + battle; **hotkey since remapped F6 → `~` by s43**) **plus a `World` tab on the overworld**: live pos/cell/block/topograph/disc/vehicle readout, vehicle-mode swap (`gEventGlobal[190]` + `w_movementChange`), disc 1↔4 switch, a Disable-control toggle, and a working **teleport** (the fix defeats Memoria's `SmoothFrameUpdater_World` reverter via `Skip`, see `../ff9mapkit/docs/OVERWORLD_ENGINE.md`). The Flags tab has a **Batch** box: many bit/byte/word writes in one click (`1520 !1423 b191=5 w0=7200`), all-or-nothing validated, with the live text + named one-click presets persisted via PlayerPrefs — multi-flag warp-in recipes (e.g. the Chocobo Hot & Cold one) stop being seven manual round-trips. The Go tab also has a **Reload + anims** button beside Reload field: it clears the static `AnimationClipReader.LoadedClips` cache before reloading so a re-deployed loose `.anim` (the model animation-edit loop) shows without a full relaunch — Reload field alone keeps the cached clip. A tester convenience; the beta bundle includes it, but it is not part of the fork-fidelity set or the upstream-candidate set. |

## ✅ STACK HEALTH — the stack replays END TO END again (2026-07-16; s43 verified on top 2026-07-20)

> **2026-07-20 status:** `base + s22..s41 + s43` replays at zero fuzz / zero rejects / zero `.orig`, and the
> s43-owned files (`UIKeyTrigger.cs`, `Ff9mkDebugMenu.cs`) come out byte-identical to live. **s42 is now
> CAPTURED** (`s42-netsync-config-split.patch`, same day): with it in sequence, `NetSyncClient.cs` reproduces
> live byte-exactly (gated via a single-file lineage replay — s36/s37/s40/s41's `NetSyncClient.cs` hunks +
> s42 == live bytes; reverse dry-run `-F0` clean on live). **s44 CAPTURED same day** (the F1 field-co-op
> round, 9 files incl. the new `NetSyncField.cs`): gated via the live-minus-the-change sidestep — forward
> `git apply` onto the pre-round snapshots (`backups/preF1-snapshots.20260720`, taken BEFORE any F1 edit)
> == live bytes on all 9; reverse `-F0` clean on live; forward GNU dry-run clean on the pre-tree.
> (`NetSyncField.cs` was LF→CRLF normalized with the emit, the s40 convention.) No uncaptured netsync
> work remains.

**`base + s22..s41` applies at ZERO fuzz, ZERO rejects, ZERO `.orig`** under the documented
(s40 emitted + gated 2026-07-16: fresh full replay reproduces the live tree byte-exactly except the
FieldMapActorController exclusion below; reverse dry-run clean. s41 emitted + gated same day via
the live-minus-the-change sidestep: forward `git apply` onto the pre-round snapshots == live bytes
on all 6 files; GNU `patch -R -p1 --dry-run -F0` clean on live; forward dry-run clean on the
pre-tree.) Original round:
`patch -p1 -F0 -s -f --binary`. The whole stack reaches the end — the first time it has.

**THE MODEL IS DECIDED: `s12`/`s18`/`s21` are DEAD — never applied.** (User call, 2026-07-16.) They are
three early drafts of one feature: s18 = "F6 reloads the field" (in `FieldMap.cs`); s21 = the same idea moved
to `UIKeyTrigger.cs` because the first spot missed keypresses, plus F10 = reset flags; **s22 = the finished
tabbed F6 menu that replaced both**. `s12` is an unrelated grab-bag (an overlay texture cache that later
*became* `s35`, a retired New-Game debug shortcut, and the `Attack9999` auto-cheat).

Why the question existed at all: later patches carried instructions to **undo** them (s37 removed s21's F6/F10
block and s12's `Attack9999`), so the stack added code and then took it back out — **the engine is identical
either way**, and the only question was whether the recipe takes the detour. Dead wins on evidence: applying
s12 **collides with s35** (it still contains all 14 of s35's lines byte-for-byte), it would resurrect the
retired New-Game debug, and nothing in s22 removes s18's F6 hotkey (so the raw hotkey and the menu would both
grab F6). The fix was to strip the orphaned removals from s37 — done, and that is what closed the stack.

⚠ **Consequence — do not "restore" s12/s18/s21 casually.** They are kept for the build record ONLY. The live
set no longer contains anything that removes their code, so applying one now injects code nothing takes out.

Two rounds got here (below): round 1 (during `s38`) found the drift; round 2 (2026-07-16) captured every
*stable* gap, corrected three of round 1's diagnoses, and stripped s37.

**REPAIRED — s22 was contaminated.** Its csproj hunk added `Memoria\Netsync\NetSyncClient.cs` and
`NetSyncSocket.cs` — s36's lines, in the F6-debug-menu patch (leaked in by an earlier regen emitted
against a live tree that already had s36). s36 adds them too, so applying s22→s36 collided and **the
live set had never replayed cleanly from base**. The two duplicate lines were removed from s22; the
stack now replays **s22..s36 at ZERO fuzz, zero rejects**. ⚠ Note `s22` *still* carries
`Memoria\World\WorldMeshOverride.cs` (s34's line) — **left in place deliberately: it is the ONLY patch
adding that Compile line**, so removing it without moving it into s34 breaks the build. Misattributed,
load-bearing, flagged.

**REPAIRED — an EOL defect in the live tree.** `EventEngine.DoEventCode.cs` was **LF** while the repo
checks out **CRLF** (`core.autocrlf=true`) — a past text-mode `patch` application rewrote it. Converted
back to CRLF (pure EOL, delta = exactly the line count, zero content change); its diff vs the
reconstructed baseline dropped from a phantom **6943-line rewrite to a real 152/138**. **LAW: apply
these patches with `patch --binary`** — plain text mode silently rewrites CRLF files as LF and seeds
exactly this drift.

**RECAPTURED (2026-07-15, round 2) — the stable drift is now in the stack.** Every file the round-1 note
listed as uncaptured/stale is captured. `base + s22..s36 + s39` replays under the documented
`patch -p1 -F0 -s -f --binary` at **zero fuzz, zero rejects, zero `.orig`**, and the four files below are
**byte-identical to live** (`cmp`):

| File | Was | Now |
|---|---|---|
| `Global/UI/UIKey/Ff9mkDebugMenu.cs` | "live ~2.7 KB smaller, s22 stale" | **s22 regenerated** — real drift was 54 add / 4 del lines |
| `Global/WM/WMWorld/WMWorld.cs` | custom-ocean loose-texture hook, uncaptured | **s34 regenerated** — hook captured |
| `Memoria/World/WorldMeshOverride.cs` | "live ~1.7 KB larger, s34 stale" | **s34 regenerated** — `TryLoadTexture` (35 lines) captured |
| `Global/ff9/ff9.cs` | `w_worldSelfHealControl`, uncaptured | **`s39-world-selfheal-control.patch`** (NEW) — 86 lines / 3 hunks |

Three of round 1's findings were **misdiagnoses, and all three had the same root**:

- **The KB deltas were the EOL trap this very section warns about.** Live's `Ff9mkDebugMenu.cs` was LF:
  2453 lines → 2453 missing CR bytes ≈ the "2.7 KB smaller" (and the sign pointed the wrong way). Same for
  `WorldMeshOverride.cs`. Real content drift was 54/4 and 35/0 lines. **LAW: never size-compare across an
  EOL boundary — normalize first, then diff.**
- **`DataPatchers.cs.orig` was never a leftover to delete.** It is not in the live tree. GNU patch's
  default is `--backup-if-mismatch`, so *any* hunk applying at an offset writes a `.orig` — and s33's
  DataPatchers hunk #2 applied at **offset 17** (authored before s24/s31 added lines above it). The `.orig`
  was **regenerated on every replay**. s33 is now re-based (change-content identical, line numbers only) and
  applies at zero offset, so the artifact is gone at the root.
- **`s24` was never stale.** A first cut diffed `ff9.cs` against `HEAD` (the BASE commit) instead of the
  s24-applied state, pulling s24's own `ForkSiblingField` redirect into the diff. s24 already carries it.
  **LAW: a mid-stack patch must be diffed against ITS stack position, not `HEAD`.**

**The EOL defect is a CLASS, not one file.** Every **untracked** file in the live tree is LF; every tracked
one is CRLF. `autocrlf` normalizes tracked files on checkout but never touches untracked ones — so the files
a patch **creates** are exactly the ones a past text-mode `patch` run left LF. `Ff9mkDebugMenu.cs` and
`WorldMeshOverride.cs` were converted back to CRLF (pure EOL: delta == line count exactly, content
byte-identical). ⚠ **Still LF, deliberately untouched:** all 8 `Memoria/Netsync/*.cs` — they belong to the
deferred s36/s37 regen below. Fix them in that round, not by hand.

**Gate correction.** "Reverse-dry-run against live" is only valid for the **top** of the stack (s38). A
mid-stack patch cannot reverse against live — later patches sit on top of its files. The correct gates for a
mid-stack patch are: forward-applies at its stack position at zero fuzz; its files are `cmp`-identical to
live after a full replay; and forward+reverse returns the tree to its pre-patch state.

**RESOLVED — `s37` now applies.** It had failed on 3 files because it carried **orphaned removals** of
s12/s21 code that a clean replay never adds. Stripped (`SettingsState` −6→−1, `UIKeyTrigger` −34→−0, csproj
−2→−0); everything s37 owns is untouched (all four new-file bodies byte-identical). Proof the removals were
no-ops: with only its real `SettingsState` hunk applied, the file is **already byte-identical to live**. The
regen also shed a **phantom near-total rewrite** of the Netsync files (`NetSyncClient` `+1048/−652` on a 1052-line
file → `+419/−23`): s37 had been diffed against the LF live tree while s36 produces CRLF. Third instance of
that trap. Round 1 never caught any of this — it only ever verified `s22..s36`.

**OPEN — deliberately deferred, not forgotten:**
| Item | Why deferred |
|---|---|
| the `s22`→`s34` csproj line move | `Assembly-CSharp.csproj` is being actively written by the in-flight B3 diorama work. s22 still carries s34's `WorldMeshOverride.cs` Compile line — misattributed, load-bearing, flagged (unchanged from round 1). |
| ~~`SetControlToLeader` / `DebugClearControl` live in **s37**~~ | **RESOLVED 2026-07-16 — moved to `s22`.** They are *called* by s22 (F6 menu) and s39 (self-heal) but were *defined* in the battle-co-op patch. Folded into s22 (the earliest consumer, where `great-williams` also places them); s24/s29/s33/s37 regenerated to rebase their EventEngine.cs line numbers. `base + s22..s41` still reproduces live byte-for-byte (EventEngine.cs `cmp`-identical); a partial stack stopping before s37 now compiles. |
| ~~B3 diorama files~~ | **RESOLVED 2026-07-16 — captured as `s40-netsync-diorama.patch`** (23 files incl. the new `NetSyncDiorama.cs` + the csproj line; both gates pass). |
| ~~the EOL-only `Memoria/Netsync/*.cs`~~ | **RESOLVED 2026-07-16 — all 8 live Netsync files normalized LF→CRLF with the s40 emit** (pure EOL, content byte-identical; the deployed DLL predates the normalization — same IL). |
| ~~`Global/Field/Map/Actor/FieldMapActorController.cs`~~ | **✅ RESOLVED + IN-GAME PROVEN 2026-07-18.** The known regression (live had been reverted **wholesale to stock**, losing all s24/s29 gates — Iifa 1751, Dali 404, Prima Vista 205/Steiner, L.Castle 2150, Treno 900, off-mesh 2504/105/2605, Oeilvert 2207). User green-lit the restore: the s24/s29 sections were applied to the live tree (stock backed up), rebuilt+deployed, and A/B-proven via a verbatim fork of field **105** (softlock severity) in an isolated slot — **softlocked pre-fix, played after**. Identity-safe: base already handled the real ids with raw `fldMapNo`; the gates only wrap them in `EffectiveFieldId(...)` so forks inherit it (vanilla was never affected — which is why it hid). |

**The exact residue** (measured on a full clean replay `base + s22..s39` — every entry attributed, nothing
unexplained):

| Residue | Nature |
|---|---|
| `NetSyncBattle/Party/Relay/Socket/State/Visitor.cs` | **EOL-only** (live LF vs patch CRLF) — content identical |
| the csproj `Diorama` line, `NetSyncClient.cs` (+4), `NetSyncDiorama.cs`, `btl_sys`/`btl_scrp`/`btl_stat`, `BattleActionCode.cs` | in-flight B3 diorama |
| `FieldMapActorController.cs` | user decision (above) |

**Everything else — every file the live set owns — is byte-identical to live.**

Two things this measurement settled: `SettingsState.cs` **converges** (s37's stale removals are no-ops — the
content isn't there to remove, so the end state matches live), and `EventEngine.cs` is **fully s37-owned**
(byte-identical once s37 applies) — verified, not assumed from the filename.

✅ **A misattribution RESOLVED (2026-07-16).** `EventEngine.SetControlToLeader` / `DebugClearControl` were
**defined in s37** but *called* by **s22** (the F6 menu's control-restore) and **s39** (the self-heal) —
dev-tooling and overworld depending on the *battle-co-op* patch for a symbol definition, the same leak class as
s22 carrying s34's csproj line. **Now defined in s22**, the earliest consumer (where `great-williams` also
placed them). Because the definitions move ~39 lines earlier in `EventEngine.cs`, the four other patches that
edit that file between s22 and s37 — **s24, s29, s33, s37** — were regenerated to rebase their hunk line
numbers (change-content byte-identical, only `@@` headers moved; s37 additionally drops the definition hunk).
A fresh `base + s22..s41` replay stays clean and `EventEngine.cs` is `cmp`-identical to live; a partial stack
that stops before s37 now compiles.

Consequence: **a from-patches rebuild now reproduces the live tree** for everything the live set owns. The only
gaps left are the in-flight B3 work, the EOL-only class, and the `FieldMapActorController` decision.
→ `project-ff9-memoria-conflict-forensics`.

⚠⚠ **PRIOR ART — an unmerged branch solved this with an INCOMPATIBLE stack model. Reconcile before merging
either.** `claude/great-williams-4052f0` (commits `48e27cd`, `708b6ff`, `1bdb4b9`, `d0d8e46`, `856f4ea`;
recorded in `project-ff9-memoria-conflict-forensics`) ran its own drift census and captured the SAME two
features — but under the **older stack model where `s12`/`s18`/`s21` are LIVE**, not superseded:

| | `great-williams` (unmerged) | this branch (the s38-era model) |
|---|---|---|
| replay baseline | `s12..s37` — s12/s18/s21 **applied** | `s22..s39` — s12/s18/s21 **superseded, not applied** |
| the self-heal | folded into **s22** (+ `EventEngine`/`FieldMap`) | its own **s39** |
| the texture override | folded into **s34** | folded into **s34** (same call) |
| s18's `FieldMap.cs` hotkey block | s22 carries a **removal** of it | never applied, so nothing to remove |
| live-tree EOL | **accept LF**, keep gates EOL-agnostic | **repair to CRLF**, gate byte-exact (`cmp`) |

**RESOLVED 2026-07-16 — the s22+ model won (user call); `great-williams` is SUPERSEDED.** Measured, not
argued: replaying `great-williams`' own `s12..s37` from pristine base gets to **s35 and then fails s36+s37 on
the csproj** — its s22 still adds the duplicate `NetSyncClient`/`NetSyncSocket` lines (the contamination the
s38 round repaired), which breaks s36's anchor. Its "applies cleanly" note was verified with `git apply`, not
the stack's documented `patch --binary`, and does not reproduce. Meanwhile the s22+ model now replays
`s22..s39` end to end.

⚠ **Do not merge `great-williams`.** It predates `s38` and does not contain it, and its s22 would REINTRODUCE
the csproj duplicates. **Port, don't merge** — the one thing it gets right is putting
`SetControlToLeader`/`DebugClearControl` in **s22** (see the misattribution note above). Its self-heal and
texture captures are already carried here (s39 / s34).

## Superseded / historical — kept for the build record, do NOT apply

Earlier dev-iteration patches, retained so the engine history stays legible. They are superseded by the
live set above (the F6 menu replaced the single-key reload/reset hotkeys).

| File | Superseded by |
|---|---|
| `s12-engine-edits.patch` | early scratch; obsolete |
| `s18-field-reload-hotkey.patch` | `s22` (F6 → Reload field) |
| `s21-dev-hotkeys-f6-f10.patch` | `s22` (the tabbed F6 menu) |

## Upstream candidates & optional polish — NOT in the bundle

| File | What it is |
|---|---|
| `upstream/fieldcreator-png-export-path.patch` (+ `upstream/UPSTREAM.md`) | A standalone Memoria bug fix (FieldCreatorScene PNG export path). Not required by the kit's CLI pipeline; offered upstream. |

---

> **License / provenance.** These patches modify MIT-licensed Memoria (© Albeoris) and contain **zero**
> Final Fantasy IX game data. The compiled bundle is likewise MIT (© Albeoris) plus the Dream World IX
> patches. See [`../ff9mapkit/docs/PROVENANCE.md`](../ff9mapkit/docs/PROVENANCE.md).
