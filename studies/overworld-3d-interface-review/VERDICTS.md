========================================================================================
CLUSTER offline-renderer [confirmed, impact 4, medium, conf high] — Promote the engine-faithful offline renderer into a first-class kit gate
REC: Promote the render gate to `ff9mapkit/ff9mapkit/world/render.py` as a LIBRARY MODULE, following the `world/meshedit.py` precedent exactly (landed 2026-08-02, 50KB, 26 hermetic synthetic-geometry tests, no CLI verb). This is a DELETION of surface, not an addition: it replaces 25 per-study rasterizers with one.

WHY IT IS CHEAP (stronger than either expert argued): render_gate.py already runs entirely on kit APIs — `A.load_atlas` (atlas.py:187) at render_gate.py:101, `M.blockmesh_from_ff9mesh` (mesh.py:122) at :141, `X.read_block`/`X.block_world_origin`/`X.CH_POS`/`X.CH_UV` at :142-143,402-407. There is no new math to write. And it has ALREADY earned library status at the wrong address: `terrain_gate.py:50` does `import render_gate as RG` and monkeypatches `RG.state_src` at :87, and atlas_map.py:24, probe_blank_paint.py:24, probe_cove_cam.py:16, probe_peer_compare.py:24, probe_seam_owner.py:18 all import it. It is load-bearing infrastructure living in studies/.

THE FOUR PIECES OF WORK, in order:
1. De-hardcode. The eight module constants at render_gate.py:47-63 (GAME/MOD/DISC/CELLS/BLOCK/OUTD/BK/TEXDIR) plus cmd_flow's hardcoded stock reference block at :402 ((5,14), disc=1) become arguments of one `render_blocks(...)` entry point. Keep the bench's current values as a named `BENCH_VSHORE` preset so terrain_gate keeps working.
2. Camera derivation is the only genuinely new part, and it is the part that decides whether the instrument sees anything — do not treat it as parameterization. The arc's own record (RENDER-GATE.md:124-135) is that the four mid-range cameras MISSED both playtest-8 residuals and `owner_close`/`graze` were hand-placed to match the owner's real vantage. So an `--around WX,WZ` rig must reproduce the two vantage CLASSES by construction — near-top-down (~20u above the surface, ~60° pitch onto the target) and low offshore graze (~3u, near edge-on to the waterline) — derived from the target's own bounds, not a generic bounds-fit. Register that as a falsifiable prediction: the auto-rig at the V-shore corner must reproduce the playtest-8 classes the hand-placed cameras caught.
3. Hermetic tests (`tests/test_world_render.py`) on synthetic geometry with an injected atlas, per the meshedit design — no install, no extracted templates, so they RUN in a fresh worktree instead of silently skipping (the worktree skip trap). Cover: determinism (identical inputs → 0 px, the existing P-E gate), backface cull handedness, alpha-0→white, NEAREST sampling, and the auto-rig's two vantage classes.
4. Expose ONE verb, `world-render --around WX,WZ [--preset NAME] [--out DIR]`. Do NOT wire it as a default side effect of `mesh.deploy_override` (mesh.py:176, the one write seam — 33 call sites across cli.py, island.py, terrain.py, transplant.py, water.py, interior.py, entrance.py, discmirror.py, blendio.py). An opt-in `--render` flag on the geometry writers is acceptable later; a default-on ~40s render on every deploy is not.

TWO GUARDRAILS, both mandatory:
- Carry the blind-spot ledger (RENDER-GATE.md:61-69) verbatim into the module docstring AND into the CLI's printed output. A clean render is a regression harness, never an oracle — CLAUDE.md §7, and the 0-of-13 record.
- Report the pixel diff, do NOT fail the build on it. WARN-by-default with an opt-in `--enforce` refusal, matching `wang_carry_gate`.

EXPLICITLY OUT OF SCOPE for this arc: the Workspace World-tab details strip, and any golden-image build gate.
FIRST STEP: Copy `studies/path-d-new-world/render_gate.py` to `ff9mapkit/ff9mapkit/world/render.py` and turn the module constants at render_gate.py:47-63 plus the hardcoded stock reference block at render_gate.py:402 into arguments of a single `render_blocks(...)` entry point, with the bench's current values retained as a `BENCH_VSHORE` preset. Then reduce `studies/path-d-new-world/render_gate.py` to a thin shim that imports the new module and supplies that preset, and run `py studies/path-d-new-world/terrain_gate.py live` — it does `import render_gate as RG` at :50 and monkeypatches `RG.state_src` at :87, so it exercises the whole seam. ACCEPTANCE: the six committed cameras render 0-px-diff against the pre-move output (the P-E determinism gate the instrument already ships) and all 10 terrain gates stay green. Nothing else moves until that diff is zero.
========================================================================================
CLUSTER walkability-oracle [plausible, impact 4, small, conf high] — Two divergent ground-query implementations; the calibrated movement oracle gates nothing
REC: The merged theme is real but both members mis-locate its centre of mass. The defensible claim, stated tightly: **the kit's only shipped walkability gate returns first-hit-or-MISS, so it is structurally incapable of seeing the one defect class in this arc that carries a playtest receipt — a walkable sheet under a walkable sheet — and that class is STATIC, not lifecycle-dependent.** BENCH-WALK-SIM.md's own findings settle the mechanism: "the lawn is buffer-EARLIER everywhere it survives (pin: lawn tri#124 vs carried tri#395), so even a cold full scan grounds under the hill: the defect is STATIC, not history-dependent." The instrument that reproduced the owner's pin at 0.00u with a clean pristine control was `walk_sim.all_sheets` plus the stacked-sheet census (~60 lines, no ring, no fan, no two-probe commit). The 421-line movement engine was confirmation, and when it was run island-wide as `coast_lint` it produced zero real defects and three false accusations against owner-accepted shore.

So: **do not promote walk_sim.py. Promote `all_sheets` and the stacked-sheet verdict into `ff9mapkit/ff9mapkit/world/placement.py`, and make the three existing census gates fail on it.** Concretely, in one session:

1. Add `all_sheets(meshlist, x, z, *, strict=True)` to `placement.py` — every full-scan-filter-passing intersection on the vertical line in scan order, deduped by y (the 0.02 epsilon), lifted verbatim from `walk_sim.py:227-248`. Add `WALK_OK` (the on-foot topograph mask, `walk_sim.py:44-45`) as a module constant next to `IDALL_SKIP`.
2. Extend `census()` (`placement.py:81-99`) with two new report keys computed from `all_sheets`: `stacked` (points with >= 2 walkable sheets, carrying the gap and whether first-in-scan-order != topmost) and `inversions`. Leave `miss` and `points` byte-identical so today's three callers are unaffected until they opt in.
3. Turn the new keys into a hard gate at the three sites that already gate on `miss`: `interior.py:2458` (`census_gate`, raise the same `ValueError`), `island.py:806`, `transplant.py:2936`. Transplant needs the same donor-inheritance escape hatch `miss` already has (`transplant.py:2937-2969`) — an INHERITED stack in the donor is not an introduced defect.
4. Add the 0x31EE veto to `place()` as a mesh-abandon (`walk_sim.py:159-161` semantics), and make `place()` REFUSE — not silently mis-order — when handed a meshlist for a registration-order exception block: a `REGISTRATION_ORDER_EXCEPTIONS = {219: [...], <volcano blocks>: [...]}` table sourced from project-ff9-overworld-placement-rules:42-45, with a raise on an unmodelled order. Refusal is cheaper than modelling and cannot go quietly wrong.
5. Tests go in `ff9mapkit/tests/test_world_island.py` beside the six that already cover `place` (:53-103), not a new file: a synthetic two-sheet fixture where the lower sheet is buffer-earlier must gate RED; a single-sheet fixture must stay GREEN; a 0x31EE tri must abandon its mesh. Keep them hermetic so they cannot join the 479 silently-skipped install-gated tests (project-ff9-test-suite-perf).

Explicitly OUT of scope, and say so in the report: the ring cache, the deflection fan, the two-probe commit, float32 barycentrics, and any auto-run boundary sweep wired into the coast writers. `walk_sim.py` and `coast_lint.py` stay in `studies/` as the calibrated Path-D bench instruments they are — the only change worth making there is deleting their duplicate `IDALL_SKIP`/`bary_y`/`up_ny` declarations in favour of importing the kit's, and only if `walk_sim.main()`'s four calibration gates re-run 4/4 PASS afterwards (the pristine control at `C:\gd\Dream-World-IX\backups\terrace-strip-prewall.20260731-220001` is present, so that re-run is possible today).
FIRST STEP: Open `ff9mapkit/ff9mapkit/world/placement.py` and `studies/path-d-new-world/walk_sim.py:227-248` side by side; port `all_sheets` (plus the `WALK_OK` mask from walk_sim.py:44-45) into placement.py, then write the failing test first in `ff9mapkit/tests/test_world_island.py` — a synthetic BlockMesh via the file's own `_bm()` helper with a walkable lawn tri emitted BEFORE a walkable carried tri 1.5u above it at the same plan point, asserting the new `census()["stacked"]` is non-empty. That single red test is the whole finding in executable form.
========================================================================================
CLUSTER census-blindness [confirmed, impact 4, small, conf high] — Placement census sampling is blind to small holes, block seams, and gates against an uncut Sea4
REC: The merged theme is real but it is NOT "the kit lacks coverage validation." The kit has FOUR proven coverage instruments, each written after a real in-game defect — an analytic once-edge closed-surface audit (island.py:714-739; interior.py:2107-2187, baseline-subtracted), a differential no-INTRODUCED-misses law (transplant.py:2937-2959), a 0.0005u/0.5u seam micro-census (transplant.py:3667-3691), and an order-preserving spatial index (coastnav.py:114-134). None of them live in the shared primitive, so whichever lane calls the bare `P.census(meshlist)` inherits the weakest possible form. Fix the one gate that is actually dead, then consolidate; author no new gate class.

STEP 1 — kill the unfireable gate (~10 lines + 1 test). In `interior.census_gate` (interior.py:2443-2470) replace `sea = dataclasses.replace(plane, x=bx, y=by, ...)` at :2454 with `island._cut_plane(plane, bx, by, frozenset(), bm)` — the identical construction `island._meshlist` already uses at island.py:784 and `island.deploy` at :1018 — so the gate scores the configuration that ships. Then add the falsification test that does not exist: `ff9mapkit/tests/test_world_interior.py::test_census_gate_raises_on_a_punched_terrain_hole` — take the existing carve fixture, delete one 4u cell's tris from a `res["changed"]` BlockMesh, assert `census_gate` RAISES. Park it beside `test_sea4_under_land.py::test_uncut_plane_is_boat_permeable` as its negative control. This matters beyond the gate: `studies/overworld-topography/rung_e_build.py:33` explicitly banks "census_gate did not raise (proves EVERY internal condition held)" as one of its two independent pass signals, so a dead predicate is already laundering into study ledgers.

STEP 2 — make density affordable (~20 lines). Lift `coastnav._build_grid` into `placement.py` as `build_index(bm)` and add an optional `index=` fast path to `place()`. Its docstring already carries the correctness proof (AABB buckets hold ascending tri indices, so a bucket scan returns the same file-order first hit), and `extract.py:250` guarantees `bm.tris[t] == flat_index[3t:3t+3]`, so tri indices map 1:1. Gate it with a test asserting indexed and unindexed `place()` agree at every sample of a real block — it is an accelerator, never a semantic change.

STEP 3 — seam + differential, in that order. Add `census(..., seam=neighbours)` that lifts transplant.py:3667-3691 verbatim: 0.0005u probe pairs at 0.5u stride along each shared block plane, each side resolved against its OWN containing block's meshlist (the engine raycasts only the containing block, so per-side MISS is the predicate — "the two sides disagree" is not a defect). Return `seam_miss` separately so `island.py:815-819`'s `clean` predicate can adopt it without changing meaning. Then promote transplant.py:2937-2959 into `placement.census_delta(built, reference)` and adopt at island.py:806 and interior.py:2458. Only after that may the interior pitch rise: absolutely, at samples=61, four Terrain-complete stock blocks — (6,11),(7,2),(13,5),(14,5) — go red on their own bytes, so a finer pitch without the differential manufactures false positives.

Finally, change placement.py:24-25's "MISS must be 0 everywhere in the cell" to state the measured detection floor (a one-triangle hole is missed ~25% of the time at the default pitch). A law in a docstring is a wish.
FIRST STEP: In ff9mapkit/ff9mapkit/world/interior.py:2454, swap `dataclasses.replace(plane, x=bx, y=by, name=...)` for `island._cut_plane(plane, bx, by, frozenset(), bm)`, then add `test_census_gate_raises_on_a_punched_terrain_hole` to ff9mapkit/tests/test_world_interior.py (delete one 4u cell's tris from a carved block and assert census_gate raises) and confirm it fails before the swap and passes after.
========================================================================================
CLUSTER gate-wiring [plausible, impact 3, small, conf high] — Built gates are not wired at the chokepoints; warn-by-default prints CLEAN over defects
REC: The merged theme is real but it is a HONESTY defect, not a coverage defect. Do not build `world/gates.py`. This project's own record says a green suite predicted 0 of 13 verdicts and that authored surface is where defects land — a new battery wired into seven builders mints exactly the surface that law tells you not to mint, and `procedural-content#4`'s own "WARN-by-default everywhere" would deepen the false-green `validation-qa#3` proves. Ship the cheap, surface-free tranche and stop:

1. THE HONEST HEADLINE (the whole cluster's real payload). In `ff9mapkit/ff9mapkit/world/texgates.py:202-212` add `d["status"] = "fail" if (dirty and not allow and enforce) else ("warn" if d["warn"] else "pass")` alongside the existing `ok` (keep `ok` — `transplant.py:2975` and `island.py:815` both consume it, do not change deploy behaviour). Then in `ff9mapkit/ff9mapkit/cli.py` replace the two lying lines: `:4458` becomes `dry run: gates {n_warn} WARN / 0 FAIL — …` when any gate row has `warn`, and `:4609` (the island path, which prints "all gates CLEAN" unconditionally after its own warning loop at `:4595-4599`) gets the same derivation. Gate it with a test in `ff9mapkit/tests/` that builds a summary carrying one warn row and asserts the string "CLEAN" is absent from stdout — per [[feedback-a-check-that-cannot-fail]], break it once to prove it fires.

2. PROMOTE THE TWO CALIBRATED GATES. `zero_uv_area_gate` and `family_rect_gate` default `enforce=True` (`texgates.py:218,299`); `--enforce-texture-gates` (`cli.py:8281`) becomes `--allow-texture-gates`-only escape. Justification is already published at `texgates.py:56-57`: 1.000 clean on ten stock blocks, 2017/2017 mains tris — they cannot false-positive a verbatim carry. Leave `one_window_gate` and `sea_plan_gate` at warn (their false-positive rate is measured non-zero, 3.2–18.5% on stock).

3. FIX THE OVERCLAIMING DOCSTRING at `transplant.py:20` ("weld_audit gates every build") — it gates transplant builds only. A false claim in a docstring is how the next agent skips wiring it.

4. THE ONE REAL COVERAGE HOLE, and scope it to ONE verb: `world/terrain.py:reshape` writes a displaced height field to real stock land with no check of any kind (`:86-96`). Its failure mode is not cracks (it moves existing verts, shared block-edge verts move identically, so weld/T-junction are N/A by construction) — it is the climb/slope class that is adjacent to [[project-ff9-overworld-actor-brick]]. The check already exists and is already hard-enforced: reuse `interior.GATE_CLIMB` (2.30) / `interior.MAX_FLANK` (28.6) and the shape of `interior._perimeter_walk_in_gate` (`interior.py:791-846`) over `reshape`'s own moved-vert set, raising `ValueError` like every other interior gate. Import the constants; do not restate the numbers.

EXPLICITLY OUT OF SCOPE, with reasons: wiring `find_tjunctions` into `mesh.write_ff9mesh` (it is a 2-D plan operator, `meshedit.py:478`, not applicable to a 3-D BlockMesh set, and `write_ff9mesh`/`deploy_override` are single-block writers that would lose `weld_audit`'s cross-part census); promoting `bench_audit.py`'s GATE T into the kit (it is a study instrument for a bench the arc is about to DELETE per the LIFT-FIELD conclusion, `GROUND-JUNCTION-SYNTHESIS.md:363-370` — promote it only if a kit verb ever mints tessellation, which none does today); and any claim that the walkability class is unguarded — `interior.py` and `island.py` already refuse on it.
FIRST STEP: Open `ff9mapkit/ff9mapkit/world/texgates.py:202-212`, add a `status` key ("pass"/"warn"/"fail") to `_gate` next to the existing `ok`, then change `ff9mapkit/ff9mapkit/cli.py:4458` and `:4609` to derive their headline from the worst `status` across `summary["gates"]` / `summary["report"]["texgates"]` — and add the test that asserts the substring "CLEAN" never appears when a warn row is present.
========================================================================================
CLUSTER promotion-path [plausible, impact 4, medium, conf high] — The study-to-kit promotion path is broken: 351 study scripts, best instruments stranded
REC: Reject the cluster's headline ("351 scripts, promotion path broken") and ship its surviving core: THE KIT CANNOT PRODUCE AN IMAGE OF WORLD GEOMETRY. Promotion demonstrably works at single-verb and per-element-gate scope (texgates.py promoted 4 of composite_gates.py's 8; orphangate.py the 5th; meshedit.py, interior.py the builders). Exactly one capability class is stranded, and it is the one the project's own retrospective indicts: 8 rounds, zero images, 0-of-13 gates predicting a verdict.

Do three things, strictly in this order, and nothing else from these findings.

1. PROMOTE THE RENDERER (the whole point; one session). Create `ff9mapkit/ff9mapkit/world/render.py` by lifting `studies/path-d-new-world/render_gate.py`'s rasterizer (:154-210 is already generic over a view dict). Turn its four site constants into parameters: `render_cells(cells, *, disc=9, mod="FF9CustomMap-world", views, out_dir)` — CELLS is consumed at exactly one site (:133), so this is a signature change, not a rewrite. Keep `config.find_game_path` (:47), drop the hardcoded `BK = C:\gd\Dream-World-IX\backups` (:58) into a parameter. Lazy-import numpy inside the entry point, matching `world/interior.py:377`. Add CLI verb `world-render --cells 5,7 6,7 --disc N --eye X,Y,Z --at X,Y,Z --out shot.png`, plus an `--orbit` default that derives cameras from the cell-set centroid so a new site is renderable with no camera authoring. Gate: render the V-shore bench cells through the new kit path and assert byte-identical PNGs against `render_gate.py`'s committed output — the bench is byte-reproducible (`bench_pipeline.py all`, all 19 snapshots identical), so this oracle already exists and is free.

2. PROMOTE THE SAVE-BRICKING WALKABILITY PASS, NOT "the gates". `bench_audit.py`'s walkability lane is the one class that can strand a save (climb ceiling 2.34375u, render-only grass with ny<=0.1 that `WMPhysics.Raycast` skips, and the cache path `WMBlock.cs:155`->`WMPhysics.cs:49` applying neither filter) and the ground-junction synthesis explicitly banks it. Move it into `world/texgates.py` in that module's existing `{gate, ok, warn, enforced}` WARN-by-default shape (texgates.py:202-212, :24-30) and call it from the mint/carry paths. Take Gate T (tessellation) with it — tessellation is the synthesis's named real carrier. Do NOT promote the other six composite_gates predicates; per the 0-of-13 record they are regression harness, not oracle, and five are already promoted.

3. THEN the peer gate, as a CONSUMER of step 1 — `peer_gate(candidate_tris, peer_tris, *, views) -> dict` in the same WARN shape, generalizing `probe_peer_compare.py`. It cannot precede the renderer: overhang-context-law defines it as rendering candidate and owner-passed peers from coast-tangent-identical cameras and comparing screen-space band thickness.

Then re-point `terrain_gate.py:50-54` at the kit's versions so bench and toolkit share one implementation, and add one line to `studies/overworld-topography/PLAN.md` recording that `junction_compose.py` is bench-only, so CLAUDE.md §10's "★ DONE" is not misread as a kit capability.

EXPLICITLY DROP from the cluster: (a) the "run at two sites -> promote or DELETE" policy — deletion is destructive here, because 9 modules in `world/` cite `studies/` paths 27 times and `orphangate.py:2-6` states the study file's docstring "is this module's contract"; a standing promote-quota also collides with THE DEFECT FOLLOWS THE AUTHORSHIP (12/13) by minting surface on a schedule. (b) Retiring `canvas_render.py` — it is a headless agent-readable PNG; `worlddoc.py` is a human-only Qt view and draws strictly less. (c) The point-in-polygon dedup (6 copies, ~10 lines each) — real but cosmetic; it has cost zero playtests and touching seven call sites in shipped geometry code risks more than it saves.
FIRST STEP: Create `ff9mapkit/ff9mapkit/world/render.py` by copying `studies/path-d-new-world/render_gate.py` and converting its four module constants (GAME/MOD/DISC at :47-49, CELLS at :50-55) into arguments of a new `render_cells(cells, *, disc, mod, views, out_dir)` — CELLS is read at only one site, :133 — then prove the lift by rendering the six V-shore bench cells through the new function and diffing the PNGs against `render_gate.py render live` output for byte equality.
========================================================================================
CLUSTER declarative-composition [plausible, impact 4, medium, conf high] — No declarative composition spec for the world; composed worlds are order-dependent and unreproducible
REC: Do this in two rungs and do NOT start with the spec. The disease is silent destructive overwrite; the spec is a separate, more speculative want.

RUNG 1 (small, ship first -- this is the whole fix for the documented losses). Put the overwrite policy at THE ONE WRITE SEAM, not at two callers. `world/mesh.py:176-196` `deploy_override` is already documented as 'deliberately THE one write seam rather than retagging bm at each call site' -- add there: if the destination exists and its bytes differ, copy it to `<name>.ff9mesh.bak-<ts>` (reusing interior.py:2513-2522's already-proven naming) unless `backup=False`. That single site covers island, terrain, coastnav, rimretile, water, entrance, interior and fuse at once, which the expert's proposed two-site fold (island.landmass + terrain.reshape) does not. Two required companions: (1) `fuse._existing_overrides` (fuse.py:113-123) matches with bare `p.name.startswith(prefix)` and no extension test, so accumulated `.bak-*` files will spuriously and permanently trip the existing-overrides gate -- add an extension filter; interior already writes those backups today, so this hazard is live before you change anything. (2) `discmirror._BLOCK_RE` (discmirror.py:57) is `^...\.(ff9mesh|txt)$`-anchored and `coastnav.py:107` / `navimap.py:384` glob exact names, so backups are safely invisible to the mirror -- verify with a test rather than assuming. Gate it with one test that deploys twice at the same block and asserts the first bytes survive beside the second.

RUNG 2 (medium, only after Rung 1 is green). Do NOT build a `world-compose` DAG over all 9 generators. Extend the EXISTING `world-fuse` layout toml one table at a time, in the order the Southern Ring actually needed them: `[[island]]`, then `[[mountain]]/[[forest]]/[[hill]]`, then `[[coastnav]]`/`[[rim_retile]]`. Each table's handler calls the verb's existing Python entry point unchanged with the kwargs it already accepts -- the productized-builder pattern `build_shore_tweaks`/`build_grow_tweaks` already establishes exactly this (declarative kwargs in cli.py, one shared core function), so no new geometry code and no tweak-IR change is needed. Run the tables in the fixed order base-mint -> relief -> nav-stamp (a literal ordered list, not a topological solver -- there are 3 tiers, not a graph). Write `world_manifest.json` beside the deploy with per-file md5 plus the spec table that produced it, exactly as `studies/path-d-new-world/bench_pipeline.py` does for its bench, and refuse to write a block whose on-disk md5 diverges from the manifest without `--allow-overwrite`. Ship a table only when a real composition needs it; stop if the Southern Ring rebuilds from one toml.

FROM FINDING 3, keep only the cheap half: declare `class Tweak(Protocol)` in transplant.py and add a one-line `_check_tweak()` at the top of `transplant`/`morph_in_place` so the three divergent access sites (2768 hard attribute, 3024/3045 getattr) become one contract that fails at the call site. Drop the `spec()` proposal -- DropTris/EmitTris constructor args are bulk float geometry, so it buys a blob, not a declaration, and the declarative boundary already exists at the builder functions.

DO NOT touch geometry while doing this. CLAUDE.md 8's dead ends are all synthesis lanes and none is re-opened here, but THE DEFECT FOLLOWS THE AUTHORSHIP applies to the blast radius: this work must be provably byte-identical on a re-deploy of an existing accepted world (diff the Southern Ring's deployed tree before/after) or it has minted the very defect class it claims to prevent.
FIRST STEP: Open `ff9mapkit/ff9mapkit/world/mesh.py:176-196` and add a `backup: bool = True` parameter to `deploy_override` that copies an existing, byte-differing destination to `<name>.ff9mesh.bak-<ts>` before `write_ff9mesh`; in the same commit add an extension filter to `fuse._existing_overrides` (fuse.py:113-123, currently bare `startswith`) so backup files stop counting as deployed overrides, and add one test that calls `deploy_override` twice at the same block and asserts the first bytes survive beside the second and that `discmirror._BLOCK_RE` does not match the backup name.
========================================================================================
CLUSTER grammar-as-code [plausible, impact 3, small, conf high] — The decoded tile/junction/rim grammar lives in prose; no gate scores against a reference
REC: Do NOT build `world/data/grammar.toml`. Encoding rim/base-tile percentiles is scoring against marginals -- the mode with the 0/13 playtest record -- for a lane memory project-ff9-path-d-new-world.md:145 declares CLOSED, and a build that beat those percentiles still failed on form. Instead, close the one gap that is real and already 90% built: PROMOTE THE BASELINE DIFF INTO THE KIT'S GATE LEDGER.

1. Add a `"gate": "tjunc"` entry to `transplant.py`'s gate list, immediately after the weld-audit gate at transplant.py:2927-2929, calling the kit's OWN `meshedit.find_tjunctions` (meshedit.py:471) -- do not port `studies/path-d-new-world/probe_tjunction.py`; the primitive is already promoted and tested, it just has no production caller.
2. Make it DIFFERENTIAL, not absolute: run the same call on the pre-edit block meshes (transplant already reads the donor/pristine bytes) and gate on `new_count == 0`, mirroring `tjunction_allowlist.json`'s named-exception shape so a permanent residual cannot train anyone to ignore a red gate.
3. Scope it to the EDITED tris plus one ring of retained neighbour verts passed as `ext_verts` -- never the whole block. `find_tjunctions` is a pure-Python O(verts x edges) double loop (meshedit.py:480-495); a 4k-tri block is ~70M inner iterations and would make the gate unrunnable.
4. Reuse the existing `--against BASELINE` CLI convention already shipped for eb-asm (cli.py:6983) rather than inventing a `reference=` kwarg, so the differential idea has one spelling across the kit.
5. Prove it the way P1 proved meshedit: a hermetic test that FAILS when the gate is deleted (NEXT-STUDIES.md:97-110's mutation table is the standard here), plus one run against the accepted bench via `bench_pipeline.py check` to confirm it is green on owner-accepted bytes before it is allowed to judge anything.
6. Leave `probe_peer_compare.py` in studies. The perceptual peer gate needs the study-local render gate, and NEXT-STUDIES.md:244-246 already flags that its reference degrades as more of the coast becomes ours -- productizing it now buys a gate whose reference is dissolving.
7. The ONE grammar law worth landing in code is not a percentile but the zero-freedom BAND-CONTINUATION check (u and v continuous across a mint-to-retained course seam; 100.0% on stock across 1,090 verts, BASE-TILE-GRAMMAR.md:84-93). Land it as an assert inside whatever function next mints a foot band, at the moment that lane reopens -- not as a table wired into three verbs today.
FIRST STEP: In ff9mapkit/ff9mapkit/world/transplant.py, directly below the weld-audit gate at line 2927-2929, add a `tjunc` gate: call `meshedit.find_tjunctions(edited_tris, ext_verts=retained_ring_verts)` on the post-edit meshes and on the pre-edit (donor/pristine) meshes, append `{"gate": "tjunc", "new": n_new, "baseline": n_base, "ok": n_new == 0}`, then add a hermetic test beside tests/test_world_meshedit.py that injects one sub-tolerance vertex onto a shared edge and asserts the gate goes red -- and delete the gate to confirm the test fails without it.
========================================================================================
CLUSTER coordinate-centralization [confirmed, impact 3, small, conf high] — No canonical coordinate module: wrap/convert/frame logic re-derived per module with divergent conventions
REC: Do the ADDITIVE half only. Explicitly refuse the migration half.

STEP 1 (the falsifier, ~30 lines, zero production edits). Add `ff9mapkit/tests/test_world_frames.py::test_frame_constants_single_source`, modelled on the existing `test_world_grid_bounds.py:27-33`. It asserts, against today's code: `terrain.BLOCK == island.BLOCK == islandbeach.BLOCK == coastnav.BLOCK == water.BLOCK == extract.BLOCK_SIZE == workspace.worldscan.BLOCK_UNITS == 64`; `islandbeach.CELL == rimretile.CELL == texgates.CELL == island.GRID == water.CELL == 4.0`; and `navimap.WORLD_MAP_EXTENT == (coastnav.WORLD_W, 1280.0) == (mesh.GRID_COLS*64, mesh.GRID_ROWS*64)`. This costs nothing, needs no refactor, and either proves the 14 literals are already consistent (in which case the whole module is ergonomics and can wait) or finds live drift today. Run it before writing anything else.

STEP 2 (the module, additive). Add `ff9mapkit/ff9mapkit/world/frames.py` re-exporting `BLOCK_SIZE` from extract and the grid dims from mesh (never re-declaring them), plus `LATTICE = 4.0` and `WORLD_EXTENT = (1536.0, 1280.0)` sourced from `mesh.GRID_COLS*64 / GRID_ROWS*64`. Six named conversions with the axis sign encoded in the NAME: `block_to_world` (delegating to `extract.block_world_origin`), `world_to_block`, `world_to_block_local` / `block_local_to_world` (written in the simplified `wz + BLOCK*by` form, not the `+BLOCK*(by+1)-BLOCK` form duplicated at island.py:808 and interior.py:2461), and the two 4u-cell conventions under DELIBERATELY DIFFERENT names -- `lattice_ij(lx, lz)` returning the `(-z)//4`, j>=0 indices that rimretile.py:43 and water.py:476 want, versus `lattice_raw_xz(x, z)` returning the `floor(z/4)`, j<=0 indices that coastmorph.py:1382 and interior.py:616 want -- so a convention mismatch becomes a NAME mismatch. Move `navimap._wrap_world` here as `wrap_world_xz` and have navimap + coastnav import it; coastnav's missing Z wrap then reads as a one-line omission rather than a hidden asymmetry. Back it with a round-trip property test over all 24x20 blocks plus the four seam corners.

STEP 3 (two cheap loudness fixes, independent of steps 1-2, highest value per line in the cluster). (a) `terrain.py:70-71`: replace the bare `continue` with `summary.setdefault("off_grid", []).append([bx, by])` and print a SEAM NOTE naming the dropped blocks -- terrain.py:122-125 already RAISES on this exact condition, so the same module holds two policies; converting a silent wrong answer to a loud one is ~6 lines. (b) `extract.py:59-62`: amend the `blocks_touched` docstring to state the seam exception, because 'the exact, crack-free set' is false within `radius` of x=0/1536 or z=0/-1280 and this project's own rule is that a law in a docstring is a wish. (c) `placement.py:81` -- add `frame='block_local'|'world'` to `census()` ONLY (3 call sites: interior.py:2458, island.py:806, transplant.py:2936), asserting under 'block_local' that all meshes share one `(bm.x, bm.y)` and the span lies in [0,64]x[-64,0]. Do NOT put the assert in `place()`: the perf ledger measures it at 18.5% of world-island verify and names it a PyO3 kernel candidate.

REFUSED, and record the refusal in the study so it is not re-proposed: (i) migrating the ~12 world->block sites in coastmorph.py/transplant.py/interior.py to the new module, and (ii) consolidating the 6 crossing-number PIPs + 2 point-in-triangle tests + 2 ear-clippers into meshedit.py. Neither diverges behaviourally today (the validation-qa expert concedes this), both touch playtest-load-bearing arithmetic, and both are exactly the surface-minting that CLAUDE.md 7's DEFECT-FOLLOWS-AUTHORSHIP law and the perf assessment's 'porting a layer mid-falsification' verdict tell this project to stop doing. The standing rule instead is DIRECTIONAL: new code imports `frames.py`; an old site converts only when a session is already editing that line for another reason.

KEEP from validation-qa#7 regardless: add the one-paragraph process rule to the `authoring-ff9-overworld` skill -- an audit's SCOPE is part of its calibration; when a builder gains a new degree of freedom, every audit that assumed the old geometry must be re-scoped or explicitly declared still-valid. HEAD 972918db is the live instance and it cost a round.
FIRST STEP: Write `ff9mapkit/tests/test_world_frames.py` containing only `test_frame_constants_single_source` (copy the shape of `ff9mapkit/tests/test_world_grid_bounds.py:27-33`), asserting the 7 BLOCK literals, the 5 CELL/GRID literals, and the 3 world-extent statements (navimap.py:237, coastnav.py:57, mesh.py:33-34) all agree and all derive from `mesh.GRID_COLS`/`GRID_ROWS`. Run `py -m pytest tests/test_world_frames.py` from `ff9mapkit/`. If it passes, the constants are consistent today and steps 2-3 are ergonomics + loudness, not bug-fixing; if it fails, you have found a live drift with zero refactoring risk.
========================================================================================
CLUSTER provenance-manifest [plausible, impact 4, small, conf high] — Deployed worlds are unattributable bytes: no manifest, no provenance, while the field pillar has one
REC: Ship a SIDECAR deploy ledger for the world lane, hung off the single write seam. Do NOT embed provenance in the .ff9mesh format, and do NOT build a stock-baseline audit verb -- both are refuted (determinism contract; the 0-of-13 gate law).

1. LEDGER (the whole of the cure). In `world/mesh.py:deploy_override` (line 176-196), after `write_ff9mesh` returns, append ONE line to `<game>/<mod_folder>/.ff9world.jsonl`: `{cell:[x,y], part, write_disc, read_disc (bm.disc), sha256 of the file bytes, kit_version, utc, argv}`. JSONL not JSON -- 18+ concurrent sessions share this install (CLAUDE.md section 3), and append-only single lines avoid the read-modify-write race a .json object would introduce. Reuse `stamp.py`'s `content_digest`/`_rel` rather than minting a second hashing shape. Capture argv once at the CLI entry into a module-global that `deploy_override` reads (26 call sites -- do not thread a parameter through them). Mesh BYTES stay untouched, so `interior.py:2538`'s converge, `coastnav.py:332`'s assert, `discmirror`'s byte-identity mirror, `bench_pipeline.py`'s md5 loop and every "byte-identical" test keep working.

2. THE REFUSAL, NOT A STATUS REPORT. Copy the stamp's IDEA (`build.py:9574`), not its file: in `deploy_override`, BEFORE writing, if the target path exists and its on-disk sha256 matches NO entry in the ledger for that cell, refuse with the cell coords and the last ledger line for it -- "another session or a hand edit owns these bytes; pass --force-overwrite". That is the concurrency guard the shared-install reality actually needs, and unlike a post-hoc `world-status` it fires before the damage. Guard it behind an env/flag escape so the first sessions after landing can bootstrap a tree that has no ledger yet (empty ledger == permissive).

3. ONE READ VERB, scoped small: `ff9mapkit world-ledger [--drift]` -- print the last write per cell (verb, argv, donor, utc), and with `--drift` name every deployed file whose bytes match no ledger entry. This is reconciliation, not quality scoring; it makes no claim about whether the geometry is good, which is what keeps it out of the 0-of-13 class. Leave look/flow judgment to `studies/path-d-new-world/terrain_gate.py`, which already encodes the right rules (compare against the NEIGHBOUR, not the marginal).

Explicitly out of scope: any change to the .ff9mesh header or a version bump; any degenerate/winding/rejected-triangle rate scored against stock marginals; anything requiring the game to be running.

Gate: one new test file `ff9mapkit/tests/test_world_ledger.py` asserting (a) two identical `deploy_override` calls still produce byte-identical mesh files (the determinism regression guard -- this is the test that would have caught the embedded-chunk mistake), (b) the ledger gains a line per call, (c) a mutated-on-disk target makes the third call refuse, (d) the ledger name is excluded from any folder digest -- see the `_META_NAMES` lesson at `stamp.py:36-42`, where omitting the receipt from the exclusion set made a correct deploy report it as an "extra" file.
FIRST STEP: In `ff9mapkit/ff9mapkit/world/mesh.py`, add `_LEDGER_NAME = ".ff9world.jsonl"` and a `_append_ledger(dest, bm, part, write_disc, mod_root)` helper that sha256s the just-written bytes and appends one JSON line, then call it from `deploy_override` (line 196) on the path `write_ff9mesh` returns. Then write `ff9mapkit/tests/test_world_ledger.py` case (a) FIRST -- deploy the same BlockMesh twice into a tmp mod root and assert the two mesh files are byte-identical while the ledger has two lines -- so the determinism invariant is pinned before anything else is built on the seam.
========================================================================================
CLUSTER engine-boundary [confirmed, impact 3, medium, conf high] — The engine boundary is unsafe: silent rejection fallback, read-back instrument unconsumed, contract enforced twice differently
REC: THE THEME IS REAL: at the .ff9mesh boundary the kit MODELS the engine instead of MEASURING it — the loader's predicates are transcribed as prose in two docstrings, the effective-prefab behaviour is modelled by an offline oracle that has already had a documented "first live miss" (project-ff9-overworld-coast-mosaic.md, THE EFFECTIVE-PREFAB ORACLE LAW), and the one instrument that measures the engine's actual composition ships in the debug menu with zero readers. But calibrate it down: 0 of 1246 deployed files violate any engine predicate today, the rejection path does emit Log.Error, and a deployed-tree scanner half-exists. This is a latent boundary, not a bleeding one. Do it in three sized steps, cheapest first, and EXTEND existing modules — this project's own law is that the defect follows the authorship, so mint no new subsystem.

(1) ONE SESSION, no rebuild, no playtest — close the write seam. In ff9mapkit/ff9mapkit/world/mesh.py add `validate_blockmesh(bm)` raising ValueError (matching require_block_in_grid's convention twelve lines above, not AssertionError) and call it from write_ff9mesh before serialisation. Transcribe the engine's predicates verbatim from memoria-patches/s34-worldmap-mesh-override.patch's ReadMesh: version == 1, `vcount <= 0 or vcount > 65535`, `icount < 0 or icount > vcount * 3`, any `idx < 0 or idx >= vcount`, plus the existing UNINDEXED equality; add one NaN/inf scan over verts/normals/uvs/tangents in the loop already being written. Do NOT add a degenerate-triangle gate — the WALL LAW records that filter misfiring here, and shape gates are the 0-of-13 class. Add one test per predicate in ff9mapkit/tests/test_world.py feeding a deliberately violating BlockMesh.

(2) SAME SESSION — kill the drift. Add a test that reads the tracked memoria-patches/s34-worldmap-mesh-override.patch text and asserts the literals `SupportedVersion = 1`, `65535` and `vcount * 3` still appear (same shape as the existing Blender vendor-drift guard), so an engine-side bump fails the kit's suite instead of silently voiding a deploy. While there, collapse the three duplicate header parsers — workspace/worldscan.py:171-183 and world/coastnav.py:284-293 both re-implement it — onto one shared header reader in mesh.py.

(3) SHORT ARC, one owner interaction — spend the instrument. Add `world-readback <dump-dir>` to cli.py: ingest ff9mk_dumps/block_<x>_<y>/ (manifest.txt + NN_<child>.obj + walk_formN_MM.obj) and report, per part, (a) whether the engine's mesh matches the deployed .ff9mesh vert-for-vert (exact float compare — the OBJ uses ToString("R"), so it round-trips), (b) whether the Form1 walkmesh Raycast reads equals the render mesh, (c) the material/shader/texture actually bound, which is the empirical answer the EFFECTIVE-PREFAB and DIVERT-ARM laws are currently guessing at. Reuse workspace/worldscan.scan_tree for the deployed side and world/blendio.read_obj for the OBJ side; add nothing new. Frame it as human-in-the-loop (owner presses ~ -> World -> Dump on a suspect cell, agent reads the folder), exactly like tools/game_snap.ps1 — it cannot be a build step, the agent cannot play.

(4) DEFER TO THE NEXT DLL REBUILD, do not rebuild for it alone. In WMWorld, when a cell was divert-gated as reclaimed sea and TryLoad then returns null, emit a distinctive marker and leave the cell as SEA rather than committing Block[12][10]'s geometry and IDALL as this cell's collision; add the missing Log.Warning on ResolveReclaimDonor's null-donor fallback (s34:39-45, the class s74:35-38 already names). Skip the proposed Memoria.log-scraping `world-doctor` verb entirely — step (1) removes the realistic cause of the substitution, step (3) answers the same questions from ground truth, and worldscan already reports the dead-file half.
FIRST STEP: Open ff9mapkit/ff9mapkit/world/mesh.py and replace the assert at lines 71-74 with a new `validate_blockmesh(bm)` that raises ValueError on all five engine predicates transcribed from memoria-patches/s34-worldmap-mesh-override.patch's ReadMesh (version==1, vcount<=0 or >65535, icount<0 or >vcount*3, any idx<0 or >=vcount, plus the existing vcount==len(flat_index) UNINDEXED equality) plus a NaN/inf scan, call it from write_ff9mesh before serialisation, and add one violating-BlockMesh test per predicate to ff9mapkit/tests/test_world.py. Run only test_world*.py — this touches no install-gated byte-level file.
========================================================================================
CLUSTER dcc-bridge [plausible, impact 3, small, conf high] — The Blender/OBJ bridge destroys per-triangle IDALL, validates nothing, and cannot see kit output
REC: The merged theme is real and correctly named, but the fix is NOT 'add validators to the round-trip.' Split `world-mesh-export`/`world-mesh-build` into a FAIL-CLOSED write path and a PERMISSIVE read path, and reject the two sub-recommendations that would mint new defect surface.

(1) CLOSE the terrain write lane rather than trying to preserve IDALL through it. In `ff9mapkit/ff9mapkit/cli.py:8028` drop `"terrain"` from `world-mesh-build --part` choices, and in `world/blendio.py:build_from_obj` raise when `part == "terrain"` unless an explicit `allow_uniform_terrain_idall=True` is passed, with a message naming the real terrain paths (`world-terrain` for reshape, `world-transplant` for a carry, `world-mountain`/`world-forest`/`world-hill` for interior relief). ~6 lines. Do NOT build the face-index IDALL sidecar: Blender does not guarantee face count/order across a reshape, and a mis-applied per-face IDALL is a walkability bug that no gate can see, whereas a refusal is free. Leave `world-mesh-export --part terrain` alone -- exporting terrain to LOOK at it is legitimate; only the rebuild is destructive.

(2) ADD one hard, fail-closed structural gate at `world/blendio.py:258`, before `M.deploy_override`: after the world->local conversion at :171, assert the local bbox satisfies `-FRAME_EPS <= x <= 64+FRAME_EPS` and `-64-FRAME_EPS <= z <= FRAME_EPS` (import FRAME_EPS from `world/transplant.py:44`), raising with the offending bbox and the target block. This is not an aesthetic gate -- an out-of-frame vert is culled/garbage geometry, and it is the one class the ground-junction 'gates are not oracles' law does not cover. Add a degenerate-triangle COUNT to the returned summary as a printed warning only. Do NOT call `mesh.weld_audit` here: it is calibrated for carried stock geometry (donor blocks have zero sub-0.05u pairs) and would false-refuse legitimate hand-modeled buildings -- the 'calibrate the instrument before you judge with it' failure this project has already paid for. Do NOT route `read_obj`'s fan triangulation through `meshedit.earclip`: it is a 2D clipper on coords [0:2] that also re-winds the ring, so it would collapse vertical building faces and flip winding (SEA-SHEET LAWS: wrong winding renders and is void). If n-gon quality ever bites, triangulate in the face's own plane, or just tell the user to tick Blender's 'Triangulate Faces' on export.

(3) OPEN a read-only deployed-inspection mode on the export half -- this is the half that actually buys speed. Give `world-mesh-export` a `--mod-folder` that routes each block read through `world/entrance.py:605 read_block_stacked` instead of `extract.read_block`, and widen `--part` to accept `all` over `transplant.PARTS + ("object",)`. Emit per-part `usemtl` plus a sidecar `.mtl`: `map_Kd` pointing at `atlas.extract_atlas(part=...)` output for terrain and object only, and flat distinguishable `Kd` colours for beach1/sea1/sea2/sea3/sea5/sea4 -- the kit has no atlas name for those parts (`atlas.py:24` ATLAS_NAMES, `_part_name` raises), and colour-coded rungs read the RING LADDER better than a wave texture would anyway. Nothing in this writes geometry, so RESHAPE-not-OVERLAY is untouched. Keep it OBJ+MTL rather than glTF: the incremental value of glTF is a format the maintainer already knows, but OBJ+MTL reuses the existing writer verbatim and is a smaller diff. Note this is the human-eye instrument; an agent still cannot read it, so it complements rather than replaces the sibling cluster's offline raster render.
FIRST STEP: In `ff9mapkit/ff9mapkit/world/blendio.py`, add the two write-path guards to `build_from_obj` -- a `part == "terrain"` refusal (overridable by an explicit kwarg, message naming `world-terrain`/`world-transplant`) and a block-frame bbox check using `transplant.FRAME_EPS` inserted immediately before `M.deploy_override` at :258 -- drop `"terrain"` from the `world-mesh-build --part` choices at `cli.py:8028`, and add three cases to `ff9mapkit/tests/test_world_mesh_deploy.py`: terrain raises without the kwarg, an OBJ vert dragged past x=64 raises with the bbox in the message, and the existing part="object" cases still pass byte-identically.
========================================================================================
CLUSTER part-order [plausible, impact 3, small, conf high] — Engine part-registration order hardcoded four times in three inconsistent orders
REC: Make the registration order one engine-cited constant, and re-aim the fix at the LIVE lane (world-mountain), not the falsified --beach one.

1. `ff9mapkit/ff9mapkit/world/placement.py`: export `REGISTRATION_ORDER = ("Object","Terrain","Beach1","Beach2","Stream","River","RiverJoint","Falls","Sea1",...,"Sea6")` with the citation in the docstring -- Memoria `Assembly-CSharp/Global/WM/WMWorld/WMWorld.cs` LoadBlock :588/:590 then :748-:807, appended via `AddWalkMeshForm1` (:850 -> WMBlock.cs:44) and scanned as `ActiveWalkMeshes` (WMBlock.cs:10-19). This retires the "which was measured" question permanently.
2. Add `placement.build_meshlist(parts_by_name: dict[str, BlockMesh]) -> list[tuple[str, BlockMesh]]` that sorts by `REGISTRATION_ORDER` and normalises case. Validate the subsequence property in `census()` and in `build_meshlist()` only -- NOT inside `place()`, which runs 576+ times per census and must stay a hot loop.
3. Convert the three real call sites to it: `island._meshlist` (island.py:775-784), `interior.census_gate` (interior.py:2455-2457), and `coastnav._Loader.parts` (coastnav.py:151,166 -- iterate `REGISTRATION_ORDER` and delete `PARTS_ORDER`). The island beach branch's Beach1-last is fixed for free by the sort.
4. THE ONE FIX THAT BUYS A PLAYTEST: make `interior.census_gate` accept `res["changed_parts"]` and feed the REAL carried Object/Falls/River/RiverJoint into the meshlist instead of `hid("Object")` + omission, so `world-mountain`'s probe assertion (interior.py:2467) is measured against the stack cli.py:4731 actually deploys. Object registers ahead of Terrain, so today a carried massif Object over the probe point passes a gate it should fail.
5. Housekeeping, no behaviour: delete the false claim in `transplant.py:40` ("placement's first-hit-wins ground query depends on it" -- PARTS is a carry set, its order is never queried); leave `workspace/worldscan.py:42 PART_ORDER` alone but comment it as display order, not registration order.
6. Gate: one hermetic test in `ff9mapkit/tests/test_world_placement_order.py` -- (a) `build_meshlist` rejects Beach1-after-Sea1; (b) a synthetic carried Object quad above Terrain is what `place` returns, and `census_gate` raises when the probe expects Terrain. Pre-merge subset: `pytest ff9mapkit/tests/test_world_island.py test_sea4_under_land.py test_world_interior.py test_world_coastnav.py test_world_transplant.py`.

Do NOT scope any re-work of `world-island --beach` into this (falsified lane, CLAUDE.md section 8) -- the sort fixes it incidentally and that is all the attention it gets.
FIRST STEP: Open `ff9mapkit/ff9mapkit/world/placement.py` and add `REGISTRATION_ORDER` + `build_meshlist()` directly above `place()`, with the docstring citing `WMWorld.cs` LoadBlock :588/:590/:748-807 -> `AddWalkMeshForm1` :850 -> `WMBlock.ActiveWalkMeshes`; then re-point `interior.census_gate` (interior.py:2455) at it and hand it `res["changed_parts"]`.
========================================================================================
CLUSTER knowledge-in-code [confirmed, impact 3, small, conf high] — Falsified lanes still first-class CLI flags; no donor catalog for the overworld
REC: MERGED THEME (real, verified): the toolkit's invocable surface does not carry the project's verdicts. Falsified/proven status and donor qualification live in CLAUDE.md §8, a 1200-line study README, a memory law index and code comments — never in `--help`, never in a runtime message, never in a test. Two consequences are live today: an unguarded falsified flag whose name collides with an advancing lane, and a qualified-donor list that contradicts itself across sources.

DO THIS (one small arc, deliberately scoped down from both members):

1. Close the one unguarded lane. In `ff9mapkit/ff9mapkit/world/islandbeach.py`, prepend to the module docstring the `deepen_shallow_plan` treatment verbatim in spirit (transplant.py:4123-4129 is the template): "FALSIFIED IN GAME (4 rounds, 2026-07-15) — THE LADDER MINT reproduces the beach's measured properties, never its look. NOT the beach path; use `world-transplant --ground` (the (7,17) ground-retile carry). This module is KEPT for the measurement: its FOAM_TOPO / sand-band / ring-zip water mechanics are sound and ARE consumed by the proven carry (transplant.py:359, :665) — do not delete it." Prefix the `--beach` help at cli.py:8542 with "[FALSIFIED — superseded by `world-transplant --ground`; see CLAUDE.md §8]". Print a runtime banner in `_cmd_world_island` at cli.py:4551, immediately after the existing off-language `--ground` note at cli.py:4546-4549 — that note is the in-repo precedent, four lines away, and `warnings.warn` at transplant.py:678 is the second.

2. Kill the name collision, which is the sharpest hazard and costs nothing. `world-island --beach` is dead; `world-transplant --beach-mint` (coastmorph.beach_mint:3971) is live at rung 3. Edit CLAUDE.md §8 to stop calling the dead lane "the beach-mint ladder" — call it THE LADDER MINT (`world-island --beach`), which is what the coast-mosaic law index already calls it (line 147) — and add "(not `world-transplant --beach-mint`, which is live)".

3. Ship a hand-written, READ-ONLY `ff9mapkit/ff9mapkit/world/data/donors.toml`. One `[[donor]]` per entry: `blocks`, `class`, `aperture` (none/object/ensemble), `alcove` (the box or absent), `status` (qualified/candidate/disqualified), `anatomy_study`, `note`. Seed it with the four rects named at interior.py:79 plus (7,17), (20,5), (15,15), (18,15) — and resolve the README:376-vs-:423 contradiction while transcribing, since that is the actual deliverable. No `--regen`: donor_qualify_scan.py stays in studies/ and its output enters as `status="candidate"` rows a human promotes.

4. Do NOT rewire the geometry modules. `MOUNTAIN_DONOR` / `UAHO_ALCOVE` / `BEACH_PINS` / `SAND_BANDS` keep their frozen literals — the Uaho carry's bit-frozen acceptance must not acquire a file-parse dependency. The table is read by exactly one new consumer: `world-donors --list [--class massif]`, which prints it and replaces the hardcoded string at cli.py:4147.

5. Two gates, both cheap, both able to fail (per feedback-a-check-that-cannot-fail): a test in `ff9mapkit/tests/` that invokes the `--beach` path and asserts the banner text appears (delete the banner and it must go red — this is the entire point, since a law in a docstring is a wish); and a DRIFT test asserting every donor literal in interior.py/island.py/transplant.py appears as a row in donors.toml with a matching status. The drift test is what turns the table from a fifth stale copy into a gate.

EXPLICITLY OUT OF SCOPE: a `world/deadends.py` registry (a data structure for one entry), promoting the 436-line scan into the kit, and `--regen`. Both members reached for more machinery than a solo maintainer's actual defect rate justifies.
FIRST STEP: Add the FALSIFIED banner + help prefix for `world-island --beach` (islandbeach.py docstring, cli.py:8542 help, cli.py:4551 handler, modeled on the existing `--ground` note five lines above) and a test that fails when the banner is removed. One commit, no geometry touched, no gate suite run needed.
========================================================================================
CLUSTER test-architecture [plausible, impact 2, small, conf high] — Largest modules untestable without the install; no property-based tests; dual topology repr unguarded
REC: The cluster's theme is real but two-thirds of its proposed work is refuted. What survives is one coherent, one-session job: make three existing silent-divergence seams LOUD, using data the code already has. Do NOT do the coastmorph fixture carve-out, the BlockMesh.tris property conversion, or the census density raise.

1. SKIP-CEILING in `tools/nightly_gate.py` (repo root, not ff9mapkit/tools/). `parse_summary` at :228 already captures `skipped` and :334 already merges it into `entry`. Add `--skip-ceiling` beside `--collect-floor` at :275 and, immediately after the `entry.update(parse_summary(suite_log))` at :334, set `entry["result"] = "skip-long"` and return 1 when `entry.get("skipped", 0)` exceeds it. Note this is a POST-run verdict, not a pre-run abort like the floor -- that is correct and sufficient, because a non-green `latest.json` trips CLAUDE.md's "RED LEDGER -> TRIAGE FIRST" rule that sessions already honor. Seed the ceiling from the next green run's actual skipped count plus ~10%, and record the number in `tools/nightly_gate.md` next to the floor's rationale. Per [[feedback-a-check-that-cannot-fail]], prove it fires: temporarily unset `$FF9_GAME_PATH` in a `--smoke`-adjacent run and confirm the ledger says `skip-long`.

2. TOPOLOGY ASSERT in `ff9mapkit/ff9mapkit/world/mesh.py`, in `write_ff9mesh` directly under the existing vcount assert at :71-74: `assert [i for t in bm.tris for i in t] == list(bm.flat_index)`. Two lines, at the one boundary that matters, beside an assert with a documented brick history (the 2026-08-02 V-corner deploy). Take the stopgap and explicitly REJECT the property conversion -- log a one-line comment saying so, citing the in-place flip at :692-694, so a future session does not re-open it.

3. FOUR HYPOTHESIS PROPERTIES on `world/meshedit.py` only -- the layer that is already 100% offline-covered (82 tests, 0 skipif), so this adds zero install-gated surface and lands in public CI. Add `hypothesis` to the `dev` extra at `pyproject.toml:38`. Generate random simple polygons (integer-lattice rings, rejection-filtered for self-intersection) and assert, against `flat_patch`/`earclip`: (a) EVERY INPUT RING EDGE APPEARS IN THE OUTPUT TRIANGULATION -- this is the one with a real bug behind it, the coast-mosaic RING-CONFORMITY LAW's concave-corner notch-quad diagonal that shipped ONE missing face past a sampled-holes gate; (b) plan area is conserved to 1e-9; (c) no output vertex is off the input ring (the :728 raise never fires on a valid ring); (d) output winding always matches the requested sign. Cap it there. Do not extend properties into transplant/coastmorph.

Scoping rationale to carry into the report: per [[project-ff9-ground-junction-synthesis]] (0 of 13 playtest verdicts predicted by any gate; 12 of 13 defects landed on newly authored surface), test surface is not free here, and none of this buys look-correctness -- it buys loudness on three silent-failure classes only. Frame it as guard-rail hardening, not as coverage improvement.
FIRST STEP: Open `C:/gd/Dream-World-IX/.claude/worktrees/overworld-custom-models-npcs-239a84/tools/nightly_gate.py`, add the `--skip-ceiling` argparse option next to `--collect-floor` at :275-276, and insert the post-run check right after `entry.update(parse_summary(suite_log))` at :334 -- then run `py tools/nightly_gate.py --smoke` with `$FF9_GAME_PATH` pointed at a nonexistent folder to observe the new guard actually fire before committing it.
========================================================================================
CLUSTER small-frictions [confirmed, impact 3, small, conf high] — Relaunch tax possibly self-imposed; authored normals are dead bytes; flagship verb has 43 flags and no tutorial
REC: THE MERGED THEME IS REAL AND VERIFIED: the overworld pillar's decoded facts live in studies/ and memory but never reached the CLI call site, so the surface an agent reads at 3am (print strings, defaults, docs index) states the wrong thing on the project's biggest pillar. Fix it at the call site in one session; refuse the two expensive halves.

1. RELAUNCH RULE (the whole payoff). Add one helper `_print_world_apply_note()` in ff9mapkit/ff9mapkit/cli.py next to the world handlers and route all ~25 world-verb apply lines through it. Its text, grounded not guessed: "APPLY: exit the overworld to any field and return, or ~ -> World -> 'Reload overworld on state' -- the scene rebuild re-reads every loose override from disk. RELAUNCH only for: a new DictionaryPatch/BattlePatch line, a Memoria.ini FolderNames change, an engine DLL rebuild, or if a ModFileList.txt exists in your mod folder." Delete "a new loose asset isn't hot-reloaded" from cli.py:3808 (world-deploy) and cli.py:5020 (world-entrance) and the self-contradicting sentence at ff9mapkit/examples/continent-v1/README.md:24-25. Do NOT write "walk away and back" -- blocks never stream out (WorldState.cs:25 sets DiscardBlockWhenStreaming=false; ff9.cs:3703 loads all 480 at scene init). Correct the stale line at memory project-ff9-overworld-coast-mosaic.md:496.

2. GATE IT so the strings cannot drift back: add a grep-level test in the existing CLI-help gate family asserting (a) no world-* handler emits "hot-reloaded" and (b) every world writer calls the shared helper. This is the cheap part that makes the fix permanent.

3. ONE PLAYTEST, the only thing offline cannot prove: deploy a visibly different world-terrain hill on the block under the player, press ~ -> World -> Reload overworld on state, look for "[WorldMeshOverride] loaded" in Memoria.log and the hill on screen. If it lands, the loop cost on every future world iteration drops from relaunch+walk-back to ~1s.

4. NORMALS, two lines, same session: replace cli.py:3805's `" + smooth normals"` with `" + normals (render-inert on WorldMap/Terrain -- see GROUND-JUNCTION-SYNTHESIS.md)"` and put the same one-liner above world/mesh.py:1000 `recompute_normals`. DO NOT flip the --no-normals default -- the channel is free, the loader binds it harmlessly (s34 patch:420), Object/Sea materials were never verified, and changing emitted bytes for a mechanism we believe dead is precisely the authorship CLAUDE.md §7 warns mints defects.

5. TUTORIAL: downgrade, do not write an S-track lesson. Add docs/OVERWORLD_RECIPES.md (~40 lines) lifting the already-proven continent-v1/README.md:15-25 four-command sequence (world-fuse -> world-island -> world-coastnav -> world-minimap), pointing at the authoring-ff9-overworld skill gate as step zero, and index it from docs/tutorials/README.md so the largest pillar stops being absent from both tracks. SKIP the --recipe preset layer entirely: the flagship example already collapses to `world-transplant --ground desert` (cli.py:8303).
FIRST STEP: In ff9mapkit/ff9mapkit/cli.py, write `_print_world_apply_note(*, needs_custom_engine=True, extra=None)` beside the world handlers and convert the single highest-traffic site first -- cli.py:3806-3810 in `_cmd_world_deploy` -- deleting "(a new loose asset isn't hot-reloaded)"; then run `py -m ff9mapkit world-deploy --help` and one dry-run deploy to confirm the new line prints, before touching the other 24 sites.