# THE BAND SEAT — prediction registration (BEFORE building)

2026-07-31. The ground-junction synthesis closed the causal model on the owner's
stills (GROUND-JUNCTION-SYNTHESIS.md): the hill is the lift field's shape, the seams
are its tears, the meadowy tiles are its shattering — one generator, absent from the
pristine bench and from the buried-mesa round, the only round whose lawn drew zero
complaints. The owner chose the **level host, no lift**. Registered before the first
builder edit; the branch is `claude/path-d-level-host` and nothing merges to master
until the owner scores it.

## The claim under test

**The bury seat was right; only its DEPTH was wrong.** The mesa round's level-cut
machinery (two clean playtests: 4 residual once-edges, a flat untouched lawn, zero
seam/tile/hill complaints) is kept VERBATIM. The one change — the round's single
lever — is the seat formula:

- The bury round chose `dy = LOWLAND − (w_max + 0.2)` = −4.35: the whole ground-weld
  line just below the cut plane, so the cut crossed MID-FACE rock and the visible
  bottom course had no transition art ("we still lack the grass↔cliff transition").
- THE BAND-SEAT LAW OF THE DONOR'S OWN NUMBERS: the band course is ~3.7u tall and
  sits ON the weld line; the honest weld spread is p10 3.81 → max 7.4 (donor frame).
  For `dy ∈ [LOWLAND − w_p10 − 3.7, LOWLAND − w_max]` ≈ **[−4.31, −4.2]**, every
  column's wall still reaches the cut plane (no skirt gaps; the west border verts at
  4.4-6.8 stay buried) AND the cut lands **inside the donor's own band course** for
  every column with weld ≥ ~3.75 — ~90% of the ring. The visible bottom course
  becomes the donor's own row-10/11 transition band: **carried bytes, u and v the
  column's own continuation — the BAND-CONTINUATION LAW satisfied by construction,
  because nothing is minted.** The low-weld tail (~10% of columns) shows the row
  above instead — stock's own intermittency pattern (53% census share; even this
  donor carries row-11 cells and gaps).
- The seat is computed from the real foot verts at build time
  (`dy = max(LOWLAND − w_max − 0.05, LOWLAND − w_p10 − 3.7)`, predicted ≈ −4.25)
  and the per-column cut-row coverage is REPORTED (predict ≥ ~80% band rows on the
  cut course).

**Deleted relative to the last deployed build (all of it the defect generator):** the
apron carry, the lift field and BLEND_R, the L3 retile, the ground-normal pass, the
donor-neighbor soup, the border/step stitches. **Deleted relative to the mesa round's
final state:** the row-10 fringe re-mint (the band is carried now; the arclength
fringe is the falsified lane). The build is `band_seat.py` = `mesa_carry.py` with the
seat formula changed and the fringe block removed — nothing else.

## Gates

- The mesa round's own suite verbatim (pristine guard, watertight ≤24/none>5u,
  winding, census, reach, culled game-eye renders).
- **THE BAND GATE, carried form:** ≥60% of cut-course fragments wear atlas rows
  10/11 (measured offline from the built output; predict ~80%).
- **`bench_audit.py` walkability on the OUTPUT:** zero climb-ceiling edges, zero
  render-only grass facets. (GATE T's tessellation clause is NOTED, not gated, on
  this build: the level cut's flat, parent-affine fragments are coplanar with their
  parents — same plane, same uv mapping — which is exactly the configuration the
  A/B measured as visually inert. GATE T's calibrated target is LIFTED ground.)

## REGISTERED PREDICTION

**PASS on the lawn and the base.** Basis: the lawn is the pristine flat sheet plus
flat coplanar fragments (the configuration with two clean playtests); the base band
is the donor's own art in the donor's own placement.

## Falsification semantics — declared in advance

- **The hill, seams, or meadowy tiles RECUR** → the causal model itself is refuted
  (their generator is absent from this build) — the strongest and most honest
  falsifier this arc has ever registered. Back to the synthesis with the new datum.
- **FAIL on the base band** (patchwork/mismatch again) → the band-seat reading of
  the course quantization is wrong; measure which column class failed before any
  further move.
- **"Stretched faces" at the base** → get coordinates + a still; the tear/sliver
  protocol (probe_white_streaks + the once-edge locator) runs before any fix.
- **Height named** (~23.9 top, the bury stature the owner has already seen) → a
  context call, not a law failure.
- **PLUMBING** → fix or stop, no verdict.

One round, one lever (the seat depth). Scored on the owner's verdict.
