# Path D adversarial critique record (2026-07-29)

Three independent skeptic passes over the draft plan, before the final synthesis in PLAN.md
folded their findings back in. Kept for provenance -- read PLAN.md section 9 for a summary of
what changed as a result.

## Feasibility skeptic

```json
{
  "findings": [
    {
      "claim": "WMBlock's IsSea/HasSpecialObject/IsSwitchable/InitialX/InitialY/Number are plain public fields with no [NonSerialized]/[SerializeField]/attribute restriction (WMBlock.cs:243-259).",
      "evidence": "Read the live file C:\\gd\\FFIX\\Memoria\\Assembly-CSharp\\Global\\WM\\WMBlock\\WMBlock.cs in full. Lines 243-258 read exactly: `public Boolean IsSea; public Boolean HasSpecialObject; public Boolean IsSwitchable; ... public Int32 InitialX; public Int32 InitialY; public Int32 Number;` -- no attributes on any of them.",
      "verdict": "CONFIRMED"
    },
    {
      "claim": "WorldDisc (WMWorld.cs:2033) is read at exactly 4 locations in the entire Assembly-CSharp tree, all inside WMWorld.cs: :105, :139, :449, :470.",
      "evidence": "Ran `grep -rn \"\\.WorldDisc\\b\" --include=*.cs .` across the whole Assembly-CSharp tree myself (excluding the unrelated string-literal 'WorldMap/Prefabs/WorldDisc{0}' path-format calls). Result: exactly 4 hits, all in WMWorld.cs at lines 105, 139, 449, 470, matching the plan's citations verbatim.",
      "verdict": "CONFIRMED"
    },
    {
      "claim": "Per-block world position is computed from InitialX/InitialY at OnInitialize() time (WMWorld.cs:449-458), not baked into a stored transform.",
      "evidence": "Read WMWorld.cs:445-474 directly. Lines 449-458: `this.Blocks = BuildBlockArray(this.WorldDisc); for(i<20) for(j<24){ position.x=j*16384*0.00390625f; position.z=i*-16384*0.00390625f; wmblock.transform.position=position; }` -- confirms the plan's claim exactly (16384*0.00390625=64, i.e. InitialX/InitialY*64).",
      "verdict": "CONFIRMED"
    },
    {
      "claim": "Plan Section 5: 'trusted from r6's exhaustive per-file census for the rest, which this session spot-checked and found consistent with the actual world/ directory listing -- 27 files, matches r6 exactly.'",
      "evidence": "Ran `ls .../ff9mapkit/ff9mapkit/world/ | wc -l` myself: 27 entries, but one of them is `__pycache__` (a bytecode-cache directory, not a source file). The actual .py file count is 26 (verified by listing and counting: __init__, atlas, blendio, coastmorph, coastscan, discmirror, encounter, entrance, environment, extract, fuse, grassland, interior, island, islandbeach, locate, mesh, navimap, orphangate, palette, placement, terrain, texgates, transplant, water, worldpack = 26). r6's own per-file census in the underlying research also enumerates only 26 distinct world/*.py paths (cli.py and the two test files are separately cited, outside world/), not 27. The plan's '27 files, matches r6 exactly' is wrong on the count and on the 'matches r6' claim.",
      "verdict": "WRONG"
    },
    {
      "claim": "Rung 2's engine work is scoped as: 'extend Memoria.World.WorldMeshOverride's per-cell key (currently (disc, x, y)) ... so cells belonging to a Rung-0-built WorldDisc are looked up under a reserved sentinel disc number ... instead of world.currentDisc' -- framed as a change inside WorldMeshOverride.cs.",
      "evidence": "Read the entire WorldMeshOverride.cs (223 lines): every function (HasLandOverride, TryReadDonorPath, TryLoad, TryLoadTexture) already accepts an arbitrary Int32 disc parameter with zero {1,4}/hardcoded restriction -- consistent with the plan's OWN later claim in the same rung that this file 'needs zero engine change to read a different disc-tag value.' The real hardcode is `this.currentDisc` baked directly into the CALLERS inside WMWorld.cs, which I found and verified at 6 separate sites: RegisterBlockComponent (:786-788, `Memoria.World.WorldMeshOverride.TryLoad(String.Format(..., this.currentDisc, ...))`), RegisterBareObjectOverride (:833-835, same pattern), the `LoadBlock(this.currentDisc, block)` call (:547), the LoadBlocks streaming loop (:1187 and :1193, both `this.LoadBlock(this.currentDisc, wmblock)`), and the async block-reload path (:1257, `ResolveReclaimDonor(this.currentDisc, ...)`). The plan never cites any of these WMWorld.cs sites for this rung -- it names only WorldMeshOverride.cs, which is the one file that needs no change at all.",
      "verdict": "OVERSTATED"
    },
    {
      "claim": "'DefinePlayerCharacter (opcode 0x2C) is a one-line controlUID write ... deriving everything else automatically -- no separate camera-init opcode exists' (Rung 1, 'Already proven-safe' bullet, carried from r4's 'does nothing but this._context.controlUID = this.gExec.uid').",
      "evidence": "Read EventEngine.DoEventCode.cs:1033-1045 directly. The handler also does: an `getActiveActorByUID(this._context.controlUID)` lookup with a conditional mutation of a DIFFERENT actor's `fieldMapActorController.isPlayer`/`gameObject.name` (guarded on gMode==1 and an `effMapNo==1607` exclusion), and a conditional `SmoothFrameUpdater_World.Skip = 1` write when gMode==3 and the controlUID is changing -- before the final `this._context.controlUID = this.gExec.uid; return 0;`. The load-bearing effect the plan needs (the final controlUID write enabling the free-roam camera) is correctly identified, but 'does nothing but' / 'one-line' understates the actual opcode logic, and this specific claim is presented in the plan without its own '[verified this session]' tag despite being stated flatly as 'already proven-safe.'",
      "verdict": "OVERSTATED"
    },
    {
      "claim": "Implicit in Rung 0's risk enumeration: the candidate failure modes for the WorldDisc-substitution spike are exhaustively named (position-write loop, Wrap() loop, ShiftBlocks, the self-heal patch) -- treated as a settled/complete risk map for the spike.",
      "evidence": "Read BuildBlockArray (WMWorld.cs:1673-1690) directly -- the exact function Rung 0 depends on to enumerate the synthetic hierarchy. Its inner loop calls `transform.GetComponent<WMBlock>()` on EVERY child of worldDisc with NO null check before dereferencing `component.InitialX`/`InitialY` (line 1684: `if (component.InitialX == i && component.InitialY == j)`). This is a distinct crash mode from the 'missing cell' gap the plan does call out (OnInitialize's later null-unguarded position write) -- any stray non-WMBlock child parented under the spike's synthetic WorldDisc_SPIKE root throws an NRE immediately during BuildBlockArray, before OnInitialize even runs. The plan's Rung 0 section does not cite this line or name this specific landmine, despite naming and citing the adjacent, less-severe risks in the same function's neighborhood.",
      "verdict": "UNSUPPORTED"
    },
    {
      "claim": "Cross-check of high-confidence engine citations used throughout the plan (spot-checked beyond the required 3): WMWorld.cs:2069-2074's s34 serialization-layout comment quoted verbatim; ff9InitStateWorldMap (ff9.cs:9293-9312) and WorldConfiguration.GetDisc() (WorldConfiguration.cs:234-241) are mutually disconnected as claimed; w_fileSystemConstructor's strict {1,4} branch (ff9.cs:3621-3644); DataPatchers.cs FieldScene/BattleScene EventDB-write pattern (:497-548); FF9DBAll.EventDB as Dictionary<Int32,String> (FF9DBAll.Events.cs:7); the continent-title switch's corrected location at ff9.cs:8838-8852 (vs. the originally-miscited :8683, which is genuinely unrelated weather-color-interpolation clamp code); w_naviLocationPos's [2,64] declaration (ff9.cs:421,10393); AssetManager.LoadFromDisc<T> genuinely has no GameObject case (AssetManager.cs:372-427); WMWorldPrefabMaker.LoadModelAsset has zero real call sites; cli.py's world-encounters --disc choices=[1,4] is the sole exception among 21 --disc arguments (cli.py:7022-7955); entrance.py's _WORLD_RE/load_all_dispatchers generic discovery (entrance.py:84,87); data/__init__.py has blank_field_bytes but no blank_world_bytes anywhere in the kit.",
      "evidence": "Each of these was independently re-read from the live source this session (not just trusted from the plan's or the research bundle's text) and matched the plan's citations to the letter, including line numbers.",
      "verdict": "CONFIRMED"
    }
  ],
  "overall_verdict": "The plan's engine-source citations are, on the whole, unusually well-grounded -- I independently re-verified roughly 20 distinct load-bearing citations across WMWorld.cs, WMBlock.cs, DataPatchers.cs, FF9DBAll.Events.cs, ff9.cs, WorldConfiguration.cs, WorldMeshOverride.cs, AssetManager.cs, WMWorldPrefabMaker.cs, WorldPlace.cs/WorldEffect.cs, and the kit's entrance.py/data/__init__.py/mesh.py/cli.py, and every one matched the plan's stated file:line and content exactly, including the session's own correction of a wrong line number (ff9.cs:8683 vs the real :8838-8852 continent-title switch). The central Rung-0-gates-everything framing, and its characterization of WMBlock/WorldDisc/OnInitialize as genuinely more codeable than the underlying research agents individually concluded, holds up against direct source reading and is appropriately hedged as unproven rather than asserted as fact.\n\nThat said, the plan is NOT uniformly as rigorous as its 'verified this session' tags imply, and its own build-versus-verify discipline erodes noticeably past Rung 0. Three concrete problems: (1) a flatly wrong, independently-falsifiable factual claim -- '27 files, matches r6 exactly' -- when the live directory has 26 .py files (27 only if you miscount the __pycache__ cache directory as a source file), and even r6's own census lists 26, not 27; (2) Rung 2's one deliberate architectural proposal (the 'sentinel disc' fork) is mis-scoped to the wrong file -- it cites WorldMeshOverride.cs as needing to be 'extended,' but that file already takes an arbitrary disc int everywhere (confirmed by reading it in full, and consistent with the plan's own contradictory later claim that the file 'needs zero engine change'); the actual hardcode is `this.currentDisc` baked into at least 6 separate call sites inside WMWorld.cs that the plan never cites or enumerates, meaning the 'one deliberate architectural fork this plan proposes' understates its own footprint inside the very class the plan elsewhere flags as serialization-fragile; (3) a minor but real oversimplification of DefinePlayerCharacter's actual opcode logic, carried from r4 without independent re-verification despite being presented flatly as 'already proven-safe.' None of these overturn the plan's core Rung-0 gating logic or its overall 'genuinely uncertain, leaning codeable' verdict, but they show the plan's confidence calibration is uneven: Rung 0's claims survive close scrutiny very well, while Rung 2-4's claims -- explicitly labeled as provisional on Rung 0 -- carry the same confident citation style without having received the same level of actual verification, and a skeptical reader should not extend Rung 0's earned credibility to those later sections uncritically.\""
}
```

## Sequencing skeptic

```json
{
  "verdict": "Sound in intent, not fully sound in practice. The plan correctly identifies and front-orders the single biggest existence question (can a synthetic 480-WMBlock hierarchy replace WorldDisc at runtime) as Rung 0, and its code-level claim that the state axis (EventDB/wldMapNo dispatch) and the geometry axis (WorldDisc) are genuinely decoupled is well-verified against live source (ff9.cs:9293-9312, WorldConfiguration.cs:234-241, WMWorld.cs:105/139/449/470 -- all re-checked this session and accurate). That part is not a disguised big-bang: it is real incremental de-risking of the CORE unknown, and Rung 2's decision to defer the sentinel-disc namespace fork's validation to Rung 2 rather than earlier is reasonable given WorldMeshOverride.cs:80-127 already takes `disc` as a plain generic Int32 parameter.\n\nHowever, the plan is NOT fully de-risked as written, in two concrete ways this session's direct source-reading surfaced that none of the six research passes (nor the plan's own re-verification pass) caught. First: Rung 0 and Rung 1's own named 'cheap verify' mechanisms do not work as described, so their observability secretly depends on unscoped extra work the cost table in §7 never prices in -- this is the classic 'rung whose success can't actually be checked without something from a later rung' anti-pattern the task asked me to look for. Second: Rung 1's 'entrance.py needs no change' claim and Rung 4's 'author_entrance already proven generic over any discovered dispatcher' claim both rest on a misread of entrance.py -- its dispatcher-discovery function structurally cannot see a live/custom dispatcher (it only reads the pristine base-game p0data*.bin), and its authoring machinery is hardwired to exactly the 13 known WORLD00-12 ids by the module's own docstring. So Rung 0's engineering risk is well-assessed, but Rungs 1 and 4's TRUE scope is understated, and the plan is missing a genuinely tiny 'can I even reach and observe a new wldMapNo at all' spike that should come before the expensive 480-block hierarchy work.",
  "issues": [
    "MISSING OBSERVABILITY RUNG -- Rung 0's cheap-verify names two debug-menu routes to reach wldMapNo 9013; both are broken as described. (1) Ff9mkDebugMenu.cs:1401-1407 ForceWorldState -- the ONLY UI control that sets FF9.wldMapNo and reloads the WorldMap scene onto a chosen dispatcher (wired from the World-tab text field at :1138-1140 and the fixed buttons at :1143-1146) -- hard-rejects any id outside 9000-9012: `if (!Int32.TryParse(text.Trim(), out Int32 id) || id < 9000 || id > 9012) { _status = \"reload world: need an id 9000-9012\"; return; }`. This directly contradicts the plan's own Executive Summary claim that 'nothing gates wldMapNo outside the fixed 9000-9012 dictionary keys' -- that claim is true of the ENGINE dispatch path (ff9InitStateWorldMap, WMAPJUMP->SetNextMap have no range check, confirmed at EventEngine.DoEventCode.cs:2458-2461) but false of the project's OWN debug tooling the plan relies on to observe the result. (2) Rung 0's cited alternative, WMBeeMenu.cs:127-153 'Jump To', is NOT a dispatcher switch at all -- it is a same-world POSITION teleport (opens `showSetPositionMenu`, gated on `ff9.w_frameDisc == 1`); it cannot select a new wldMapNo. So as literally written, neither route in the plan can reach or observe dispatcher 9013 without an unlisted third change: either a one-line widen of Ff9mkDebugMenu.cs's range check, or building a minimal field-side WMAPJUMP trigger (pulling forward a sliver of Rung 4's own machinery). This is exactly the hidden-dependency pattern the review was asked to check for: Rung 0's pass/fail signal cannot actually be read on the first attempt as scoped.",
    "RISKY UNSCOPED FALLBACK -- Rung 0's own 'OR simplest' alternative (fire the WorldDisc substitution unconditionally on the very next WorldMap scene load, entered via any existing route) sidesteps the id-9013-reachability problem, but does so by making the throwaway spike fire for EVERY player who loads ANY world map (real disc 1 or disc 4) while the patch is deployed -- not just a Path-D test session. CLAUDE.md §3/§5 states explicitly that many concurrent agent worktrees share ONE game install and mod folder set; an unconditional WorldDisc substitution deployed to that shared install would corrupt normal disc-1/4 play for every other concurrent session until reverted. The plan's 'S / one throwaway patch' sizing for Rung 0 in §7 does not flag this blast-radius risk, and the safer, properly-gated design (behind a debug flag) is the one that inherits problem #1 above (no working way to flip that flag to a NEW id in-game).",
    "UNDERSTATED KIT SCOPE for Rung 1 -- the plan asserts (entrance.py's dispatcher-discovery half, verified this session against `entrance.py`... confirmed by direct read) that no kit change is needed to discover the new dispatcher. Direct re-reading this session shows `load_all_dispatchers` (entrance.py:87-120) sources EXCLUSIVELY from `StreamingAssets/p0data*.bin` via UnityPy -- the pristine, unmodified base-game asset bundles -- never from a deployed mod folder or the live EventDB. A brand-new custom dispatcher such as EVT_WORLD_CUSTOM_WORLD would never exist inside p0data*.bin, so this function cannot discover it under any circumstance, regardless of filename. `load_world_dispatchers` (entrance.py:123-126) is a thin wrapper over the same source. This is a load-bearing misreading, not a nitpick: it means the claim 'entrance.py needs no change' for Rung 1 is not supported by the function actually cited.",
    "COMPOUNDING NAMING-CONVENTION BUG -- even setting aside issue above, the plan's own worked DataPatchers.cs example for the WorldScene directive (`WorldScene 9013 CUSTOM_WORLD` -> `FF9DBAll.EventDB[ID] = \"EVT_WORLD_\" + entry[2]`, i.e. container name evt_world_custom_world) would not match `_WORLD_RE = re.compile(r\"eventbinary/world/([a-z]{2})/(evt_world_world\\d+)\\.eb\")` (entrance.py:84) -- the regex requires literally 'world' followed by digits after 'evt_world_'. Only a name shaped like WORLD13 would match. This is a second, independent reason the 'no kit change / confirmed generic' framing for Rung 1 is optimistic.",
    "UNDERSTATED KIT SCOPE for Rung 4 -- the plan characterizes extending `author_entrance` to target 9013 as 'already proven generic over any discovered dispatcher' (citing entrance.py:731-1039) with only a narrow named gap (the switch-building helper). Direct reading of `entrance_func_body` (entrance.py:360-369) shows it hardcodes `disp[\"evt_world_world00\"]` as the template source, and the module's own docstring (entrance.py:1-16) states outright: 'There are 13 dispatchers (EVT_WORLD_WORLD00..12) selected by entry/story state... this covers them all' -- describing the ENTIRE authoring loop (add the trigger func to every dispatcher whose AREA switch carries the case) as scoped to exactly those 13. A 14th dispatcher is not a case this machinery has any concept of. This is real, non-trivial generalization work that none of the plan's six research passes flagged and that materially changes Rung 4's 'M, bounded, well-scoped gap' sizing in §7 -- it is currently the plan's single largest unacknowledged kit-side risk.",
    "SCOPE-BUNDLING inside Rung 1 -- Rung 1 bundles three genuinely untested primitives under one 'S / low risk' label: (a) a new DataPatchers.cs directive (well-precedented, genuinely low risk, confirmed by direct comparison to FieldScene/BattleScene at DataPatchers.cs:497-548), (b) a new `blank_world_bytes()` kit function with no prior art (confirmed missing -- only `blank_field_bytes` exists at data/__init__.py:32), and (c) untested application of `eb.edit`/`eb.cmdasm` primitives to WORLD02-shaped bytes specifically (r4's 'minimal Main_Init' decode is cited as a research report, explicitly flagged in the plan's own preamble as re-verified only where noted -- this specific claim was not independently re-verified this session by the plan or by this review). The project's own house law (incremental verbatim-first: study real bytes -> replicate ONE piece -> verify, referenced in the auto-loaded memory index) argues against combining (b) and (c) into the same unverified step as (a); a failure in (b)/(c) would be misdiagnosed as a Rung-1 dispatcher-registration failure when it is actually a byte-editing failure on a novel container shape.",
    "MINOR VERIFIED-CORRECT NOTE (not a defect, stated for completeness) -- the plan's central engine-risk claims for Rung 0 check out under direct re-reading this session: WorldDisc's exactly-4 field-access call sites (WMWorld.cs:105,139,449,470), WMBlock's unattributed public fields (WMBlock.cs:243-259), OnInitialize's computed (not baked) per-block position write with no null guard (WMWorld.cs:449-458), BuildBlockArray's gap-prone linear scan (WMWorld.cs:1673-1690), Wrap()'s early-exit on a null/dummy actor (WMWorld.cs:1077-1082), and the already-shipped s34 reclaim/donor mechanism carrying live 'DWIX Path D' comments in the engine source (WMWorld.cs:490-537) are all accurately cited. The next-free-patch-number claim (s70, highest existing s69-minimap-visible-state.patch) also checks out against the live memoria-patches/ directory listing."
  ],
  "proposed_reordering": "Keep Rung 0's target (prove runtime WorldDisc substitution survives Initialize/OnInitialize/Wrap) as the correct first big question -- do not reorder the CORE risk itself. But insert a new, genuinely smaller 'Rung 0-pre' immediately before it, and split the current Rung 1 into two:\n\nRung 0-pre -- THE REACHABILITY SPIKE (new, ~1 line, do this literally first): before building any synthetic WMBlock hierarchy, prove that an arbitrary/new wldMapNo can be set and the WorldMap scene reloaded onto it AT ALL, and that the result is observable in Memoria.log. Concretely: widen Ff9mkDebugMenu.cs:1403's `id < 9000 || id > 9012` guard (or add a second debug-only text field with no range check) as its own one-line, easily-reverted throwaway edit, distinct from and prior to the WorldDisc spike patch. Verify by setting wldMapNo to any currently-unused id in the existing 4000-9899/30000-32767 bands (no new EventDB entry needed -- just confirm the scene reload fires, `WMScriptDirector.HonoAwake` reads the id, and the game fails gracefully -- e.g. logs the expected 'WorldDisc can't be null' or an EventDB-miss -- rather than silently refusing at the menu layer). This turns 'can I observe a new wldMapNo at all' into its own single-fact spike, decoupled from the 480-block substitution question, so a Rung-0 failure can't be confused with a Rung-0-pre observability gap.\n\nRung 0 -- THE WORLDDISC SPIKE (unchanged target, now with a working verify path): same as the draft, but its 'cheap verify' section should point at Rung 0-pre's now-proven reach mechanism instead of the two broken routes (ForceWorldState-as-is and WMBeeMenu Jump To) currently named. Also explicitly gate the spike behind a flag that defaults OFF and is never fired unconditionally, given the shared-install risk -- drop the 'fire unconditionally on the next WorldMap load' fallback entirely now that Rung 0-pre supplies a safe, targeted way to trigger it.\n\nRung 1a -- THE EVENTDB DIRECTIVE ONLY (narrowed from the draft's Rung 1): just the new DataPatchers.cs WorldScene branch + registering a real new id (e.g. 9013), verified via Rung 0-pre's reach mechanism against the EXISTING WORLD00-12 bytes remapped to the new id (no new blank_world_bytes needed yet -- clone a real dispatcher's bytes verbatim first, the project's own verbatim-first house law). This isolates and verifies the state-axis claim (EventDB has no range check) with zero new byte-editing risk.\n\nRung 1b -- THE MINIMAL WORLD BYTES (new, split out of the draft's Rung 1): only after 1a passes, build `blank_world_bytes()` + the minimal Main_Init splice via eb.cmdasm/eb.edit, verified standalone (does it parse, does DefinePlayerCharacter fire, log-confirmed) before being combined with anything from Rung 0.\n\nRung 2 -- combine 0 + 1a + 1b as in the draft (now three verified, independently-observable pieces instead of two, one of which secretly wasn't observable as specified).\n\nRung 3 -- unchanged; correctly deferred, low engine risk, real but bounded calibration risk (texgates thresholds) already flagged by the plan.\n\nRung 4 -- re-scope upward before starting: add an explicit sub-spike 'can author_entrance's 13-dispatcher loop be generalized to a 14th id at all' (read/extend entrance_func_body's WORLD00-only template source and the 'every dispatcher whose switch carries the case' enumeration) as its own checkpoint before assuming the existing switch-building gap is the only unknown -- this was the plan's largest unflagged scope gap and should not be discovered mid-rung.\n\nNet effect: the CORE risk (does a third world even render) stays probed first and cheaply, per the task's question (c) -- but the reachability precondition that both Rung 0 and Rung 1 silently assumed is pulled out into its own near-zero-cost spike so a later failure to observe results isn't mistaken for the WorldDisc substitution itself failing, and Rung 1/4's true kit-side scope is no longer hidden inside an 'S'/'M' label."
}
```

## Scope-honesty skeptic

```json
{
  "minimum_viable_rung1": "Verified from source: the task's own suggested floor (\"one flat block renders and the player can stand on it\") is not achievable as literally stated. `WMWorld.OnInitialize()`'s position-write loop iterates the full [0,24)x[0,20) grid with no null guard (WMWorld.cs:449-458, verified this session: `wmblock.transform.position = position` on every `Blocks[j,i]` with zero null-check) -- so ANY successful pass through Initialize()/OnInitialize() already requires a complete, gap-free 480-cell array, not \"one block.\" There is no incremental on-ramp; the engine's own hardcoded double loop forces the full grid on attempt one.\n\nGiven that hard floor, the draft's actual Rung 0 (not its Rung 1, which is a separate independent track proving only the EventDB-dispatcher axis on EXISTING disc-1 geometry) is already close to the true minimum, and it correctly does NOT bundle in \"player can stand on it\" -- Rung 0's own success criterion explicitly allows \"look like empty ocean,\" no DefinePlayerCharacter, no new EventDB id, no dispatcher. That is the right minimal spike: 480 WMBlock stubs, ALL IsSea=true (with zero .ff9mesh overrides authored anywhere yet, every cell resolves via the existing s34 reclaim/sea branch straight to the already-loaded stock SeaBlockPrefab -- verified WMWorld.cs:495-507 -- no per-block prefab lookups, no override authoring needed for this first pass), substituted into WorldDisc, reached via an unconditional debug hook. No new dispatcher id is needed for this proof at all.\n\nWhat IS mis-scoped is Rung 2, which currently bundles three separately-novel, previously-untested things into one atomic milestone behind a single pass/fail check: the WorldDisc runtime substitution (Rung 0), a brand-new EventDB dispatcher id (Rung 1), and a from-scratch `blank_world_bytes()` Main_Init splice (zero prior art -- confirmed no WORLD analog of `blank_field_bytes()` exists in ff9mapkit/ff9mapkit/data/__init__.py today). Recommend splitting: Rung 2a = WorldScene 9013 dispatcher pointed at Rung-0's spike geometry via debug-warp only, still no player, still no blank_world_bytes(); Rung 2b = layer in DefinePlayerCharacter once 2a is confirmed clean. That isolates which of the three genuinely novel pieces failed on a bad round, instead of diagnosing all three simultaneously on the very first in-game check -- closer to how this project's own precedent (the scene-ladder) kept each round single-variable.",
  "oversold_claims": [
    "Rung 2's 'sentinel-disc namespace' fork is described as a single, contained extension to WorldMeshOverride's per-cell key ('no engine change should be needed'; 'worst case a second small design iteration, not a rewrite') but the actual call graph is far more scattered. WorldMeshOverride.cs itself IS generic over disc (verified, lines 80-156, zero disc-specific logic) -- but every CALLER hardcodes `this.currentDisc` as the disc argument, at five separate WMWorld.cs sites: LoadBlock's reclaim divert (WMWorld.cs:505,507,547), RegisterBlockComponent's mesh AND texture-override lookups (WMWorld.cs:787-788 and 798-799), RegisterBareObjectOverride (WMWorld.cs:834), and UpdateLoadBlocks' streaming-reload mirror of the same divert (WMWorld.cs:1256-1259). Keeping currentDisc==1 (the plan's own explicit design choice) while routing override lookups to a different sentinel value requires threading brand-new state through all of those sites in the single most central, most-called class in the subsystem -- a materially larger and more scattered patch than 'extend the per-cell key' implies, and it is not named as its own line item in the §4 patch inventory.",
    "§7's cost table gives Rung 1 (the WorldScene directive + blank_world_bytes()) one flat rating -- 'S / Low risk / Heavily precedented' -- that quietly merges two different risk classes the plan's own body correctly keeps separate. The DataPatchers.cs 'WorldScene' directive genuinely is a copy-paste of FieldScene/BattleScene (verified DataPatchers.cs:497-548, matches the proposed shape closely) -- low risk, real precedent. blank_world_bytes(), by contrast, has ZERO prior art: no WORLD equivalent of blank_field_bytes() exists anywhere in the kit today (confirmed via data/__init__.py:32 -- only the field version exists), and that field-side template's own docstring calls itself 'the proven minimal playable field,' language that implies real iteration was needed to land on a Main_Init that behaves -- there is no basis for assuming the WORLD equivalent lands clean on the first try just because the mechanism (clone a donor, strip to a skeleton) is precedented in the FIELD domain.",
    "The phrase 'five-minute in-game spike' undersells Rung 0's own stated correctness bar. The plan itself derives (correctly) that OnInitialize()'s position-write loop (WMWorld.cs:449-458, verified, no null guard) means the FIRST attempt must already supply all 480 (x,y) cells with zero gaps or duplicates just to avoid an immediate NRE -- there is no incremental on-ramp. That is a real, first-of-its-kind, all-480-cells-or-nothing correctness bar with no stock reference shape to sanity-check against, not a five-minute check; it more closely resembles the class of change that took this project's own scene-ladder rung 1a (camera-rig proof on ALREADY-EXISTING geometry, with a stock rig idiom to copy) six full playtest rounds.",
    "§7's 'M-L... worst case a second small design iteration' sizing for Rung 2 presumes Rung 0 resolves to a clean binary pass/fail. But Rung 0's own named failure modes (Wrap()'s loop, ~15 raw ShiftBlocks torus-wrap sites, the self-heal interaction) are exactly the kind that can PARTIALLY misbehave -- hang vs. silent bad state vs. crash -- with no stock-shape reference to triangulate against, unlike every prior scene-ladder rung which could always ask 'what would the stock rig do here.' The plan doesn't flag that a partial, non-crashing misbehavior in genuinely unprecedented territory is usually the expensive kind of bug to characterize, not the cheap kind."
  ],
  "pacing_reality_check": "The plan's nearest real precedent inside this same codebase is the scene-ladder (studies/overworld-topography/scene-ladder/README.md, directly read this session) -- and it is a STRICTLY NARROWER problem than Path D's Rung 0/2: it reused already-populated disc-1/world-9011 geometry throughout and never constructed new WMBlock state, had a stock rig idiom to copy from, had an already-decoded reference mechanism (the F2/9001 flight scene) to reason from, and had its own hard unblocking prerequisite (s64) already banked before it started. Even so it took: rung 0, one round; rung 1a (the camera-rig proof) SIX rounds, explicitly 'probe-verified'; rung 1b two rounds (one a hard softlock, THE CARROT LAW / THE RIG-RADIUS LAW discovered live); rung 1c two rounds (one a hard white-screen lock); rung 2a three rounds; rung 3 at least two iterations (v1 shore read as empty ocean, reframed to v2); and rung 3c surfaced a LATENT correctness bug 'at build time' even after the design was believed closed (the origin-port Int24 cache-order bug, requiring a hall-side repair). That is on the order of 18-20+ discrete playtest-and-relaunch rounds, run across roughly two intensive days, for a feature family this project's own engine authors already had working muscle memory for.\n\nPath D's Rung 0 has none of those advantages: no stock reference shape exists anywhere for 'a synthetic runtime-built 480-block WorldDisc' (this is explicitly the FIRST attempt, per the task framing), and Rung 2's sentinel-disc idea alone (see oversold_claims #1) already touches more scattered call sites than any single scene-ladder rung needed to touch. The plan's §7 sizing -- S for Rung 0, S for Rung 1, 'M-L... mostly plumbing' for Rung 2, overall 'L, multi-session' -- is a defensible size CATEGORY, but its supporting language ('cheap,' 'five-minute spike,' 'heavily precedented,' 'mostly plumbing,' 'a second small design iteration') reads as though Rungs 0-2 could clear in something like the scene-ladder's BEST rungs (1-3 rounds) rather than its typical or worst ones (2-6 rounds), on a problem that is harder and less precedented than anything the ladder attempted. Given this project's own adjacent history, the honest pacing expectation to set for the user is: several relaunch-and-diagnose rounds per rung is the norm here, not the exception; a double-digit total round count across Rungs 0-2 alone would be unsurprising by this project's own track record; and there is a real chance Rung 0's first failure mode is not a clean crash but a confusing partial misbehavior that itself burns 2-3 rounds just to characterize -- mirroring rung 1a's own explicit lesson that 'every deviation from stock shape cost a playtest round,' a lesson that applies MORE severely here since Path D has no stock shape to deviate from in the first place."
}
```
