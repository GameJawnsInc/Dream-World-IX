# Path D — handoff, 2026-07-30

Branch **`claude/dream-world-ix-deploy-9b630f`**, worktree
`C:\gd\Dream-World-IX\.claude\worktrees\angry-williamson-08e8bb`. Not merged to master.

## Where Path D actually is

**Rungs 0–5a are in-game proven.** World 9013 renders a 480-block `WorldDisc` minted in C# at
runtime; the player and the Blue Narciss work; the first authored land exists in a third overworld.

This session's playtest (Step F in [`RUNGS-0-3-PLAYTEST.md`](RUNGS-0-3-PLAYTEST.md)) — **all three
PASS, owner-confirmed**:

| | result |
|---|---|
| **F1** boat no longer sails through the island | PASS |
| **F2** s75 CLONE mode | PASS — "the island carved out itself into the same cells" |
| **F3** mist suppression | PASS — "toggle works as expected" |

Earlier Step E also passed: walkable, cliffs block on foot, meadows render.

## What shipped today

| commit | what |
|---|---|
| `b73c2533` | THE SEA4-UNDER-LAND LAW — `island._cut_plane` takes an `under` mesh and drops Sea4 tris wholly beneath land |
| `fd868b3b` | `terrain.reshape` threaded for the read/write disc split |
| `498d44e3` | Step F playtest script |
| `d54be0f7` | `ff9mapkit world-coastnav` — the R5d/R5e coast-nav stamp, productized |

## THE THING TO READ FIRST

**[[project-ff9-sea4-under-land-law]]** in the memory store, and
`studies/overworld-topography/southern-ring/REVERT.md` §30–§31.

The boat-through-cliffs bug **was already solved on 2026-07-26** by Southern Ring R5d + R5e, on the
Crimson Narciss bench, owner-confirmed with *"that did the trick"*. It was re-derived from scratch
today because the fix only ever existed as a one-off study script — zero lines in `ff9mapkit`, zero
CHANGELOG mentions, and the READ-FIRST overworld skill's law index predates it. The owner remembered
it; the record did not surface it. → [[feedback-own-prior-art-before-new-lanes]].

## Two measurement errors made and corrected today

Both are fixed in the memory file; noting them so they are not re-introduced.

1. **The 56 shipped Southern Ring disc-1 cells are NOT boat-permeable.** I filed that as a defect
   against working code. Measured live: `Block[1][17] Sea4` is 512 tris **all topo 56**; (2,17),
   (3,17), (1,18), (2,18) likewise. The R5e stamp is still live on disc-1. Geometry permeable,
   behaviour not.
2. **"572/576 → 7/576" was under-sampled.** `placement.census`'s default 24×24 over 60u is a 2.61u
   step — too coarse for a ~4u fringe. At 96×96: 138–192 boat-legal columns over y≥3.0 land on every
   one of the five blocks, plus an isolated interior pocket ~46u inside (12,10). Whether any of it
   forms a continuous path in from open water was never established.
   **Match probe resolution to feature size before quoting a residual.**

## The owner's design ruling this session

**Landability policy = `cliffs-refuse`.** Water fronting high walls gets topo **54** — boat-legal, so
you can sail up to it, but the get-off gate demands **53**, so you cannot disembark. Consequence,
accepted deliberately: **the Rung-5a island is unlandable by boat.** That is the coherent form of
"islands without beaches exist" — such an island is not a boat destination, rather than one we bolt a
beach onto. Any island intended as a destination needs a genuine low shore.

Deployed classes on Disc9 (`cliffs-refuse`): KEEL 468 · BELT 282 · CLIFF 1128 · BEACH **0**.
Under `land-anywhere` the same 1128 verts would be BEACH; seal and belt are identical either way.

## Next steps, in order

1. ~~**Playtest the coast-nav stamp.**~~ ✅ **PASS, owner-confirmed 2026-07-30** — sailed all the
   way round: the ~3.5u standoff and the no-landing-anywhere policy both behaved exactly as
   expected. With the offline gates (step 2) this closes the Rung-5a coast-nav lane end to end.
2. ~~**Verify against the study's own gates**~~ ✅ DONE 2026-07-30 (`1725ced2`):
   [`probe_coastnav_disc9.py`](probe_coastnav_disc9.py) re-points the suite at Disc9 sharing the
   kit's own loader/query (origin convention included), with a calibration gate first. **ALL PASS**:
   SEAL 0 leaks / STANDOFF 0.0% / integrity clean / census = the deploy exactly / zero landable 53.
   Results: [`probe_coastnav_disc9_output.txt`](probe_coastnav_disc9_output.txt).
3. **Task #15 — re-snapshot the live disc-1 coast-nav sea files.** ✅ RESOLVED 2026-07-30, and the
   premise was wrong: the backup **did exist** — the `--deploy` run's `ROOT` resolved per-worktree,
   so `pre-coastnav-sea/` was parked in the ephemeral `r3-lamplight-island-overworld-44317f` tree
   and merely read as nonexistent from everywhere else. Now consolidated (verified byte-identical)
   into the MAIN repo's `backups/r3-lamplight.20260726-r3lamplight/`, beside `pre-fringe-sea4/` and
   a new `live-coastnav-sea.20260730/` snapshot of the stamped state (all 65 cells: stamp live,
   disc1/disc4 parity intact). `stamp_coast_nav.py`'s `BACKUP` is now absolute to the main repo.
4. ~~**Task #13 — thread `interior.py` / `fuse.py` / `navimap.py`**~~ ✅ DONE 2026-07-30
   (`e7733e60`), `transplant.py` included (fuse's engine): `--target-disc` on all six remaining
   verbs, interior's deployed-island READ moves too. Live A/B: `--target-disc 9` at (800,−672)
   finds exactly the five Rung-5a blocks; the default found one unrelated REAL disc-1 override at
   (11,9) — the block a carve would have silently reshaped. Open refinement: the orphan-decal
   gate's ring context still reads the stock disc (WARN-only).
5. ~~**`terrain.reclaim` emits no navigation classes**, so any new reclaim is permeable and
   unlandable.~~ ✅ RESOLVED 2026-07-30, in two halves — and the premise was HALF wrong:
   - **`island.landmass` now stamps by default** (`coastnav=True`, policy `land-anywhere`,
     `--skip-coastnav`/`--coastnav-policy` on `world-island`), before the disc-4 mirror so parity
     carries the stamped bytes. Re-stamp is idempotent (measured: 0 changes on the live Disc9 cells).
   - **`terrain.reclaim` needs NO stamp — "permeable" was wrong.** Engine source
     (`WMWorld.cs:1206-1211`): a sidecar-less reclaimed cell uses the inland donor `[12][10]`
     ("no beach/sea") and never loads `SeaBlockPrefab`, so it holds no water meshes at all — the
     boat's probe MISSES → invisible-wall sealed at the cell edge, unlandable by absence of 53. All
     reclaim profiles fill the whole cell with land, so there is no in-cell fringe to classify;
     the hard cell-edge wall is intrinsic to reclaim's geometry. (Engine-source-derived; a boat
     nosing a reclaimed edge has not been specifically playtested.)
6. **The synthesis arc — STARTED 2026-07-30.** Step 2 of the plan (the accepted carry into 9013)
   is done offline: `junction_compose` at the RATIFIED site (160,−1152) went **COMPOSITE GREEN,
   all 36 gates**, deployed as 180 files / 20 cells into Disc9 via
   [`deploy_staged_to_disc9.py`](deploy_staged_to_disc9.py) (manifest + revert in the main repo's
   backups), coast-nav stamped `land-anywhere` (KEEL 19077 / BELT 735 / BEACH 2742, 6.7s), and the
   full gate probe passes over all 25 Disc9 cells (SEAL 0/3393). Same coords as the accepted
   disc-1 island, different namespace — a direct A/B between worlds. ✅ **Playtest PASSED,
   owner-confirmed: "looks identical to the disc-1 island."** The 9013 deploy plumbing (donor
   sidecars, sea rendering, stamp) is proven end to end on a known-good look. **Step 3 ran the
   same day and FAILED its first in-game round** — the terrace wall read as "a mess" (banded
   top, stamped-together sides, flipped transition tiles, missing faces in the bottom third);
   reverted, bench byte-identical again. The scoring — which defects were implementation
   (two SOLVED classes re-derived naively) vs form, and why the discriminant is NOT yet
   refuted — lives in the prediction doc. The synthesis rung is PAUSED on the owner's
   meta-law ruling ("not enough study knowledge to synth yet"); resumption starts with a
   per-instance tile-orientation anatomy study and must reuse the folded generators. Step 3
   (the terrace wall) is prediction-registered in
   [`TERRACE-WALL-PREDICTION.md`](TERRACE-WALL-PREDICTION.md) — read it BEFORE building.

## Open and unresolved

- ~~**`world-coastnav` is SLOW**~~ ✅ RESOLVED 2026-07-30: `coastnav._query_top` now runs on a
  uniform-grid triangle index (bucketed 2D AABBs, first-hit order preserved; calibrated 0/1500
  mismatches against the linear scan). The full Disc9 gate pass that was hour-class ran in minutes.
  The emitter-default fold-in (`world-island`/`terrain.reclaim` emitting navigation classes) is now
  unblocked but still TO DO.
- ~~**A doc says the opposite of the Sea4 cut.**~~ ✅ RECONCILED 2026-07-30 in
  `terrain-entrance.md` itself: the FULL-CELL revelation protects the *fringe*, and `_cut_plane`'s
  AND-over-3-corners drops only tris wholly beneath land, so the outline seam never appears and
  MISS stays 0 (Terrain wins the query first). The cut fixes permeability; coastnav's keel closes
  the straddling remainder. Run both; neither reverts the other.
- **THE COMPATIBILITY LAW may rest on a misreading.** The 4.375u belt ceiling is derived from the
  get-off sweep, but that sweep is a conjunction over eight radii probed with on-foot `limit0`, which
  excludes topos 53–57 outright. `BELT_R = 3.5` is playtest-proven regardless; the *reason* is not.
- **Does a cut Sea4 triangle under land ever render as a void hole?** Placement rule 2 pairs the
  invisible vehicle wall with a void render. It should be occluded by opaque land, but that has not
  been checked from a low camera angle. The topo re-class has no such exposure — it deletes nothing.
- **Is `ff9mapkit/examples/continent-v1` shipping a sailable-through island?** Its README says the
  continent is "reached by boat/airship" and no seal was ever applied. Would be a shipped-example
  defect and a good regression bench.
- ~~**Stale line in the study script**~~ ✅ FIXED 2026-07-30 (`4e277d6e`): the docstring now states
  the v2.3 truth, 56 > 53 > 55.

## Engine state

s70–s75 built and live. **Pre-build DLL backup: `py tools/restore_memoria_dll.py 20260729-173259`.**
Everything Path D is inert until deliberately armed — a normal disc-1/4 load is unaffected, and so is
every other concurrent worktree.
