# JUNCTION-AWARE STRIP CARRY — prediction registration (BEFORE building)

2026-07-31. The strip-carry round stopped as PLUMBING (STRIP-CARRY-PREDICTION.md: carried
faces drew ZERO complaints across both playtests; every defect lived on a minted join),
and the junction grammar study then decoded all three join classes to small lawful
vocabularies (JUNCTION-GRAMMAR.md). The owner chose this round: same carried strips,
joins rebuilt to the measured laws. Written before the first builder edit; the bench is
byte-verified pristine (radius-47, `backups/terrace-strip-prewall.20260731-003703`).

## The claim under test

The ORIGINAL strip-carry claim, finally presented in a judgeable state: **whole-mesh
strips recomposed read as FF9 when the joins follow stock's own junction grammar.**

- **CARRIED**: the same four tier-gated level-chain strips (donor pool unchanged; cut
  columns MAY shift within their level chains if the new profile-match term moves them —
  declared freedom, same ±2-column spirit as round 5's window freedom).
- **MINTED — all three joins rebuilt to the measured laws:**
  1. **SEAMS (the corner law)** — the mortar columns are DELETED. Stock corners are two
     full-tile stations creased at ONE shared edge; ours become that: strips cut at tile
     column boundaries meet at a single shared edge path. Two donors' cross-sections
     differ where stock's are identical, so the composition solve gains an END-PROFILE
     MATCH term (prefer cut columns whose terminal profiles agree), and the residual
     mismatch is absorbed by moving the outgoing strip's boundary verts onto the
     incoming strip's real boundary path, tapered across ONE station width. Carried uv
     untouched on both sides; each seam's tile pair h_pairs-gated; seam u at tile
     boundaries (the dominant lawful case for kinks ≤ 25°).
  2. **TOP (the crest law)** — the minted level top welds EDGE-FOR-EDGE to the strips'
     ACTUAL top once-edge path. The simplified crest polyline, the notch stitch, the
     crest caps, and the top-side component capper are all deleted. The top is level
     plain ground from the weld line (dy ≈ 0, no rim vocabulary), per the law.
  3. **FOOT (the foot law, geometry half)** — the burial pierce is DELETED. Strips are
     cut LEVEL at the bench ground plane (y = 3.2); the ground partition's hole rim IS
     the foot polyline, shared vertex for shared vertex. The composition becomes a
     CLOSED surface over the ground hole — no hidden buried geometry, no rim inset,
     no ground caps.
- **DECLARED DEFERRED**: the foot law's TEXTURE half (the row-10 bottom-course
  transition band). Geometry first, one lever per test; if the verdict names the foot
  texture specifically, that retile is the next single change, v_pairs-gated.

Seat unchanged: crest-anchored at one TOP_Y in the stock shelf band; feasibility bar
per column H ≥ TOP_Y − 3.2 (the same 12.8u-class bar as round 5).

## Gates (the round-5 instrument gap, closed)

- **Watertight, strengthened**: zero once-edges of ANY class except bench block borders.
  Round 5 carried declared classes (hole-rim, buried, mortar-zone); this build declares
  NONE — full closure is the target, not an audit vocabulary.
- h_pairs legality at every seam; massing gates; reach gates on the radius-47 bench.
- **THE GAME-EYE PASS** (banked finding 3): backface-CULLED renders from low, game-like
  camera angles reviewed BEFORE deploy — the class of defect (cull holes, flicker
  geometry) that round 5's once-edge audits could not see.

## REGISTERED PREDICTION

**PASS** — the owner reads it as FF9 interior rock wall. Basis: the carried surfaces
already survived two playtests without a single complaint; every defect named in round 5
maps to a junction this build rebuilds to stock's measured law.

## Falsification semantics — declared in advance

- **FAIL on form** → law-governed junctions cannot rescue recomposition either; the
  wall lane closes to WHOLE-FEATURE CARRY ONLY or rests (the standing FAIL branch).
  If the complaint localizes to the SEAMS specifically, record as partial: strip carry
  + crest/foot laws work, crease absorption is the one residual gap.
- **PLUMBING failure** → fix or stop, no verdict on the claim — **and, new this round:
  a SECOND plumbing stop CLOSES the minted-plan wall lane.** Round 5 already spent the
  "fix the plumbing and retry" credit; the lane does not get to spin twice.
- **PASS** → the wall rung closes SOLVED at strip granularity; `world-terrace`
  productization becomes eligible.

One round. Scored on the owner's verdict, whichever way it lands.
