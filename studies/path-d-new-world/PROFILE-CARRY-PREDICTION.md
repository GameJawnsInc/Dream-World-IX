# PROFILE-CARRY — prediction registration (BEFORE building)

2026-07-30, following the tile-language discriminant's refutation and the massing decode.
Written before the first line of the builder, per the house rule the terrace test set.

## The claim under test

**Rung F's architecture applied to walls**: the look survives when the look's CARRIERS are
verbatim and only the SEAT is minted. The massing decode identified two carriers — the
per-column silhouette wobble (organic, mural-like) and the tile language (decoded to three
instance laws). Profile-carry makes BOTH verbatim-grounded:

- **CARRIED**: a contiguous run of real wall columns' silhouette profiles from ONE stock
  component, in stock order (preserving neighbour-to-neighbour wobble correlation), each
  sampled along its own real polyline at course boundaries; per-column uniform scale
  k ∈ [0.92, 1.08] to seat crest→foot into the bench's 13.8u drop (similarity, batter
  preserved; a donor needing more is REFUSED). Tiles by the three instance laws (gated
  LAW-3 chains, majority orientations, 12% mirrors) — unchanged from round 3.
- **MINTED**: the plan only — the blob seat, the crest ring, the station bearings, the
  grass apron joining the carried foot line to the bench lowland, the junction-L3 top.

What changed vs the refuted rounds, per the massing laws: the foot is the CARRIED smooth
node line joined by a near-flat grass apron (never a lattice jag — right angles are 1% in
stock); the batter is whatever the donor columns carry (~50° family, not the coastal 73);
ledge setbacks arrive verbatim where donor columns have them; columns are geometrically
individual, wobble-correlated as in stock.

## REGISTERED PREDICTION

**PASS** — the owner reads it as FF9 interior rock wall (no "mess"/"tiling"/"sharp edges"
class verdicts) within this single round. Basis: every prior Rung-F-shaped carry has passed
in game (the junction landmass, THE SPUR, the v4 horseshoe), and this build removes the two
carriers the refutation localized the failure to.

## Falsification semantics — declared in advance

- **FAIL on form** → Rung F's frame does NOT extend to walls by profile alone: the look's
  carrier is finer than per-column silhouettes + tiles (candidate: intra-course 3D relief
  of the face sheet). Record it; the wall lane then needs mesh-strip carry (whole real wall
  geometry) or rests.
- **Plumbing failure** (gates red offline) → fix or stop; no verdict on the claim.
- **PASS** → the wall rung closes SOLVED: minted-plan walls are buildable from carried
  profiles + the instance laws; productization (a `world-terrace` verb) becomes eligible.

One round. Scored on the owner's verdict, whichever way it lands.

## AMENDMENT (pre-deploy, 2026-07-30): the seat is BURIAL, not per-column scaling

The k∈[0.92,1.08] search refused every donor: no stock wall keeps even 10 neighbours inside
an ±8% height corridor, because stock column heights swing 9–28u within one run — **crests
are level; the GROUND under a wall undulates**. Demanding a uniform crest-to-floor drop was
the un-stock assumption. Amended to stock's own mechanism, strictly more rigid: **k = 1.0
for the whole run** (one rigid pose, no per-column scaling at all), crest-anchored in the
shelf band, and every column's surplus height BURIES below the bench floor — the visible
foot line is the ground-crossing contour of the carried faces, organic per column. The
donor window requires every column at least (shelf − lowland) tall; course boundaries stay
level (stock's course quantization), sampled off the carried polylines at course heights.
The prediction and its scoring are unchanged.
